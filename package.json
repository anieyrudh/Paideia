{
  "name": "paideia",
  "version": "0.0.0",
  "private": true,
  "description": "Static library for academic mini-apps, simulations, and lesson materials.",
  "license": "SEE LICENSE AND LICENSE-content",
  "packageManager": "pnpm@10.15.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm build:pages",
    "build:pages": "node scripts/build-pages.mjs",
    "test": "pnpm contribution:organize -- --check && pnpm contribution:validate",
    "lint": "node --check scripts/build-pages.mjs && node --check scripts/organize-contributions.mjs && node --check scripts/validate-contributions.mjs",
    "contribution:validate": "node scripts/validate-contributions.mjs",
    "contribution:organize": "node scripts/organize-contributions.mjs"
  },
  "devDependencies": {
    "yaml": "^2.9.0"
  }
}
