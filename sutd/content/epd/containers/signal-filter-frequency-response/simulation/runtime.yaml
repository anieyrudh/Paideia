runtime:
  package: "@paideia/sutd-sims"
  module: "@paideia/sutd-sims/signal-filter-frequency-response"
  export: SignalFilterFrequencyResponse
  package_id: "sutd/epd/signal-filter-frequency-response"
  sim_id: signal-filter-frequency-response
  stages:
    - predict
    - manipulate
    - observe
    - explain
  prediction_gate:
    required: true
    storage_scope: "sutd/epd/signal-filter-frequency-response/signal-filter-frequency-response"
  deterministic: true
  units:
    resistance: kOhm
    capacitance: uF
    frequency: Hz
    angular_frequency: rad/s
    magnitude: dB
    phase: deg
visual_quality:
  setup: []
  prediction:
    option_label: "The output is about 0.707 of the input and the phase has already shifted"
    rationale: "At the cutoff frequency the magnitude drops to 1/sqrt(2) ≈ 0.707 (the -3 dB point) and the phase has already rotated; cutoff is a response marker, not a hard wall."
  observation:
    observation_label: "Observation unlocked"
    visual: required
    formula: required
