# Recursion Tree Complexity · Technical Record

> **Auto-generated by `pnpm container:docs`. Updated by every product PR.**
> Source: `container.yaml`, sim source declarations, and preserved review sections.

## Architecture

- Container path: `sutd/content/csd/containers/recursion-tree-complexity`
- Status: `draft`
- Concept card: `concept-card.md`
- Concept map: `concept-map/concept-map.yaml`
- Simulation spec: `simulation/simulation.yaml`
- Embed API: `embed/api.ts`
- Problem-solving algorithm: `problem-solving/algorithm.md`

## Imports

| Sim | Module | Symbols / role |
|---|---|---|
| recursion-tree-complexity | `core/sim-runtime` | Declared in `simulation/simulation.yaml` |
| recursion-tree-complexity | `core/algorithm-trace` | Declared in `simulation/simulation.yaml` |
| recursion-tree-complexity | `core/graph-layout` | Declared in `simulation/simulation.yaml` |
| recursion-tree-complexity | `core/charting` | Declared in `simulation/simulation.yaml` |
| recursion-tree-complexity | `core/prediction-gate` | Declared in `simulation/simulation.yaml` |
| recursion-tree-complexity | `core/shared` | Declared in `simulation/simulation.yaml` |
| recursion-tree-complexity | `core/ui-sim` | Declared in `simulation/simulation.yaml` |

## SimulationSpec (frozen)

Full validated YAML for the declared sim in this container at the time of last docs regeneration.

```yaml
id: recursion-tree-complexity
title: Recursion Tree Complexity Lab
interaction_type: algorithm-state-visualisation
kernel_deps:
  - core/sim-runtime
  - core/algorithm-trace
  - core/graph-layout
  - core/charting
  - core/prediction-gate
  - core/shared
  - core/ui-sim
predict:
  prompt: For T(n) = 2T(n/2) + n, which part of the recursion tree contributes the same order of work as every other level?
  commit_format:
    kind: multiple-choice
    options:
      - Every level contributes n operations, so the height adds a log n factor.
      - Only the root matters because it sees the full input size.
      - Only the leaves matter because there are many base cases.
      - The base case never changes the total work.
    correct_index: 0
  rationale_required: true
manipulate:
  controls:
    - id: input-size
      label: Input size
      kind: selector
      kernel_binding: state.inputSize
    - id: branching-factor
      label: Recursive calls per node
      kind: selector
      kernel_binding: state.branchingFactor
    - id: shrink-factor
      label: Shrink factor
      kind: selector
      kernel_binding: state.shrinkFactor
    - id: combine-exponent
      label: Combine-work pattern
      kind: selector
      kernel_binding: state.combineExponent
observe:
  renderers:
    - id: recursion-tree-panel
      module: "@paideia/sutd-sims/recursion-tree-complexity"
      symbol: RecursionTreeComplexity
      props_binding: Recursion tree, level-cost chart, formula substitution, and algorithm trace evidence.
explain:
  prompt: Explain why the same recursive shape can be root-heavy, balanced, or leaf-heavy depending on per-node work.
  socratic: true
  expected_misconceptions_surfaced:
    - recursive code always has exponential cost
    - base cases do not affect total work
```

## Kernel extensions

Declared runtime kernel dependencies are listed above. Any additive or breaking core API change must be linked in the preserved review section below.

## Accessibility

- Prediction gate contract: required and tested when the sim is registered.
- Route-level axe coverage: record the latest shell or container-specific result in the preserved review section below.
- Media fallback: `media/fallback.svg` present.

## Tests

- Container validation: `pnpm container:validate sutd/content/csd/containers/recursion-tree-complexity`
- Docs regeneration: `pnpm container:docs sutd/content/csd/containers/recursion-tree-complexity`
- Prediction-gate test: `simulation/simulation.test.ts`
- Package or shell tests: record exact commands in the preserved validation section below.

## How to run locally

```bash
pnpm container:docs sutd/content/csd/containers/recursion-tree-complexity
pnpm container:validate sutd/content/csd/containers/recursion-tree-complexity
pnpm graph:generate
```

## Anieyrudh Filter pass

Date: 2026-05-21
Reviewers (subagents): container-auditor, sim-architect, pedagogy-reviewer

### P0 resolved
- Initial implementation review: no P0 issues recorded before automated gates.

### P1 addressed or deferred
- Browser playtest completed: prediction gate blocked reveal, desktop and mobile revealed states rendered without console errors, mobile horizontal overflow was fixed, and the sim-harness Playwright accessibility test passed.

### P2 noted
- A future shared recurrence-analysis kernel may absorb the local recurrence-tree model once more containers need the same calculations.

## Iteration log

- Regenerated README.md and TECHNICAL.md with `pnpm container:docs`.
