# Graph Search and Shortest Paths · Technical Record

> **Auto-generated by `pnpm container:docs`. Updated by every product PR.**
> Source: `container.yaml`, sim source declarations, and preserved review sections.

## Architecture

- Container path: `sutd/content/csd/containers/graph-search-and-shortest-paths`
- Status: `draft`
- Concept card: `concept-card.md`
- Concept map: `concept-map/concept-map.yaml`
- Simulation spec: `simulation/simulation.yaml`
- Embed API: `embed/api.ts`
- Problem-solving algorithm: `problem-solving/algorithm.md`

## Imports

| Sim | Module | Symbols / role |
|---|---|---|
| graph-search-and-shortest-paths | `core/sim-runtime` | Declared in `simulation/simulation.yaml` |
| graph-search-and-shortest-paths | `core/prediction-gate` | Declared in `simulation/simulation.yaml` |
| graph-search-and-shortest-paths | `core/shared` | Declared in `simulation/simulation.yaml` |
| graph-search-and-shortest-paths | `core/ui-sim` | Declared in `simulation/simulation.yaml` |

## SimulationSpec (frozen)

Full validated YAML for the declared sim in this container at the time of last docs regeneration.

```yaml
id: graph-search-and-shortest-paths
title: Graph Search and Shortest Paths
interaction_type: algorithm-state-visualisation
kernel_deps:
  - core/sim-runtime
  - core/prediction-gate
  - core/shared
  - core/ui-sim
predict:
  prompt: |
    Which statement is correct for directed non-negative weighted graphs?
  commit_format:
    kind: multiple-choice
    options:
      - BFS and Dijkstra optimize the same objective.
      - BFS minimizes edge count; Dijkstra minimizes total non-negative weight.
      - DFS always returns a minimum-cost path first.
    correct_index: 1
  rationale_required: true
manipulate:
  controls:
    - id: traversal-mode
      label: Traversal mode
      kind: selector
      kernel_binding: state.mode
      options:
        - BFS
        - DFS
observe:
  renderers:
    - id: traversal-and-cost-panel
      module: local
      symbol: ObservePanel
      props_binding: Traversal order and Dijkstra cost comparison.
explain:
  prompt: Explain why edge-count shortest paths can differ from weighted shortest paths.
  socratic: true
  expected_misconceptions_surfaced:
    - BFS minimizes weighted cost
```

## Kernel extensions

No core-change proposal is declared in this container. Kernel use is limited to declared dependencies above.

## Accessibility

- Prediction gate contract: required and tested when the sim is registered.
- Route-level axe coverage: record the latest shell or container-specific result in the preserved review section below.
- Media fallback: `media/fallback.svg` present.

## Tests

- Container validation: `pnpm container:validate sutd/content/csd/containers/graph-search-and-shortest-paths`
- Docs regeneration: `pnpm container:docs sutd/content/csd/containers/graph-search-and-shortest-paths`
- Prediction-gate test: `simulation/simulation.test.ts`
- Package or shell tests: record exact commands in the preserved validation section below.

## How to run locally

```bash
pnpm container:docs sutd/content/csd/containers/graph-search-and-shortest-paths
pnpm container:validate sutd/content/csd/containers/graph-search-and-shortest-paths
pnpm graph:generate
```

## Anieyrudh Filter pass

> Required once `container.yaml` reaches the configured Filter lifecycle
> threshold, usually `published`. Draft UI and layout iterations may leave this
> section as a short note explaining why the Filter was not run.

Date: <YYYY-MM-DD>
Filter version: aniegpt v1.0

### P0 issues

(Each P0 must be resolved before publishing. List them and resolution.)

- ...

### P1 issues

(P1 may be deferred with a tracked issue. List them and either resolution or
deferral issue link.)

- ...

### High-bandwidth questions surfaced

- ...

## Iteration log

Brief log of what the agent did and what was rejected during authoring. This
is the "What AI did / what I rejected" record from the PR template, persisted.

- ...
