# Heat Transfer Modes · Technical Record

> **Auto-generated by `pnpm container:docs`. Updated by every product PR.**
> Source: `container.yaml`, sim source declarations, and preserved review sections.

## Architecture

- Container path: `sutd/content/10-023-designing-energy-systems/containers/heat-transfer-modes`
- Status: `reviewed`
- Concept card: `concept-card.md`
- Concept map: `concept-map/concept-map.yaml`
- Simulation spec: `simulation/simulation.yaml`
- Embed API: `embed/api.ts`
- Problem-solving algorithm: `problem-solving/algorithm.md`

## Imports

| Sim | Module | Symbols / role |
|---|---|---|
| heat-flow-comparison-lab | `core/content-schema` | Declared in `simulation/simulation.yaml` |
| heat-flow-comparison-lab | `core/heat-transfer` | Declared in `simulation/simulation.yaml` |
| heat-flow-comparison-lab | `core/prediction-gate` | Declared in `simulation/simulation.yaml` |
| heat-flow-comparison-lab | `core/shared` | Declared in `simulation/simulation.yaml` |
| heat-flow-comparison-lab | `core/thermodynamics` | Declared in `simulation/simulation.yaml` |
| heat-flow-comparison-lab | `core/ui-sim` | Declared in `simulation/simulation.yaml` |

## SimulationSpec (frozen)

Full validated YAML for the declared sim in this container at the time of last docs regeneration.

```yaml
id: heat-flow-comparison-lab
title: Heat Flow Comparison Lab
interaction_type: systems-flow-diagram
kernel_deps:
  - core/content-schema
  - core/heat-transfer
  - core/prediction-gate
  - core/shared
  - core/thermodynamics
  - core/ui-sim
predict:
  prompt: |
    A hot panel loses heat to cooler surroundings. If the temperature difference doubles while geometry stays fixed, what happens to conduction and convection heat rates?
  commit_format:
    kind: multiple-choice
    options:
      - They approximately double because both are proportional to Delta T.
      - They stay unchanged because only material choice matters.
      - They become zero because heat and temperature are the same quantity.
      - They halve because the heat has farther to travel.
    correct_index: 0
  rationale_required: true
manipulate:
  controls:
    - id: hot-temperature
      label: Hot surface temperature
      kind: slider
      kernel_binding: state.hotTemperatureCelsius
      bounds:
        min: 30
        max: 120
        step: 1
    - id: cold-temperature
      label: Ambient temperature
      kind: slider
      kernel_binding: state.coldTemperatureCelsius
      bounds:
        min: 0
        max: 35
        step: 1
    - id: panel-thickness
      label: Wall thickness
      kind: slider
      kernel_binding: state.thicknessMetres
      bounds:
        min: 0.04
        max: 0.4
        step: 0.01
    - id: convection-coefficient
      label: Convection coefficient
      kind: slider
      kernel_binding: state.convectionCoefficientWattsPerSquareMetreKelvin
      bounds:
        min: 4
        max: 35
        step: 1
    - id: emissivity
      label: Surface emissivity
      kind: slider
      kernel_binding: state.emissivity
      bounds:
        min: 0.1
        max: 0.95
        step: 0.05
observe:
  renderers:
    - id: heat-flow-comparison-lab
      module: "@paideia/sutd-sims/heat-transfer-modes"
      symbol: HeatTransferModes
      props_binding: |
        Compare conduction, convection, and radiation heat-flow rates with formula substitution and visual bars.
explain:
  prompt: |
    Which mode is controlled by material thickness, which by fluid motion, and which by absolute temperature?
  socratic: true
  expected_misconceptions_surfaced:
    - Heat and temperature are the same quantity
    - Radiation needs air or a contact path
    - Conduction depends only on area and not on thickness
```

## Kernel extensions

Declared runtime kernel dependencies are listed above. Any additive or breaking core API change must be linked in the preserved review section below.

## Accessibility

- Prediction gate contract: required and tested when the sim is registered.
- Route-level axe coverage: record the latest shell or container-specific result in the preserved review section below.
- Media fallback: `media/fallback.svg` present.

## Tests

- Container validation: `pnpm container:validate sutd/content/10-023-designing-energy-systems/containers/heat-transfer-modes`
- Docs regeneration: `pnpm container:docs sutd/content/10-023-designing-energy-systems/containers/heat-transfer-modes`
- Prediction-gate test: `simulation/simulation.test.ts`
- Package or shell tests: record exact commands in the preserved validation section below.

## How to run locally

```bash
pnpm container:docs sutd/content/10-023-designing-energy-systems/containers/heat-transfer-modes
pnpm container:validate sutd/content/10-023-designing-energy-systems/containers/heat-transfer-modes
pnpm graph:generate
```

## Anieyrudh Filter pass

Date: 2026-05-28

### P0 issues

- None open. The container has the required v2 surfaces, prediction gate,
  visual heat-flow model, formula substitution, cited source, and no copied
  proprietary assets.

### P1 issues

- None open. The sim composes `core/heat-transfer` for conduction,
  convection, and radiation rates and `core/thermodynamics` for Celsius to
  kelvin conversion. It does not inline the reusable heat-transfer kernels.

### P2 follow-ups

- Add a second transfer problem for a heat-exchanger wall where conduction and
  convection are both design constraints.
- Add transient warm-up/cool-down animation only after a reusable transient
  thermal kernel exists.

## Iteration log

- Regenerated README.md and TECHNICAL.md with `pnpm container:docs`.
