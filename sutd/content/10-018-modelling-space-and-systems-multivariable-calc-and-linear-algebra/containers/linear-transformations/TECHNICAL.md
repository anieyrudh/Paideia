# Linear Transformations · Technical Record

> **Auto-generated by `pnpm container:docs`. Updated by every product PR.**
> Source: `container.yaml`, sim source declarations, and preserved review sections.

## Architecture

- Container path: `sutd/content/10-018-modelling-space-and-systems-multivariable-calc-and-linear-algebra/containers/linear-transformations`
- Status: `draft`
- Concept card: `concept-card.md`
- Concept map: `concept-map/concept-map.yaml`
- Simulation spec: `simulation/simulation.yaml`
- Embed API: `embed/api.ts`
- Problem-solving algorithm: `problem-solving/algorithm.md`

## Imports

| Sim | Module | Symbols / role |
|---|---|---|
| linear-transformations | `core/sim-runtime` | Declared in `simulation/simulation.yaml` |
| linear-transformations | `core/linear-algebra` | Declared in `simulation/simulation.yaml` |
| linear-transformations | `core/prediction-gate` | Declared in `simulation/simulation.yaml` |
| linear-transformations | `core/ui-sim` | Declared in `simulation/simulation.yaml` |

## SimulationSpec (frozen)

Full validated YAML for the declared sim in this container at the time of last docs regeneration.

```yaml
id: linear-transformations
title: Linear Transformation Classifier
interaction_type: diagram-builder
kernel_deps:
  - core/sim-runtime
  - core/linear-algebra
  - core/prediction-gate
  - core/ui-sim
predict:
  prompt: |
    Matrix A = [[0, -1], [1, 0]]. Before reveal, which classification best describes this linear transformation?
  commit_format:
    kind: multiple-choice
    options:
      - Pure rotation by 90 degrees counter-clockwise; area preserved, orientation preserved
      - Reflection across the x-axis; orientation flipped
      - Horizontal shear; area preserved, orientation preserved
      - Anisotropic scaling; both basis vectors stretched differently
    correct_index: 0
  rationale_required: true
manipulate:
  controls:
    - id: a11
      label: Top-left entry a
      kind: slider
      kernel_binding: state.a11
      bounds:
        min: -3
        max: 3
        step: 0.5
    - id: a12
      label: Top-right entry b
      kind: slider
      kernel_binding: state.a12
      bounds:
        min: -3
        max: 3
        step: 0.5
    - id: a21
      label: Bottom-left entry c
      kind: slider
      kernel_binding: state.a21
      bounds:
        min: -3
        max: 3
        step: 0.5
    - id: a22
      label: Bottom-right entry d
      kind: slider
      kernel_binding: state.a22
      bounds:
        min: -3
        max: 3
        step: 0.5
observe:
  renderers:
    - id: linear-transformations-readout
      module: "@paideia/sutd-sims/linear-transformations"
      symbol: LinearTransformations
      props_binding: |
        Show the basis images on a grid, the canonical-class verdict, area scale, orientation, and rotation/scaling extraction.
explain:
  prompt: |
    Explain why specifying T(e_1) and T(e_2) is sufficient to describe T on every vector in the plane.
  socratic: true
  expected_misconceptions_surfaced:
    - Linear transformations always preserve length
    - Rotation matrices have nonzero diagonal entries only
```

## Kernel extensions

Declared runtime kernel dependencies are listed above. Any additive or breaking core API change must be linked in the preserved review section below.

## Accessibility

- Prediction gate contract: required and tested when the sim is registered.
- Route-level axe coverage: record the latest shell or container-specific result in the preserved review section below.
- Media fallback: `media/fallback.svg` present.

## Tests

- Container validation: `pnpm container:validate sutd/content/10-018-modelling-space-and-systems-multivariable-calc-and-linear-algebra/containers/linear-transformations`
- Docs regeneration: `pnpm container:docs sutd/content/10-018-modelling-space-and-systems-multivariable-calc-and-linear-algebra/containers/linear-transformations`
- Prediction-gate test: `simulation/simulation.test.ts`
- Package or shell tests: record exact commands in the preserved validation section below.

## How to run locally

```bash
pnpm container:docs sutd/content/10-018-modelling-space-and-systems-multivariable-calc-and-linear-algebra/containers/linear-transformations
pnpm container:validate sutd/content/10-018-modelling-space-and-systems-multivariable-calc-and-linear-algebra/containers/linear-transformations
pnpm graph:generate
```

## Anieyrudh Filter pass

Date: 2026-05-26
Filter version: aniegpt v1.0 (builder self-audit, awaiting reviewer pass)

### P0 issues

- None observed. Container shape validates. Prediction gate is wired end-to-end. Reveal renders an SVG basis-image plot (dashed e_1/e_2 + blue T(e_1) + green T(e_2)) and a colour-coded formula card with substitution, units, and per-class verdict. Computation goes through `core/linear-algebra` (`matrix2`, `determinant2`, `trace2`, `dot2`, `norm2`, `vector2`).

### P1 issues

- Formula colours pair `T(e_1)` (blue) <-> blue arrow, `T(e_2)` (green) <-> green arrow, `det A` (orange), and `tr A` (purple); the legend uses existing `.legend-swatch` classes. Manipulation visibly retargets both image arrows; sliders snap at 0.5 to reveal canonical cases (rotation, scaling, shear, reflection). Substitution + interpretation per case is present.

### P2 issues (deferred)

- Add an inline rotation-angle dial that animates between `T(e_1) = (1, 0)` and the current `T(e_1)` for the rotation case.
- Add a composite-decomposition preset that animates the "rotate then scale" pipeline step by step.
- Add a transfer problem on the affine vs. linear distinction with a translation that breaks linearity.

### High-bandwidth questions surfaced

- Should rotation-angle extraction (`atan2(c, a)`) and the canonical-class diagnosis become a `core/linear-algebra` surface (e.g. `classifyAffine2`) for reuse by downstream graphics-pipeline and modal-analysis containers? Open as a `core-change-proposal` if a second container asks for the same surface.

## Iteration log

- 2026-05-26: Scaffolded the container. Built `sutd/packages/sims/src/linear-transformations.tsx` with SVG basis-image plot, canonical-class diagnosis (identity / rotation / reflection / scaling / shear / singular / composite), rotation-angle extraction, and formula card with colour-coded LaTeX, substitution, and per-class interpretation. Generated README.md and TECHNICAL.md via `pnpm container:docs`.

## Validation log (2026-05-26)

| Check | Result |
| --- | --- |
| `pnpm container:validate sutd/content/10-018-.../containers/linear-transformations` | passed (54 containers OK) |
| `pnpm container:docs sutd/content/10-018-.../containers/linear-transformations` | regenerated README.md and TECHNICAL.md |
| `pnpm graph:generate` | wrote SUTD shell knowledge-graph with the new container entry |
| `pnpm graph:check` | clean |
| `pnpm typecheck` | clean across all workspaces |
| `pnpm lint` | clean across all workspaces |
| `pnpm boundary` | 1212 modules, 2034 deps, no violations |
| `pnpm license:check` | 84 production deps, all compatible |
| `pnpm roadmap:validate` | 54 queue entries OK |
| `pnpm agent:validate` | OK |
| `pnpm test` | vitest suites green; Playwright suites blocked by environment (chrome-headless-shell missing for installed Playwright version) — unrelated to this PR. |
