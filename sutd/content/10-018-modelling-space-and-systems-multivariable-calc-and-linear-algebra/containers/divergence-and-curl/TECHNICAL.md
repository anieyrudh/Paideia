# Divergence and Curl · Technical Record

> **Auto-generated by `pnpm container:docs`. Updated by every product PR.**
> Source: `container.yaml`, sim source declarations, and preserved review sections.

## Architecture

- Container path: `sutd/content/10-018-modelling-space-and-systems-multivariable-calc-and-linear-algebra/containers/divergence-and-curl`
- Status: `draft`
- Concept card: `concept-card.md`
- Concept map: `concept-map/concept-map.yaml`
- Simulation spec: `simulation/simulation.yaml`
- Embed API: `embed/api.ts`
- Problem-solving algorithm: `problem-solving/algorithm.md`

## Imports

| Sim | Module | Symbols / role |
|---|---|---|
| divergence-and-curl | `core/sim-runtime` | Declared in `simulation/simulation.yaml` |
| divergence-and-curl | `core/vector-calculus` | Declared in `simulation/simulation.yaml` |
| divergence-and-curl | `core/prediction-gate` | Declared in `simulation/simulation.yaml` |
| divergence-and-curl | `core/ui-sim` | Declared in `simulation/simulation.yaml` |

## SimulationSpec (frozen)

Full validated YAML for the declared sim in this container at the time of last docs regeneration.

```yaml
id: divergence-and-curl
title: Divergence and Curl
interaction_type: diagram-builder
kernel_deps:
  - core/sim-runtime
  - core/vector-calculus
  - core/prediction-gate
  - core/ui-sim
predict:
  prompt: |
    For the field F = <-y, x>, which local diagnostic is nonzero at the origin?
  commit_format:
    kind: multiple-choice
    options:
      - Curl is nonzero while divergence is zero
      - Divergence is nonzero while curl is zero
      - Both divergence and curl are zero
      - Both divergence and curl are negative
    correct_index: 0
  rationale_required: true
manipulate:
  controls:
    - id: field-kind
      label: Vector field
      kind: selector
      kernel_binding: state.fieldKind
    - id: sample-x
      label: Sample x
      kind: slider
      kernel_binding: state.sampleX
      bounds:
        min: -1.5
        max: 1.5
        step: 0.25
    - id: sample-y
      label: Sample y
      kind: slider
      kernel_binding: state.sampleY
      bounds:
        min: -1.5
        max: 1.5
        step: 0.25
    - id: strength
      label: Field strength
      kind: slider
      kernel_binding: state.strength
      bounds:
        min: 0.5
        max: 2
        step: 0.25
observe:
  renderers:
    - id: divergence-curl-readout
      module: "@paideia/sutd-sims/divergence-and-curl"
      symbol: DivergenceAndCurl
      props_binding: |
        Show field arrows, selected sample point, divergence, curl, and local source/spin interpretation.
explain:
  prompt: |
    Explain why divergence measures local source strength while curl measures local rotation.
  socratic: true
  expected_misconceptions_surfaced:
    - Large arrows automatically mean large divergence
    - Curl means the whole field must move in circles
```

## Kernel extensions

Declared runtime kernel dependencies are listed above. Any additive or breaking core API change must be linked in the preserved review section below.

## Accessibility

- Prediction gate contract: required and tested when the sim is registered.
- Route-level axe coverage: record the latest shell or container-specific result in the preserved review section below.
- Media fallback: `media/fallback.svg` present.

## Tests

- Container validation: `pnpm container:validate sutd/content/10-018-modelling-space-and-systems-multivariable-calc-and-linear-algebra/containers/divergence-and-curl`
- Docs regeneration: `pnpm container:docs sutd/content/10-018-modelling-space-and-systems-multivariable-calc-and-linear-algebra/containers/divergence-and-curl`
- Prediction-gate test: `simulation/simulation.test.ts`
- Package or shell tests: record exact commands in the preserved validation section below.

## How to run locally

```bash
pnpm container:docs sutd/content/10-018-modelling-space-and-systems-multivariable-calc-and-linear-algebra/containers/divergence-and-curl
pnpm container:validate sutd/content/10-018-modelling-space-and-systems-multivariable-calc-and-linear-algebra/containers/divergence-and-curl
pnpm graph:generate
```

## Anieyrudh Filter pass

Date: 2026-05-26
Filter version: aniegpt v1.0

### P0 issues

- None after repair. Reusable divergence/curl computation remains in `core/vector-calculus`, revealed evidence is blocked until prediction commit, and the UI remains student-facing.

### P1 issues

- Runtime dependency drift: `simulation.yaml` declared `core/prediction-gate` while `runtime.yaml` omitted `@paideia/prediction-gate`; fixed.
- Embed lifecycle aliasing: `load()` returned the shared default state object; fixed by returning a fresh copy.
- Gate assertions: every simulation test that reveals evidence now asserts the observation region is absent before commit.
- Source resolution: container sources now include the MIT OCW source used by misconception evidence.

### High-bandwidth questions surfaced

- No high-bandwidth questions.

## Iteration log

- Regenerated README.md and TECHNICAL.md with `pnpm container:docs`.

## P2 notes

- No P2 issues recorded.

## Performance notes

- The sim runs one bounded divergence/curl finite-difference readout plus a 5 by 5 vector-field sample per render. Visuals are SVG-only with no network assets, animation loop, or large allocation.
