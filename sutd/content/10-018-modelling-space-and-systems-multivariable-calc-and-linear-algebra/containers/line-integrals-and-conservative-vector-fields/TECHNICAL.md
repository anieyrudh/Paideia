# Line Integrals and Conservative Vector Fields · Technical Record

> **Auto-generated by `pnpm container:docs`. Updated by every product PR.**
> Source: `container.yaml`, sim source declarations, and preserved review sections.

## Architecture

- Container path: `sutd/content/10-018-modelling-space-and-systems-multivariable-calc-and-linear-algebra/containers/line-integrals-and-conservative-vector-fields`
- Status: `draft`
- Concept card: `concept-card.md`
- Concept map: `concept-map/concept-map.yaml`
- Simulation spec: `simulation/simulation.yaml`
- Embed API: `embed/api.ts`
- Problem-solving algorithm: `problem-solving/algorithm.md`

## Imports

| Sim | Module | Symbols / role |
|---|---|---|
| line-integrals-and-conservative-vector-fields | `core/sim-runtime` | Declared in `simulation/simulation.yaml` |
| line-integrals-and-conservative-vector-fields | `core/vector-calculus` | Declared in `simulation/simulation.yaml` |
| line-integrals-and-conservative-vector-fields | `core/prediction-gate` | Declared in `simulation/simulation.yaml` |
| line-integrals-and-conservative-vector-fields | `core/ui-sim` | Declared in `simulation/simulation.yaml` |

## SimulationSpec (frozen)

Full validated YAML for the declared sim in this container at the time of last docs regeneration.

```yaml
id: line-integrals-and-conservative-vector-fields
title: Line Integrals and Conservative Vector Fields
interaction_type: diagram-builder
kernel_deps:
  - core/sim-runtime
  - core/vector-calculus
  - core/prediction-gate
  - core/ui-sim
predict:
  prompt: |
    For F = grad(0.5x^2 + y) from (0,0) to (2,1), what happens if the path changes from direct to an elbow?
  commit_format:
    kind: multiple-choice
    options:
      - The work stays the same because the field is conservative
      - The work doubles because the elbow is longer
      - The work becomes zero because the start point is the origin
      - The work depends only on the number of sample steps
    correct_index: 0
  rationale_required: true
manipulate:
  controls:
    - id: field-kind
      label: Vector field
      kind: selector
      kernel_binding: state.fieldKind
    - id: curve-kind
      label: Path shape
      kind: selector
      kernel_binding: state.curveKind
    - id: end-x
      label: Endpoint x
      kind: slider
      kernel_binding: state.endX
      bounds:
        min: -2
        max: 2
        step: 0.25
    - id: end-y
      label: Endpoint y
      kind: slider
      kernel_binding: state.endY
      bounds:
        min: -2
        max: 2
        step: 0.25
    - id: bend
      label: Elbow height
      kind: slider
      kernel_binding: state.bend
      bounds:
        min: -2
        max: 2
        step: 0.25
observe:
  renderers:
    - id: line-integrals-readout
      module: "@paideia/sutd-sims/line-integrals-and-conservative-vector-fields"
      symbol: LineIntegralsAndConservativeVectorFields
      props_binding: |
        Show the vector field, selected path, work integral, potential comparison, and path-dependence verdict.
explain:
  prompt: |
    Explain why a conservative field makes line-integral work path independent, and why circulation fields break that shortcut.
  socratic: true
  expected_misconceptions_surfaced:
    - Longer path always means larger line integral
    - Every vector field has a potential function
```

## Kernel extensions

Declared runtime kernel dependencies are listed above. Any additive or breaking core API change must be linked in the preserved review section below.

## Accessibility

- Prediction gate contract: required and tested when the sim is registered.
- Route-level axe coverage: record the latest shell or container-specific result in the preserved review section below.
- Media fallback: `media/fallback.svg` present.

## Tests

- Container validation: `pnpm container:validate sutd/content/10-018-modelling-space-and-systems-multivariable-calc-and-linear-algebra/containers/line-integrals-and-conservative-vector-fields`
- Docs regeneration: `pnpm container:docs sutd/content/10-018-modelling-space-and-systems-multivariable-calc-and-linear-algebra/containers/line-integrals-and-conservative-vector-fields`
- Prediction-gate test: `simulation/simulation.test.ts`
- Package or shell tests: record exact commands in the preserved validation section below.

## How to run locally

```bash
pnpm container:docs sutd/content/10-018-modelling-space-and-systems-multivariable-calc-and-linear-algebra/containers/line-integrals-and-conservative-vector-fields
pnpm container:validate sutd/content/10-018-modelling-space-and-systems-multivariable-calc-and-linear-algebra/containers/line-integrals-and-conservative-vector-fields
pnpm graph:generate
```

## Anieyrudh Filter pass

Date: 2026-05-26
Filter version: aniegpt v1.0

### P0 issues

- None after repair. The simulation keeps reusable vector-field and line-integral computation in `core/vector-calculus`, gates revealed evidence through prediction commit, and avoids branch-specific logic.

### P1 issues

- Runtime dependency drift: `simulation.yaml` declared `core/prediction-gate` but `runtime.yaml` omitted `@paideia/prediction-gate`; fixed by adding the dependency.
- Embed scoring coverage: added an assertion for the `evidenceRevealed` flag so lifecycle regressions are visible in tests.

### High-bandwidth questions surfaced

- No high-bandwidth questions.

## Iteration log

- Regenerated README.md and TECHNICAL.md with `pnpm container:docs`.

## P2 notes

- No P2 issues recorded.

## Performance notes

- The sim runs one bounded `lineIntegral2D` midpoint calculation plus a 5 by 5 vector-field sample per render. Visuals are SVG-only with no network assets, animation loop, or large allocation.
