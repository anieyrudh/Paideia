# Optimisation with Lagrange Multipliers · Technical Record

> **Auto-generated by `pnpm container:docs`. Updated by every product PR.**
> Source: `container.yaml`, sim source declarations, and preserved review sections.

## Architecture

- Container path: `sutd/content/10-018-modelling-space-and-systems-multivariable-calc-and-linear-algebra/containers/optimisation-with-lagrange-multipliers`
- Status: `reviewed`
- Concept card: `concept-card.md`
- Concept map: `concept-map/concept-map.yaml`
- Simulation spec: `simulation/simulation.yaml`
- Embed API: `embed/api.ts`
- Problem-solving algorithm: `problem-solving/algorithm.md`

## Imports

| Sim | Module | Symbols / role |
|---|---|---|
| optimisation-with-lagrange-multipliers | `core/sim-runtime` | Declared in `simulation/simulation.yaml` |
| optimisation-with-lagrange-multipliers | `core/optimization` | Declared in `simulation/simulation.yaml` |
| optimisation-with-lagrange-multipliers | `core/prediction-gate` | Declared in `simulation/simulation.yaml` |
| optimisation-with-lagrange-multipliers | `core/ui-sim` | Declared in `simulation/simulation.yaml` |

## SimulationSpec (frozen)

Full validated YAML for the declared sim in this container at the time of last docs regeneration.

```yaml
id: optimisation-with-lagrange-multipliers
title: Lagrange Multipliers Lab
interaction_type: diagram-builder
kernel_deps:
  - core/sim-runtime
  - core/optimization
  - core/prediction-gate
  - core/ui-sim
predict:
  prompt: |
    At a constrained optimum, what relationship should hold between the objective gradient and the constraint gradient?
  commit_format:
    kind: multiple-choice
    options:
      - They are parallel, so the objective has no first-order gain along the constraint
      - They are perpendicular, so the objective crosses the constraint
      - The objective gradient must be zero everywhere on the constraint
      - The multiplier is the optimum value of the objective
    correct_index: 0
  rationale_required: true
manipulate:
  controls:
    - id: angle-degrees
      label: Constraint point angle
      kind: slider
      kernel_binding: state.angleDegrees
      bounds:
        min: 0
        max: 360
        step: 5
    - id: radius-x
      label: x resource radius
      kind: slider
      kernel_binding: state.radiusX
      bounds:
        min: 1
        max: 4
        step: 0.1
    - id: radius-y
      label: y resource radius
      kind: slider
      kernel_binding: state.radiusY
      bounds:
        min: 1
        max: 4
        step: 0.1
    - id: linear-x
      label: x benefit
      kind: slider
      kernel_binding: state.linearX
      bounds:
        min: 0.5
        max: 6
        step: 0.1
    - id: linear-y
      label: y benefit
      kind: slider
      kernel_binding: state.linearY
      bounds:
        min: 0.5
        max: 6
        step: 0.1
observe:
  renderers:
    - id: lagrange-multipliers-renderer
      module: "@paideia/sutd-sims/optimisation-with-lagrange-multipliers"
      symbol: OptimisationWithLagrangeMultipliers
      props_binding: |
        Show constraint ellipse, selected feasible point, objective and constraint gradients, lambda estimate, tangent derivative residual, formula legend, substitutions, units, and interpretation.
explain:
  prompt: |
    Explain why parallel gradients mean every feasible first-order move has zero objective gain at the optimum.
  socratic: true
  expected_misconceptions_surfaced:
    - The multiplier is the answer rather than a sensitivity
    - The gradients should be perpendicular at an optimum
transfer:
  prompt: |
    A package design has a profit objective and a fixed material-budget constraint. Use Lagrange multipliers to decide when no feasible first-order move improves profit.
  rubric_path: ../problem-solving/packaging-material-budget.md
```

## Kernel extensions

Declared runtime kernel dependencies are listed above. Any additive or breaking core API change must be linked in the preserved review section below.

## Accessibility

- Prediction gate contract: required and tested when the sim is registered.
- Route-level axe coverage: record the latest shell or container-specific result in the preserved review section below.
- Media fallback: `media/fallback.svg` present.

## Tests

- Container validation: `pnpm container:validate sutd/content/10-018-modelling-space-and-systems-multivariable-calc-and-linear-algebra/containers/optimisation-with-lagrange-multipliers`
- Docs regeneration: `pnpm container:docs sutd/content/10-018-modelling-space-and-systems-multivariable-calc-and-linear-algebra/containers/optimisation-with-lagrange-multipliers`
- Prediction-gate test: `simulation/simulation.test.ts`
- Package or shell tests: record exact commands in the preserved validation section below.

## How to run locally

```bash
pnpm container:docs sutd/content/10-018-modelling-space-and-systems-multivariable-calc-and-linear-algebra/containers/optimisation-with-lagrange-multipliers
pnpm container:validate sutd/content/10-018-modelling-space-and-systems-multivariable-calc-and-linear-algebra/containers/optimisation-with-lagrange-multipliers
pnpm graph:generate
```

## Anieyrudh Filter pass

- P0: none.
- P1: none.
- P2: no open issues.

## Performance notes

- The sim computes one closed-form gradient-alignment readout for the selected feasible point and renders SVG vectors; it does not fetch network assets, run an animation loop, or use an iterative solver.

## Iteration log

- Regenerated README.md and TECHNICAL.md with `pnpm container:docs`.
