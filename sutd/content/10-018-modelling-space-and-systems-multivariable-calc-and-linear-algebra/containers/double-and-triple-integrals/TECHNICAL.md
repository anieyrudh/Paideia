# Double and Triple Integrals · Technical Record

> **Auto-generated by `pnpm container:docs`. Updated by every product PR.**
> Source: `container.yaml`, sim source declarations, and preserved review sections.

## Architecture

- Container path: `sutd/content/10-018-modelling-space-and-systems-multivariable-calc-and-linear-algebra/containers/double-and-triple-integrals`
- Status: `reviewed`
- Concept card: `concept-card.md`
- Concept map: `concept-map/concept-map.yaml`
- Simulation spec: `simulation/simulation.yaml`
- Embed API: `embed/api.ts`
- Problem-solving algorithm: `problem-solving/algorithm.md`

## Imports

| Sim | Module | Symbols / role |
|---|---|---|
| double-and-triple-integrals | `core/sim-runtime` | Declared in `simulation/simulation.yaml` |
| double-and-triple-integrals | `core/vector-calculus` | Declared in `simulation/simulation.yaml` |
| double-and-triple-integrals | `core/prediction-gate` | Declared in `simulation/simulation.yaml` |
| double-and-triple-integrals | `core/ui-sim` | Declared in `simulation/simulation.yaml` |

## SimulationSpec (frozen)

Full validated YAML for the declared sim in this container at the time of last docs regeneration.

```yaml
id: double-and-triple-integrals
title: Double and Triple Integrals Lab
interaction_type: diagram-builder
kernel_deps:
  - core/sim-runtime
  - core/vector-calculus
  - core/prediction-gate
  - core/ui-sim
predict:
  prompt: |
    When a rectangular region doubles in one bound, what happens to a constant-density double integral?
  commit_format:
    kind: multiple-choice
    options:
      - It doubles, because twice as much base area is accumulated
      - It stays fixed, because density does not change
      - It squares, because there are two variables
      - It becomes a line integral along the boundary
    correct_index: 0
  rationale_required: true
manipulate:
  controls:
    - id: integral-mode
      label: Integral mode
      kind: selector
      kernel_binding: state.mode
    - id: density-kind
      label: Density field
      kind: selector
      kernel_binding: state.densityKind
    - id: x-max
      label: x upper bound
      kind: slider
      kernel_binding: state.xMax
      bounds:
        min: 1
        max: 5
        step: 0.25
    - id: y-max
      label: y upper bound
      kind: slider
      kernel_binding: state.yMax
      bounds:
        min: 1
        max: 5
        step: 0.25
    - id: z-max
      label: z layer height
      kind: slider
      kernel_binding: state.zMax
      bounds:
        min: 1
        max: 4
        step: 0.25
observe:
  renderers:
    - id: double-triple-integrals-renderer
      module: "@paideia/sutd-sims/double-and-triple-integrals"
      symbol: DoubleAndTripleIntegrals
      props_binding: |
        Show base region, sampled cells, layer stack, double/triple integral readouts, formula legend, substitutions, units, and interpretation.
explain:
  prompt: |
    Explain how an integral accumulates density over area or volume, and why changing bounds changes the accumulated quantity.
  socratic: true
  expected_misconceptions_surfaced:
    - Bounds are independent of the chosen order
    - A double integral is only area, never accumulated density
transfer:
  prompt: |
    A rectangular component has non-uniform areal density. Set up and evaluate a double integral for total mass, then extend it to a constant-height volume.
  rubric_path: ../problem-solving/nonuniform-plate-mass.md
```

## Kernel extensions

Declared runtime kernel dependencies are listed above. Any additive or breaking core API change must be linked in the preserved review section below.

## Accessibility

- Prediction checkpoint contract: required and tested when the sim is registered.
- Route-level axe coverage: record the latest shell or container-specific result in the preserved review section below.
- Media fallback: `media/fallback.svg` present.

## Tests

- Container validation: `pnpm container:validate sutd/content/10-018-modelling-space-and-systems-multivariable-calc-and-linear-algebra/containers/double-and-triple-integrals`
- Docs regeneration: `pnpm container:docs sutd/content/10-018-modelling-space-and-systems-multivariable-calc-and-linear-algebra/containers/double-and-triple-integrals`
- Prediction-checkpoint test: `simulation/simulation.test.ts`
- Package or shell tests: record exact commands in the preserved validation section below.

## How to run locally

```bash
pnpm container:docs sutd/content/10-018-modelling-space-and-systems-multivariable-calc-and-linear-algebra/containers/double-and-triple-integrals
pnpm container:validate sutd/content/10-018-modelling-space-and-systems-multivariable-calc-and-linear-algebra/containers/double-and-triple-integrals
pnpm graph:generate
```

## Anieyrudh Filter pass

- P0: none.
- P1: none.
- P2: no open issues.

## Performance notes

- The sim uses one `doubleIntegralRect` midpoint calculation per render with a bounded grid and SVG-only visuals; no network assets, animation loop, or large allocation is used.

## Iteration log

- Regenerated README.md and TECHNICAL.md with `pnpm container:docs`.
