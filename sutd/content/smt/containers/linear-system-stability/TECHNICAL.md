# Linear System Stability · Technical Record

> **Auto-generated by `pnpm container:docs`. Updated by every product PR.**
> Source: `container.yaml`, sim source declarations, and preserved review sections.

## Architecture

- Container path: `sutd/content/smt/containers/linear-system-stability`
- Status: `reviewed`
- Concept card: `concept-card.md`
- Concept map: `concept-map/concept-map.yaml`
- Simulation spec: `simulation/simulation.yaml`
- Embed API: `embed/api.ts`
- Problem-solving algorithm: `problem-solving/algorithm.md`

## Imports

| Sim | Module | Symbols / role |
|---|---|---|
| linear-system-stability | `core/sim-runtime` | Declared in `simulation/simulation.yaml` |
| linear-system-stability | `core/content-schema` | Declared in `simulation/simulation.yaml` |
| linear-system-stability | `core/dynamical-systems` | Declared in `simulation/simulation.yaml` |
| linear-system-stability | `core/linear-algebra` | Declared in `simulation/simulation.yaml` |
| linear-system-stability | `core/plotting` | Declared in `simulation/simulation.yaml` |
| linear-system-stability | `core/prediction-gate` | Declared in `simulation/simulation.yaml` |
| linear-system-stability | `core/shared` | Declared in `simulation/simulation.yaml` |
| linear-system-stability | `core/ui-sim` | Declared in `simulation/simulation.yaml` |

## SimulationSpec (frozen)

Full validated YAML for the declared sim in this container at the time of last docs regeneration.

```yaml
id: linear-system-stability
title: Linear System Stability Lab
interaction_type: function-plot-with-draggable
kernel_deps:
  - core/sim-runtime
  - core/content-schema
  - core/dynamical-systems
  - core/linear-algebra
  - core/plotting
  - core/prediction-gate
  - core/shared
  - core/ui-sim
predict:
  prompt: |
    For the system matrix you just set, what should happen to a small perturbation from the origin?
  commit_format:
    kind: multiple-choice
    options:
      - It spirals inward and settles near the origin
      - It shoots away along one direction
      - It stops immediately because the origin is stable
      - It grows outward while rotating
    correct_index: 0
  rationale_required: true
manipulate:
  controls:
    - id: preset
      label: System preset
      kind: selector
      kernel_binding: state.preset
    - id: a-entry
      label: a coefficient
      kind: slider
      kernel_binding: state.aPerTimeUnit
      bounds:
        min: -2
        max: 2
        step: 0.05
    - id: b-entry
      label: b coefficient
      kind: slider
      kernel_binding: state.bPerTimeUnit
      bounds:
        min: -2
        max: 2
        step: 0.05
    - id: c-entry
      label: c coefficient
      kind: slider
      kernel_binding: state.cPerTimeUnit
      bounds:
        min: -2
        max: 2
        step: 0.05
    - id: d-entry
      label: d coefficient
      kind: slider
      kernel_binding: state.dPerTimeUnit
      bounds:
        min: -2
        max: 2
        step: 0.05
    - id: initial-x
      label: Initial x
      kind: slider
      kernel_binding: state.initialXStateUnits
      bounds:
        min: -2
        max: 2
        step: 0.05
    - id: initial-y
      label: Initial y
      kind: slider
      kernel_binding: state.initialYStateUnits
      bounds:
        min: -2
        max: 2
        step: 0.05
observe:
  renderers:
    - id: linear-stability-renderer
      module: "@paideia/sutd-sims/linear-system-stability"
      symbol: LinearSystemStability
      props_binding: |
        Show matrix coefficients, stability class, phase portrait, trajectory, eigenvalue formula substitution, units, and interpretation.
explain:
  prompt: |
    Which evidence in the formula panel tells you whether every nearby direction settles, and where could one escaping direction hide?
  socratic: true
  expected_misconceptions_surfaced:
    - Stable means every state stops moving instantly
    - Only one eigenvalue matters
transfer:
  prompt: |
    A control loop linearises to A = [[0, 1], [-0.8, 0.4]]. What changes in the formula evidence, and what stability class follows?
  rubric_path: ../problem-solving/control-loop-stability-check.md
```

## Kernel extensions

Declared runtime kernel dependencies are listed above. Any additive or breaking core API change must be linked in the preserved review section below.

## Accessibility

- Prediction gate contract: required and tested when the sim is registered.
- Route-level axe coverage: record the latest shell or container-specific result in the preserved review section below.
- Media fallback: `media/fallback.svg` present.

## Tests

- Container validation: `pnpm container:validate sutd/content/smt/containers/linear-system-stability`
- Docs regeneration: `pnpm container:docs sutd/content/smt/containers/linear-system-stability`
- Prediction-gate test: `simulation/simulation.test.ts`
- Package or shell tests: record exact commands in the preserved validation section below.

## How to run locally

```bash
pnpm container:docs sutd/content/smt/containers/linear-system-stability
pnpm container:validate sutd/content/smt/containers/linear-system-stability
pnpm graph:generate
```

## Anieyrudh Filter pass

Date: 2026-05-21
Reviewers (subagents): container-auditor, sim-architect, pedagogy-reviewer, test-writer, frontend UI review
Filter version: aniegpt v1.0

### P0 resolved

- Prediction reveal is blocked until commitment - resolution: Playwright test asserts the observation region is absent before commit and visible after commit.
- Reusable math stays in shared kernels - resolution: simulation calls shared linear stability classification, vector-field generation, trajectory sampling, plotting, and eigendirection checks rather than implementing local solvers or classifiers.
- Student-facing UI avoids package names, file paths, kernel names, YAML labels, and implementation details.

### P1 addressed or deferred

- Formula substitution required for shown calculations - addressed in the observation formula panel for trace, determinant, discriminant, eigenvalues, sampled path, units, and interpretation.
- Manipulation must visibly change the state - addressed by a test that switches to the saddle preset and checks the revealed classification and interpretation.
- Misconception coverage - addressed through prediction distractors, concept-map misconceptions, and the explain/transfer prompt.
- Accessibility after reveal - covered by route-level axe test for serious and critical violations.
- Concrete axe evidence: `pnpm -F @paideia/sim-harness test sutd/content/smt/containers/linear-system-stability/simulation/simulation.test.ts` on 2026-05-21 passed 4/4 tests; the revealed-state axe scan reported 0 critical or serious violations.

### P2 noted

- Future iteration can add a trace-determinant plane overlay when the plotting package exposes reusable region shading.

## Iteration log

- Built the SUTD SMT linear system stability container as one v2 container plus one SUTD sim export.
- Kept matrix classification, vector fields, integration, eigendirection checks, and plotting in declared kernels.
- Used generated docs and graph files rather than hand-editing shell graph metadata.
