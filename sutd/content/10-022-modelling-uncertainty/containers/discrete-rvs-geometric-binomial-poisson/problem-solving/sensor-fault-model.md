# Sensor Fault Model

A sensor can fail independently on each inspection, can be observed until the
first fault, or can generate fault counts over an hour. Match each story to
binomial, geometric, or Poisson, then compute the requested probability.
