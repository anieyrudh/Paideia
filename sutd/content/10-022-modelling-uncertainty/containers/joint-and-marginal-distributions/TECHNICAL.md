# Joint and Marginal Distributions · Technical Record

> **Auto-generated by `pnpm container:docs`. Updated by every product PR.**
> Source: `container.yaml`, sim source declarations, and preserved review sections.

## Architecture

- Container path: `sutd/content/10-022-modelling-uncertainty/containers/joint-and-marginal-distributions`
- Status: `reviewed`
- Concept card: `concept-card.md`
- Concept map: `concept-map/concept-map.yaml`
- Simulation spec: `simulation/simulation.yaml`
- Embed API: `embed/api.ts`
- Problem-solving algorithm: `problem-solving/algorithm.md`

## Imports

| Sim | Module | Symbols / role |
|---|---|---|
| joint-table-lab | `core/content-schema` | Declared in `simulation/simulation.yaml` |
| joint-table-lab | `core/shared` | Declared in `simulation/simulation.yaml` |
| joint-table-lab | `core/probability-stats` | Declared in `simulation/simulation.yaml` |
| joint-table-lab | `core/prediction-gate` | Declared in `simulation/simulation.yaml` |
| joint-table-lab | `core/ui-sim` | Declared in `simulation/simulation.yaml` |

## SimulationSpec (frozen)

Full validated YAML for the declared sim in this container at the time of last docs regeneration.

```yaml
schema_version: 1.0.0
id: joint-table-lab
title: Joint and Marginal Distribution Table Lab
module: "@paideia/sutd-sims/joint-and-marginal-distributions"
symbol: JointAndMarginalDistributions
interaction_type: comparative-matrix
prediction_gate: true
kernel_deps:
  - core/content-schema
  - core/shared
  - core/probability-stats
  - core/prediction-gate
  - core/ui-sim
manipulate:
  controls:
    - id: event-a-rate
      label: Marginal P(A)
      kind: slider
      kernel_binding: state.eventA
      bounds:
        min: 0.05
        max: 0.95
        step: 0.01
    - id: event-b-rate
      label: Marginal P(B)
      kind: slider
      kernel_binding: state.eventB
      bounds:
        min: 0.05
        max: 0.95
        step: 0.01
    - id: association
      label: Association
      kind: slider
      kernel_binding: state.association
      bounds:
        min: -0.95
        max: 0.95
        step: 0.01
observe:
  renderers:
    - id: joint-table
      module: "@paideia/sutd-sims/joint-and-marginal-distributions"
      symbol: JointAndMarginalDistributions
      props_binding: Render a 2x2 joint table, marginals, conditional probability formula, substitution, legend, units, and interpretation.
explain:
  prompt: Explain how summing joint cells creates marginals and why conditioning changes the denominator.
  socratic: true
  expected_misconceptions_surfaced:
    - Treating joint and conditional probability as interchangeable.
    - Reading a row total as if it were a joint cell.
```

## Kernel extensions

Declared runtime kernel dependencies are listed above. Any additive or breaking core API change must be linked in the preserved review section below.

## Accessibility

- Prediction checkpoint contract: required and tested when the sim is registered.
- Route-level axe coverage: record the latest shell or container-specific result in the preserved review section below.
- Media fallback: `media/fallback.svg` present.

## Tests

- Container validation: `pnpm container:validate sutd/content/10-022-modelling-uncertainty/containers/joint-and-marginal-distributions`
- Docs regeneration: `pnpm container:docs sutd/content/10-022-modelling-uncertainty/containers/joint-and-marginal-distributions`
- Prediction-checkpoint test: `simulation/simulation.test.ts`
- Package or shell tests: record exact commands in the preserved validation section below.

## How to run locally

```bash
pnpm container:docs sutd/content/10-022-modelling-uncertainty/containers/joint-and-marginal-distributions
pnpm container:validate sutd/content/10-022-modelling-uncertainty/containers/joint-and-marginal-distributions
pnpm graph:generate
```

## Anieyrudh Filter pass

Date: 2026-05-28
Filter version: aniegpt v1.0
Reviewers (local): container-auditor, sim-architect, pedagogy-reviewer

### P0 resolved

- Prediction checkpoint must block joint cells, marginals, conditionals, and formula evidence before commitment - resolved with package-level prediction and route-level Playwright coverage.
- Kernel boundary must preserve probability normalisation - resolved by using `core/probability-stats` to normalise the four-cell joint distribution while keeping learner-specific 2x2 table state local to the sim.

### P1 addressed or deferred

- Learner-facing calculations need formula, substitution, units, interpretation, and legend - addressed in the observe-stage formula panel and tested in `simulation/simulation.test.ts`.
- Misconception coverage must distinguish joint cells, marginal totals, and conditional denominators - addressed in the concept card, concept map, prediction, table copy, and transfer problem.
- Student UI must avoid package names, file paths, kernel names, YAML fields, and queue IDs - addressed by keeping implementation references in docs/tests only, not visible simulation copy.

### High-bandwidth questions surfaced

- P2: Promote a reusable contingency-table helper into `core/probability-stats` if another simulation needs the same 2x2 joint/marginal/conditional calculations.
- P2: Add a second transfer problem with three-category marginals after source review.

## Iteration log

- Scaffolded from a reviewed SUTD container shape, then replaced placeholder content with the SUTD 10.022 joint and marginal distributions slice.
- Built a 2x2 joint table simulation with PMOE-T flow, prediction checkpoint, formula-backed conditional readout, marginals, and transfer problem.
- Corrected scaffold residue in metadata, media, embed contract, and technical review output before validation.
