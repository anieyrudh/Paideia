runtime:
  renderer: react
  state_owner: sim
  persistence: local
  deterministic_sampling: true
  accessibility:
    keyboard_controls: true
    screen_reader_labels: true
    color_not_only_cue: true
visual_quality:
  setup: []
  prediction:
    option_label: "The sample means will be more bell-shaped and less spread out."
    rationale: "Averages should vary less than individual draws."
  observation:
    observation_label: "Observation unlocked"
    visual: required
    formula: required
