# Conditional Probability and Bayes · Technical Record

> **Auto-generated by `pnpm container:docs`. Updated by every product PR.**
> Source: `container.yaml`, sim source declarations, and preserved review sections.

## Architecture

- Container path: `sutd/content/10-022-modelling-uncertainty/containers/conditional-probability-and-bayes`
- Status: `reviewed`
- Concept card: `concept-card.md`
- Concept map: `concept-map/concept-map.yaml`
- Simulation spec: `simulation/simulation.yaml`
- Embed API: `embed/api.ts`
- Problem-solving algorithm: `problem-solving/algorithm.md`

## Imports

| Sim | Module | Symbols / role |
|---|---|---|
| conditional-probability-and-bayes | `core/sim-runtime` | Declared in `simulation/simulation.yaml` |
| conditional-probability-and-bayes | `core/prediction-gate` | Declared in `simulation/simulation.yaml` |
| conditional-probability-and-bayes | `core/probability-stats` | Declared in `simulation/simulation.yaml` |

## SimulationSpec (frozen)

Full validated YAML for the declared sim in this container at the time of last docs regeneration.

```yaml
id: conditional-probability-and-bayes
title: Conditional Probability and Bayes Explorer
interaction_type: decision-matrix
kernel_deps:
  - core/sim-runtime
  - core/prediction-gate
  - core/probability-stats
predict:
  prompt: |
    With prior 10%, sensitivity 95%, and specificity 90%, what is P(H|+) approximately?
  commit_format:
    kind: multiple-choice
    options:
      - 9.5%
      - 34.5%
      - 51.4%
      - 90.0%
    correct_index: 2
  rationale_required: true
manipulate:
  controls:
    - id: prevalence-percent
      label: Prior prevalence P(H)
      kind: slider
      kernel_binding: state.prevalencePercent
      bounds:
        min: 1
        max: 60
        step: 1
    - id: sensitivity-percent
      label: Sensitivity P(+|H)
      kind: slider
      kernel_binding: state.sensitivityPercent
      bounds:
        min: 50
        max: 99
        step: 1
    - id: specificity-percent
      label: Specificity P(-|not H)
      kind: slider
      kernel_binding: state.specificityPercent
      bounds:
        min: 50
        max: 99
        step: 1
observe:
  renderers:
    - id: posterior-readout
      module: "@paideia/sutd-sims/conditional-probability-and-bayes"
      symbol: ConditionalProbabilityBayes
      props_binding: |
        Show Bayes formula, substituted values, and posterior interpretation.
explain:
  prompt: |
    Explain why high sensitivity can still produce moderate posterior when prevalence is low.
  socratic: true
  expected_misconceptions_surfaced:
    - A positive test always means high probability
    - Sensitivity alone determines posterior
```

## Kernel extensions

Declared runtime kernel dependencies are listed above. Any additive or breaking core API change must be linked in the preserved review section below.

## Accessibility

- Prediction gate contract: required and tested when the sim is registered.
- Route-level axe coverage: record the latest shell or container-specific result in the preserved review section below.
- Media fallback: `media/fallback.svg` present.

## Tests

- Container validation: `pnpm container:validate sutd/content/10-022-modelling-uncertainty/containers/conditional-probability-and-bayes`
- Docs regeneration: `pnpm container:docs sutd/content/10-022-modelling-uncertainty/containers/conditional-probability-and-bayes`
- Prediction-gate test: `simulation/simulation.test.ts`
- Package or shell tests: record exact commands in the preserved validation section below.

## How to run locally

```bash
pnpm container:docs sutd/content/10-022-modelling-uncertainty/containers/conditional-probability-and-bayes
pnpm container:validate sutd/content/10-022-modelling-uncertainty/containers/conditional-probability-and-bayes
pnpm graph:generate
```

## Anieyrudh Filter pass

Date: 2026-05-28

### P0 issues

- None open. The container has the required v2 surfaces, prediction gate,
  cited sources, an SVG evidence-flow visual, formula substitution, and
  package-boundary-safe simulation entrypoint. A follow-up local review found
  that the first reveal was too text-heavy; that was repaired before merge.

### P1 issues

- None open. The posterior computation consumes `core/probability-stats`
  through `bayesPositiveEvidence`; the sim does not inline the probability
  normalisation kernel.

### P2 follow-ups

- Add a second transfer problem that uses a design-screening or quality-control
  context rather than a rare-fault sensor.
- Add a fuller probability-tree diagram with adjustable branch widths after
  learner testing confirms the current evidence-flow visual is not enough.

## Iteration log

- Kept the container focused on prior prevalence, sensitivity, specificity,
  true-positive weight, false-positive weight, and the explicit posterior
  formula.
