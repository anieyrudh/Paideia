# Linear Regression · Technical Record

> **Auto-generated by `pnpm container:docs`. Updated by every product PR.**
> Source: `container.yaml`, sim source declarations, and preserved review sections.

## Architecture

- Container path: `sutd/content/10-022-modelling-uncertainty/containers/linear-regression`
- Status: `reviewed`
- Concept card: `concept-card.md`
- Concept map: `concept-map/concept-map.yaml`
- Simulation spec: `simulation/simulation.yaml`
- Embed API: `embed/api.ts`
- Problem-solving algorithm: `problem-solving/algorithm.md`

## Imports

| Sim | Module | Symbols / role |
|---|---|---|
| linear-regression | `core/content-schema` | Declared in `simulation/simulation.yaml` |
| linear-regression | `core/shared` | Declared in `simulation/simulation.yaml` |
| linear-regression | `core/sim-runtime` | Declared in `simulation/simulation.yaml` |
| linear-regression | `core/numerical-math` | Declared in `simulation/simulation.yaml` |
| linear-regression | `core/probability-stats` | Declared in `simulation/simulation.yaml` |
| linear-regression | `core/plotting` | Declared in `simulation/simulation.yaml` |
| linear-regression | `core/prediction-gate` | Declared in `simulation/simulation.yaml` |
| linear-regression | `core/ui-sim` | Declared in `simulation/simulation.yaml` |

## SimulationSpec (frozen)

Full validated YAML for the declared sim in this container at the time of last docs regeneration.

```yaml
id: linear-regression
title: Linear Regression Fit Lab
interaction_type: other
kernel_deps:
  - core/content-schema
  - core/shared
  - core/sim-runtime
  - core/numerical-math
  - core/probability-stats
  - core/plotting
  - core/prediction-gate
  - core/ui-sim
predict:
  prompt: |
    If one high-x observation is pulled upward while the rest of the cloud stays similar, what happens to the least-squares line?
  commit_format:
    kind: multiple-choice
    options:
      - The fitted slope usually increases
      - Only the intercept can change
      - R squared must become exactly 1
      - The line ignores the shifted point
    correct_index: 0
  rationale_required: true
manipulate:
  controls:
    - id: dataset
      label: Dataset
      kind: selector
      kernel_binding: state.dataset
    - id: outlier-shift
      label: Outlier shift
      kind: slider
      kernel_binding: state.outlierShift
      bounds:
        min: -12
        max: 12
        step: 1
    - id: noise-level
      label: Pattern noise
      kind: slider
      kernel_binding: state.noiseLevel
      bounds:
        min: 0
        max: 6
        step: 1
observe:
  renderers:
    - id: least-squares-fit
      module: "@paideia/sutd-sims/linear-regression"
      symbol: LinearRegression
      props_binding: |
        Display scatter plot, least-squares line, slope, intercept, R squared, residual table, formula, substitution, units, and interpretation.
explain:
  prompt: |
    Explain why the least-squares line balances residuals rather than passing through every point.
  socratic: true
  expected_misconceptions_surfaced:
    - The regression line must pass through all observations.
    - A high R squared proves causation.
```

## Kernel extensions

Declared runtime kernel dependencies are listed above. Any additive or breaking core API change must be linked in the preserved review section below.

## Accessibility

- Prediction gate contract: required and tested when the sim is registered.
- Route-level axe coverage: record the latest shell or container-specific result in the preserved review section below.
- Media fallback: `media/fallback.svg` present.

## Tests

- Container validation: `pnpm container:validate sutd/content/10-022-modelling-uncertainty/containers/linear-regression`
- Docs regeneration: `pnpm container:docs sutd/content/10-022-modelling-uncertainty/containers/linear-regression`
- Prediction-gate test: `simulation/simulation.test.ts`
- Package or shell tests: record exact commands in the preserved validation section below.

## How to run locally

```bash
pnpm container:docs sutd/content/10-022-modelling-uncertainty/containers/linear-regression
pnpm container:validate sutd/content/10-022-modelling-uncertainty/containers/linear-regression
pnpm graph:generate
```

## Anieyrudh Filter pass

Date: 2026-05-28
Filter version: aniegpt v1.0

### P0 issues

- Prediction-gate reveal leak: resolved by routing slope, intercept, R squared, residual table, formula evidence, and interpretation through `SimRuntime` with package-level prediction and Playwright pre-commit DOM assertions.
- Text-only reveal risk: resolved with a scatter plot plus least-squares fitted line, residual table, and metric cards.

### P1 issues

- Formula panel incompleteness: resolved with formula, legend, substituted mean/slope/intercept values, dataset units, and interpretation beside the visual model.
- Misconception coverage too implicit: resolved by mapping interpolation, causation, and extrapolation misconceptions into `container.yaml`, concept card, concept map, formula interpretation, and transfer.
- Kernel boundary risk: resolved by consuming `core/numerical-math.linearRegression` and `core/probability-stats.summarize`; no regression fitting logic is duplicated in the container.
- Accessibility risk: addressed with semantic regions, labelled formula/legend sections, text alternatives, and axe coverage.

### P2 follow-up

- Promote a reusable residual-diagnostics helper if another regression or model-checking sim needs residual sums, residual standard error, or leverage calculations.
- Add a second transfer problem comparing interpolation, extrapolation, and causation claims after source review.

### High-bandwidth questions surfaced

- Should `core/plotting` expose labelled axis annotations for regression scatter plots rather than relying on nearby textual labels?
- Should generated graph metadata represent queue dependencies differently from curricular prerequisites?

## Iteration log

- Built the SUTD 10.022 container from the queue item only.
- Added package unit tests before broad validation.
- Replaced scaffold placeholder docs, embed API, media, and Playwright tests with product content.
