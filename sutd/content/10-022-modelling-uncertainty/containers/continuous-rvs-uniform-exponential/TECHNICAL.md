# Continuous RVs: Uniform, Exponential · Technical Record

> **Auto-generated by `pnpm container:docs`. Updated by every product PR.**
> Source: `container.yaml`, sim source declarations, and preserved review sections.

## Architecture

- Container path: `sutd/content/10-022-modelling-uncertainty/containers/continuous-rvs-uniform-exponential`
- Status: `reviewed`
- Concept card: `concept-card.md`
- Concept map: `concept-map/concept-map.yaml`
- Simulation spec: `simulation/simulation.yaml`
- Embed API: `embed/api.ts`
- Problem-solving algorithm: `problem-solving/algorithm.md`

## Imports

| Sim | Module | Symbols / role |
|---|---|---|
| continuous-density-lab | `core/content-schema` | Declared in `simulation/simulation.yaml` |
| continuous-density-lab | `core/shared` | Declared in `simulation/simulation.yaml` |
| continuous-density-lab | `core/probability-stats` | Declared in `simulation/simulation.yaml` |
| continuous-density-lab | `core/prediction-gate` | Declared in `simulation/simulation.yaml` |
| continuous-density-lab | `core/ui-sim` | Declared in `simulation/simulation.yaml` |

## SimulationSpec (frozen)

Full validated YAML for the declared sim in this container at the time of last docs regeneration.

```yaml
schema_version: 1.0.0
id: continuous-density-lab
title: Continuous Random Variable Density Lab
module: "@paideia/sutd-sims/continuous-rvs-uniform-exponential"
symbol: ContinuousRvsUniformExponential
interaction_type: comparative-matrix
prediction_gate: true
kernel_deps:
  - core/content-schema
  - core/shared
  - core/probability-stats
  - core/prediction-gate
  - core/ui-sim
manipulate:
  controls:
    - id: model
      label: Density family
      kind: selector
      kernel_binding: state.model
    - id: minimum
      label: Uniform minimum
      kind: slider
      kernel_binding: state.min
      bounds:
        min: 0
        max: 9
        step: 0.1
    - id: maximum
      label: Uniform maximum
      kind: slider
      kernel_binding: state.max
      bounds:
        min: 0.5
        max: 10
        step: 0.1
    - id: rate
      label: Exponential rate
      kind: slider
      kernel_binding: state.rate
      bounds:
        min: 0.1
        max: 2
        step: 0.01
    - id: lower-bound
      label: Interval lower bound
      kind: slider
      kernel_binding: state.lower
      bounds:
        min: 0
        max: 10
        step: 0.1
    - id: upper-bound
      label: Interval upper bound
      kind: slider
      kernel_binding: state.upper
      bounds:
        min: 0.1
        max: 12
        step: 0.1
observe:
  renderers:
    - id: density-readout
      module: "@paideia/sutd-sims/continuous-rvs-uniform-exponential"
      symbol: ContinuousRvsUniformExponential
      props_binding: Render PDF curve, interval shading, formula, legend, substitution, mean, variance, probability, units, and interpretation.
explain:
  prompt: Explain why continuous probabilities come from area under a density, not point height.
  socratic: true
  expected_misconceptions_surfaced:
    - Reading a point on the density curve as a probability.
    - Treating exponential waiting time like a bounded uniform interval.
```

## Kernel extensions

Declared runtime kernel dependencies are listed above. Any additive or breaking core API change must be linked in the preserved review section below.

## Accessibility

- Prediction checkpoint contract: required and tested when the sim is registered.
- Route-level axe coverage: record the latest shell or container-specific result in the preserved review section below.
- Media fallback: `media/fallback.svg` present.

## Tests

- Container validation: `pnpm container:validate sutd/content/10-022-modelling-uncertainty/containers/continuous-rvs-uniform-exponential`
- Docs regeneration: `pnpm container:docs sutd/content/10-022-modelling-uncertainty/containers/continuous-rvs-uniform-exponential`
- Prediction-checkpoint test: `simulation/simulation.test.ts`
- Package or shell tests: record exact commands in the preserved validation section below.

## How to run locally

```bash
pnpm container:docs sutd/content/10-022-modelling-uncertainty/containers/continuous-rvs-uniform-exponential
pnpm container:validate sutd/content/10-022-modelling-uncertainty/containers/continuous-rvs-uniform-exponential
pnpm graph:generate
```

## Anieyrudh Filter pass

Date: 2026-05-28
Filter version: aniegpt v1.0
Reviewers (local): container-auditor, sim-architect, pedagogy-reviewer

### P0 resolved

- Prediction checkpoint must block density area, moment, and formula evidence before commitment - resolved with package-level prediction and route-level Playwright coverage.
- Kernel boundary must not invent reusable statistics APIs inside content - resolved by keeping the local sim to distribution-specific PDF/CDF formulas while relying on existing Paideia runtime, schema, shared result, prediction-checkpoint, and UI kernels. `core/probability-stats` does not currently own continuous distribution constructors.

### P1 addressed or deferred

- Learner-facing calculations need formula, substitution, units, interpretation, and legend - addressed in the observe-stage formula panel with PDF curve, shaded interval area, substituted bounds/rate, unitless probability, and reason sentence.
- Misconception coverage must distinguish density height from probability - addressed in the concept card, misconception map, prediction copy, formula panel, and transfer problem.
- Student UI must avoid package names, file paths, kernel names, YAML fields, and queue IDs - addressed by keeping implementation references in docs/tests only, not visible simulation copy.

### High-bandwidth questions surfaced

- P2: Promote reusable continuous distribution helpers for Uniform and Exponential PDF/CDF/moments into `core/probability-stats` if another simulation needs the same formulas.
- P2: Add a transfer item contrasting exponential memorylessness with conditional uniform intervals after course-source review.

## Iteration log

- Scaffolded from a reviewed SUTD container shape, then replaced placeholder content with the SUTD 10.022 continuous random variables slice.
- Built a Uniform-vs-Exponential density lab with PMOE-T flow, prediction checkpoint, PDF visualization, interval area, formula-backed readouts, and transfer problem.
- Corrected scaffold residue in metadata, media, embed contract, and technical review output before validation.
