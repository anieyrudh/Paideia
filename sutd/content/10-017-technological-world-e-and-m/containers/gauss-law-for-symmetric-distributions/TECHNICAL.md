# Gauss Law for Symmetric Distributions · Technical Record

> **Auto-generated by `pnpm container:docs`. Updated by every product PR.**
> Source: `container.yaml`, sim source declarations, and preserved review sections.

## Architecture

- Container path: `sutd/content/10-017-technological-world-e-and-m/containers/gauss-law-for-symmetric-distributions`
- Status: `draft`
- Concept card: `concept-card.md`
- Concept map: `concept-map/concept-map.yaml`
- Simulation spec: `simulation/simulation.yaml`
- Embed API: `embed/api.ts`
- Problem-solving algorithm: `problem-solving/algorithm.md`

## Imports

| Sim | Module | Symbols / role |
|---|---|---|
| gauss-law-flux-surface-lab | `core/sim-runtime` | Declared in `simulation/simulation.yaml` |
| gauss-law-flux-surface-lab | `core/electromagnetism` | Declared in `simulation/simulation.yaml` |
| gauss-law-flux-surface-lab | `core/plotting` | Declared in `simulation/simulation.yaml` |
| gauss-law-flux-surface-lab | `core/prediction-gate` | Declared in `simulation/simulation.yaml` |
| gauss-law-flux-surface-lab | `core/shared` | Declared in `simulation/simulation.yaml` |
| gauss-law-flux-surface-lab | `core/ui-sim` | Declared in `simulation/simulation.yaml` |

## SimulationSpec (frozen)

Full validated YAML for the declared sim in this container at the time of last docs regeneration.

```yaml
id: gauss-law-flux-surface-lab
title: Gauss Law Flux Surface Lab
interaction_type: diagram-builder
kernel_deps:
  - core/sim-runtime
  - core/electromagnetism
  - core/plotting
  - core/prediction-gate
  - core/shared
  - core/ui-sim
manipulate:
  controls:
    - id: symmetry
      label: Symmetry
      kind: selector
      kernel_binding: state.symmetry
    - id: enclosed-charge
      label: Enclosed charge
      kind: slider
      kernel_binding: state.enclosedChargeNanoCoulombs
      bounds:
        min: -8
        max: 8
        step: 0.5
    - id: line-charge-density
      label: Line charge density
      kind: slider
      kernel_binding: state.linearChargeDensityNanoCoulombsPerMetre
      bounds:
        min: -8
        max: 8
        step: 0.5
    - id: surface-charge-density
      label: Surface charge density
      kind: slider
      kernel_binding: state.surfaceChargeDensityNanoCoulombsPerSquareMetre
      bounds:
        min: -8
        max: 8
        step: 0.5
    - id: radius
      label: Gaussian radius
      kind: slider
      kernel_binding: state.radiusMetres
      bounds:
        min: 0.15
        max: 1.2
        step: 0.05
    - id: length
      label: Cylinder length
      kind: slider
      kernel_binding: state.lengthMetres
      bounds:
        min: 0.5
        max: 3
        step: 0.1
    - id: face-area
      label: Pillbox face area
      kind: slider
      kernel_binding: state.faceAreaSquareMetres
      bounds:
        min: 0.1
        max: 1.5
        step: 0.05
predict:
  prompt: |
    A point charge is centered inside a Gaussian sphere. If the sphere radius doubles while the enclosed charge stays fixed, what happens to the total electric flux through the sphere?
  commit_format:
    kind: multiple-choice
    options:
      - The total flux stays the same because it depends only on enclosed charge.
      - The total flux doubles because the surface area doubles.
      - The total flux falls by a factor of four because the field weakens.
      - The total flux becomes zero because the surface is closed.
    correct_index: 0
  rationale_required: true
observe:
  renderers:
    - id: gauss-law-flux-surface-lab
      module: "@paideia/sutd-sims/gauss-law-for-symmetric-distributions"
      symbol: GaussLawFluxSurfaceLab
      props_binding: |
        Show Gaussian surface symmetry, flux arrows, field strength, formula substitution, units, legend, and interpretation.
explain:
  prompt: |
    Why does Gauss law become easiest only when symmetry makes the field constant on the chosen surface?
  socratic: true
  expected_misconceptions_surfaced:
    - Gauss law only works for spheres.
    - Flux is the same as electric field strength.
    - A larger Gaussian surface must always give larger flux.
```

## Kernel extensions

Declared runtime kernel dependencies are listed above. Any additive or breaking core API change must be linked in the preserved review section below.

## Accessibility

- Prediction gate contract: required and tested when the sim is registered.
- Route-level axe coverage: record the latest shell or container-specific result in the preserved review section below.
- Media fallback: `media/fallback.svg` present.

## Tests

- Container validation: `pnpm container:validate sutd/content/10-017-technological-world-e-and-m/containers/gauss-law-for-symmetric-distributions`
- Docs regeneration: `pnpm container:docs sutd/content/10-017-technological-world-e-and-m/containers/gauss-law-for-symmetric-distributions`
- Prediction-gate test: `simulation/simulation.test.ts`
- Package or shell tests: record exact commands in the preserved validation section below.

## How to run locally

```bash
pnpm container:docs sutd/content/10-017-technological-world-e-and-m/containers/gauss-law-for-symmetric-distributions
pnpm container:validate sutd/content/10-017-technological-world-e-and-m/containers/gauss-law-for-symmetric-distributions
pnpm graph:generate
```

## Anieyrudh Filter pass

> Required once `container.yaml` reaches the configured Filter lifecycle
> threshold, usually `published`. Draft UI and layout iterations may leave this
> section as a short note explaining why the Filter was not run.

Date: 2026-05-27
Filter version: aniegpt v1.0

### P0 issues

(Each P0 must be resolved before publishing. List them and resolution.)

- None open after local product review on 2026-05-27. The reveal state includes a visible vector-field plot and Gaussian surface overlay, so it is not text-only.

### P1 issues

(P1 may be deferred with a tracked issue. List them and either resolution or
deferral issue link.)

- None open after local product review on 2026-05-27. Prediction-gate behavior is covered by both package and route-level tests.

### P2 issues

- The vector-field plot uses normalized arrows for readability. A future polish pass could vary arrow opacity by relative field magnitude once the shared plotting package exposes magnitude styling hooks.

### Performance notes

- The simulation uses closed-form Gauss-law helpers and a 9-by-9 normalized vector field, so render cost is bounded and independent of time stepping.
- No new runtime dependencies were added.

### Validation notes

- `pnpm --dir testing/sim-harness test -- sutd/content/10-017-technological-world-e-and-m/containers/gauss-law-for-symmetric-distributions/simulation/simulation.test.ts` passed on 2026-05-27 after rerunning with localhost permission for Playwright.

### High-bandwidth questions surfaced

- Whether the course wants signed flux shown as negative numeric output or as an outward/inward direction convention. The current surface preserves signed values and labels direction in learner-facing copy.

## Iteration log

Brief log of what the agent did and what was rejected during authoring. This
is the "What AI did / what I rejected" record from the PR template, persisted.

- Claimed the queue row as `in-build`, scaffolded the canonical v2 container, and replaced placeholder content with a product slice for spherical, cylindrical, and planar symmetry.
- Reused the shared electromagnetism Gauss-law helper for all flux and field calculations, `core/plotting` for field vectors, and `core/ui-sim` controls for manipulation.
- Rejected a text-only reveal: the observe stage now shows both vector arrows and Gaussian surface geometry before the formula/result panel.
