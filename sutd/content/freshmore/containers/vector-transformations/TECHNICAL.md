# Vector Transformations · Technical Record

> **Auto-generated by `pnpm container:docs`. Updated by every product PR.**
> Source: `container.yaml`, sim source declarations, and preserved review sections.

## Architecture

- Container path: `sutd/content/freshmore/containers/vector-transformations`
- Status: `draft`
- Concept card: `concept-card.md`
- Concept map: `concept-map/concept-map.yaml`
- Simulation spec: `simulation/simulation.yaml`
- Embed API: `embed/api.ts`
- Problem-solving algorithm: `problem-solving/algorithm.md`

## Imports

| Sim | Module | Symbols / role |
|---|---|---|
| vector-transformations-sim | `core/sim-runtime` | Declared in `simulation/simulation.yaml` |
| vector-transformations-sim | `core/prediction-gate` | Declared in `simulation/simulation.yaml` |
| vector-transformations-sim | `core/linear-algebra` | Declared in `simulation/simulation.yaml` |
| vector-transformations-sim | `core/ui-sim` | Declared in `simulation/simulation.yaml` |

## SimulationSpec (frozen)

Full validated YAML for the declared sim in this container at the time of last docs regeneration.

```yaml
id: vector-transformations-sim
title: 2D Matrix-Vector Transformation Explorer
interaction_type: diagram-builder
kernel_deps:
  - core/sim-runtime
  - core/prediction-gate
  - core/linear-algebra
  - core/ui-sim
manipulate:
  controls:
    - id: a11
      label: a11
      kind: slider
      kernel_binding: state.a11
      bounds:
        min: -3
        max: 3
        step: 1
observe:
  renderers:
    - id: transform-readout
      module: ../sims/vector-transformations
      symbol: VectorTransformations
      props_binding: matrix entries + vector
explain:
  prompt: |
    Use basis movement: where does e1 go? where does e2 go? Then combine those moves to explain T(x,y).
  socratic: true
  expected_misconceptions_surfaced:
    - Columns are outputs for basis vectors
```

## Kernel extensions

No core-change proposal is declared in this container. Kernel use is limited to declared dependencies above.

## Accessibility

- Prediction gate contract: required and tested when the sim is registered.
- Route-level axe coverage: record the latest shell or container-specific result in the preserved review section below.
- Media fallback: `media/fallback.svg` present.

## Tests

- Container validation: `pnpm container:validate sutd/content/freshmore/containers/vector-transformations`
- Docs regeneration: `pnpm container:docs sutd/content/freshmore/containers/vector-transformations`
- Prediction-gate test: `simulation/simulation.test.ts`
- Package or shell tests: record exact commands in the preserved validation section below.

## How to run locally

```bash
pnpm container:docs sutd/content/freshmore/containers/vector-transformations
pnpm container:validate sutd/content/freshmore/containers/vector-transformations
pnpm graph:generate
```

## Anieyrudh Filter pass

> Required once `container.yaml` reaches the configured Filter lifecycle
> threshold, usually `published`. Draft UI and layout iterations may leave this
> section as a short note explaining why the Filter was not run.

Date: 2026-05-19
Filter version: aniegpt v1.0

### P0 issues

(Each P0 must be resolved before publishing. List them and resolution.)

- ...

### P1 issues

(P1 may be deferred with a tracked issue. List them and either resolution or
deferral issue link.)

- ...

### High-bandwidth questions surfaced

- ...

## Iteration log

Brief log of what the agent did and what was rejected during authoring. This
is the "What AI did / what I rejected" record from the PR template, persisted.

- ...
