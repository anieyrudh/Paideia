# Bayes Updating Mindmap

```mermaid
mindmap
  root((Bayes Updating))
    Inputs
      "prior P(H)"
      "sensitivity P(+|H)"
      "specificity P(-|not H)"
    Evidence routes
      "true positives"
      "false positives"
      "all positive results"
    Posterior
      "P(H|+)"
      "dimensionless probability"
      "base rate matters"
    Misconceptions
      "positive means true"
      "sensitivity equals posterior"
      "false positives ignored"
```
