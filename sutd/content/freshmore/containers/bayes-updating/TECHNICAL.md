# Bayes Updating · Technical Record

> **Auto-generated by `pnpm container:docs`. Updated by every product PR.**
> Source: `container.yaml`, sim source declarations, and preserved review sections.

## Architecture

- Container path: `sutd/content/freshmore/containers/bayes-updating`
- Status: `draft`
- Concept card: `concept-card.md`
- Concept map: `concept-map/concept-map.yaml`
- Simulation spec: `simulation/simulation.yaml`
- Embed API: `embed/api.ts`
- Problem-solving algorithm: `problem-solving/algorithm.md`

## Imports

| Sim | Module | Symbols / role |
|---|---|---|
| bayes-updating | `core/sim-runtime` | Declared in `simulation/simulation.yaml` |
| bayes-updating | `core/prediction-gate` | Declared in `simulation/simulation.yaml` |
| bayes-updating | `core/probability-stats` | Declared in `simulation/simulation.yaml` |

## SimulationSpec (frozen)

Full validated YAML for the declared sim in this container at the time of last docs regeneration.

```yaml
id: bayes-updating
title: Bayes Updating Explorer
interaction_type: decision-matrix
kernel_deps:
  - core/sim-runtime
  - core/prediction-gate
  - core/probability-stats
predict:
  prompt: |
    With prior 10%, sensitivity 95%, and specificity 90%, what is P(H|+) approximately?
  commit_format:
    kind: multiple-choice
    options:
      - 9.5%
      - 34.5%
      - 51.3%
      - 90.0%
  rationale_required: true
manipulate:
  controls:
    - id: prevalence-percent
      label: Prior prevalence P(H)
      kind: slider
      kernel_binding: state.prevalencePercent
      bounds:
        min: 1
        max: 60
        step: 1
    - id: sensitivity-percent
      label: Sensitivity P(+|H)
      kind: slider
      kernel_binding: state.sensitivityPercent
      bounds:
        min: 50
        max: 99
        step: 1
    - id: specificity-percent
      label: Specificity P(-|¬H)
      kind: slider
      kernel_binding: state.specificityPercent
      bounds:
        min: 50
        max: 99
        step: 1
observe:
  renderers:
    - id: posterior-readout
      module: local
      symbol: bayes-posterior
      props_binding: |
        Show Bayes formula, substituted values, and posterior interpretation.
explain:
  prompt: |
    Explain why high sensitivity can still produce moderate posterior when prevalence is low.
  socratic: true
  expected_misconceptions_surfaced:
    - A positive test always means high probability
    - Sensitivity alone determines posterior
```

## Kernel extensions

No core-change proposal is declared in this container. Kernel use is limited to declared dependencies above.

## Accessibility

- Prediction gate contract: required and tested when the sim is registered.
- Route-level axe coverage: record the latest shell or container-specific result in the preserved review section below.
- Media fallback: `media/fallback.svg` present.

## Tests

- Container validation: `pnpm container:validate sutd/content/freshmore/containers/bayes-updating`
- Docs regeneration: `pnpm container:docs sutd/content/freshmore/containers/bayes-updating`
- Prediction-gate test: `simulation/simulation.test.ts`
- Package or shell tests: record exact commands in the preserved validation section below.

## How to run locally

```bash
pnpm container:docs sutd/content/freshmore/containers/bayes-updating
pnpm container:validate sutd/content/freshmore/containers/bayes-updating
pnpm graph:generate
```

## Anieyrudh Filter pass

Date: 2026-05-19
Filter version: aniegpt v1.0

### P0 issues

- Package-boundary violation: draft implementation lived inside the content folder and imported a wrong renderer package. Resolved by moving the sim to `@paideia/sutd-sims/bayes-updating` and using `module: local`.
- Prediction-gate test was placeholder-only. Resolved with Playwright assertions for blocked reveal, committed reveal, manipulation, and axe critical violations.

### P1 issues

- Concept card and technical record were scaffold placeholders. Resolved with reviewed concept text and this technical record.

### High-bandwidth questions surfaced

- The next Freshmore slice should decide whether bayes updating uses real course datasets or small deterministic teaching datasets.

## Iteration log

- Rejected the scaffold `FunctionPlot` path because this concept is a decision-policy surface, not a plotting demo.
- Kept the container focused on threshold, wrong-decision cost, review cost, and the explicit total-cost formula.
