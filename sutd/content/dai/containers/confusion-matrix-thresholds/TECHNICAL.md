# Confusion Matrix Thresholds · Technical Record

> **Auto-generated by `pnpm container:docs`. Updated by every product PR.**
> Source: `container.yaml`, sim source declarations, and preserved review sections.

## Architecture

- Container path: `sutd/content/dai/containers/confusion-matrix-thresholds`
- Status: `reviewed`
- Concept card: `concept-card.md`
- Concept map: `concept-map/concept-map.yaml`
- Simulation spec: `simulation/simulation.yaml`
- Embed API: `embed/api.ts`
- Problem-solving algorithm: `problem-solving/algorithm.md`

## Imports

| Sim | Module | Symbols / role |
|---|---|---|
| confusion-matrix-thresholds | `core/sim-runtime` | Declared in `simulation/simulation.yaml` |
| confusion-matrix-thresholds | `core/probability-stats` | Declared in `simulation/simulation.yaml` |
| confusion-matrix-thresholds | `core/charting` | Declared in `simulation/simulation.yaml` |
| confusion-matrix-thresholds | `core/annotation` | Declared in `simulation/simulation.yaml` |
| confusion-matrix-thresholds | `core/prediction-gate` | Declared in `simulation/simulation.yaml` |
| confusion-matrix-thresholds | `core/ui-sim` | Declared in `simulation/simulation.yaml` |

## SimulationSpec (frozen)

Full validated YAML for the declared sim in this container at the time of last docs regeneration.

```yaml
id: confusion-matrix-thresholds
title: Confusion Matrix Threshold Explorer
interaction_type: decision-matrix
kernel_deps:
  - core/sim-runtime
  - core/probability-stats
  - core/charting
  - core/annotation
  - core/prediction-gate
  - core/ui-sim
predict:
  prompt: |
    If the threshold rises from 65% to 80% while false negatives are costly, what is most likely to happen?
  commit_format:
    kind: multiple-choice
    options:
      - Precision and recall both increase
      - Recall falls, so missed-positive cost can rise
      - Accuracy is unchanged
    correct_index: 1
  rationale_required: true
manipulate:
  controls:
    - id: threshold-percent
      label: Decision threshold
      kind: slider
      kernel_binding: state.thresholdPercent
      bounds:
        min: 35
        max: 85
        step: 5
    - id: false-negative-cost
      label: False-negative cost
      kind: slider
      kernel_binding: state.falseNegativeCost
      bounds:
        min: 5
        max: 45
        step: 5
    - id: false-positive-cost
      label: False-positive cost
      kind: slider
      kernel_binding: state.falsePositiveCost
      bounds:
        min: 1
        max: 15
        step: 1
observe:
  renderers:
    - id: confusion-matrix-threshold-renderer
      module: "@paideia/sutd-sims/confusion-matrix-thresholds"
      symbol: ConfusionMatrixThresholds
      props_binding: |
        Show thresholded cases, confusion-matrix counts, precision, recall, accuracy, and cost formula with substituted values.
explain:
  prompt: |
    Which cell in your confusion matrix would you defend first to a stakeholder, and what evidence shows that accuracy alone is not enough?
  socratic: true
  expected_misconceptions_surfaced:
    - Accuracy is enough for every deployment
    - A single threshold is neutral for all groups
```

## Kernel extensions

Declared runtime kernel dependencies are listed above. Any additive or breaking core API change must be linked in the preserved review section below.

## Accessibility

- Prediction gate contract: required and tested when the sim is registered.
- Route-level axe coverage: record the latest shell or container-specific result in the preserved review section below.
- Media fallback: `media/fallback.svg` present.

## Tests

- Container validation: `pnpm container:validate sutd/content/dai/containers/confusion-matrix-thresholds`
- Docs regeneration: `pnpm container:docs sutd/content/dai/containers/confusion-matrix-thresholds`
- Prediction-gate test: `simulation/simulation.test.ts`
- Package or shell tests: record exact commands in the preserved validation section below.

## How to run locally

```bash
pnpm container:docs sutd/content/dai/containers/confusion-matrix-thresholds
pnpm container:validate sutd/content/dai/containers/confusion-matrix-thresholds
pnpm graph:generate
```

## Anieyrudh Filter pass

Date: 2026-05-21
Filter version: aniegpt v1.0

### P0 issues

- Prediction-gate enforcement was required for reveal. Resolved with package unit coverage of the gate and route-level Playwright coverage proving the matrix is hidden before prediction commit.
- Declared kernel dependencies had to be consumed rather than named only. Resolved by using sim-runtime, probability-stats, charting, annotation, prediction-gate, and ui-sim in the package surface.
- Sim-architect review found local threshold/count metric math in the React sim surface. Resolved by moving the count update, metric, base-rate, curve, per-case outcome, and weighted-cost logic into `core/probability-stats`.
- Sim-architect review found undeclared runtime imports from `core/content-schema` and `core/shared`. Resolved by deriving the spec/package-id types from `SimRuntime` and removing direct runtime imports from those packages.

### P1 issues

- Accuracy-only framing could reinforce the target misconception. Resolved by placing FP/FN costs, precision, recall, and stakeholder interpretation next to the count table.
- Formula work needed substitution, units, and interpretation. Resolved with a formula panel showing definitions, color-coded legend, substituted counts, cost units, and plain-language interpretation.
- Pedagogy review found the formula legend missing TN, N, C_FP, C_FN, and the accuracy substitution. Resolved by adding the missing legend terms and explicit accuracy substitution with case units.
- Pedagogy review found Explain wording too declarative. Resolved by changing it to a stakeholder-facing Socratic question.
- Pedagogy review asked for a misconception artifact; the container validator rejects unexpected top-level files, so the evidence note lives under `concept-map/misconceptions.md` and is linked from `concept-map/mindmap.md`.
- Sim-architect review found the dominant-risk interpretation compared raw error counts instead of weighted costs. Resolved by comparing FP and FN cost totals from `core/probability-stats`.
- Sim-architect review found `core/prediction-gate` was only used in tests. Resolved by reading reveal status with the prediction-gate API inside the observed runtime surface.

### Accessibility result

- `pnpm test` route coverage passed for `simulation/simulation.test.ts`; the revealed-state axe scan reported 0 critical violations for this container.

### High-bandwidth questions surfaced

- Future fairness-auditing slices should add separate group distributions rather than overloading this single-threshold introduction.

## Iteration log

- Kept the dataset deterministic so every threshold movement can be traced by eye.
- Used a compact matrix, score strip, and threshold curve instead of a form-only calculator.
