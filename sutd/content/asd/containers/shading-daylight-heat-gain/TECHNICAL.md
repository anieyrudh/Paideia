# Shading, Daylight, and Heat Gain · Technical Record

> **Auto-generated by `pnpm container:docs`. Updated by every product PR.**
> Source: `container.yaml`, sim source declarations, and preserved review sections.

## Architecture

- Container path: `sutd/content/asd/containers/shading-daylight-heat-gain`
- Status: `reviewed`
- Concept card: `concept-card.md`
- Concept map: `concept-map/concept-map.yaml`
- Simulation spec: `simulation/simulation.yaml`
- Embed API: `embed/api.ts`
- Problem-solving algorithm: `problem-solving/algorithm.md`

## Imports

| Sim | Module | Symbols / role |
|---|---|---|
| shading-daylight-heat-gain | `core/sim-runtime` | Declared in `simulation/simulation.yaml` |
| shading-daylight-heat-gain | `core/linear-algebra` | Declared in `simulation/simulation.yaml` |
| shading-daylight-heat-gain | `core/charting` | Declared in `simulation/simulation.yaml` |
| shading-daylight-heat-gain | `core/prediction-gate` | Declared in `simulation/simulation.yaml` |
| shading-daylight-heat-gain | `core/ui-sim` | Declared in `simulation/simulation.yaml` |

## SimulationSpec (frozen)

Full validated YAML for the declared sim in this container at the time of last docs regeneration.

```yaml
id: shading-daylight-heat-gain
title: Shading, Daylight, and Heat Gain
interaction_type: systems-flow-diagram
kernel_deps:
  - core/sim-runtime
  - core/linear-algebra
  - core/charting
  - core/prediction-gate
  - core/ui-sim
manipulate:
  controls:
    - id: overhang-depth
      label: Overhang depth
      kind: slider
      kernel_binding: state.overhangDepthM
      bounds:
        min: 0.2
        max: 1.8
        step: 0.1
    - id: glazing-ratio
      label: Glazing ratio
      kind: slider
      kernel_binding: state.glazingRatio
      bounds:
        min: 0.3
        max: 0.9
        step: 0.05
    - id: solar-altitude
      label: Solar altitude
      kind: slider
      kernel_binding: state.solarAltitudeDeg
      bounds:
        min: 20
        max: 75
        step: 5
    - id: facade-orientation
      label: Facade orientation
      kind: selector
      kernel_binding: state.facadeOrientation
predict:
  prompt: |
    A studio facade gets deeper horizontal shading while the glazing area stays fixed. What is the most likely tradeoff?
  commit_format:
    kind: multiple-choice
    options:
      - Daylight and heat gain both increase.
      - Heat gain falls, but useful daylight can also fall if the shade is too deep.
      - Only daylight changes; heat gain is controlled only by wall insulation.
      - A deeper shade always improves every performance metric.
    correct_index: 1
  rationale_required: true
observe:
  renderers:
    - id: shading-daylight-readout
      module: "@paideia/sutd-sims/shading-daylight-heat-gain"
      symbol: ShadingDaylightHeatGain
      props_binding: |
        Show facade shading geometry, daylight proxy, heat-gain proxy, formula trail, and tradeoff curve.
explain:
  prompt: |
    Which input changed the shaded fraction most, and how did that same fraction affect daylight and heat gain differently?
  socratic: true
  expected_misconceptions_surfaced:
    - More glass always improves daylight quality
    - Shading only reduces light and never heat
```

## Kernel extensions

Declared runtime kernel dependencies are listed above. Any additive or breaking core API change must be linked in the preserved review section below.

## Accessibility

- Prediction gate contract: required and tested when the sim is registered.
- Route-level axe coverage: record the latest shell or container-specific result in the preserved review section below.
- Media fallback: `media/fallback.svg` present.

## Tests

- Container validation: `pnpm container:validate sutd/content/asd/containers/shading-daylight-heat-gain`
- Docs regeneration: `pnpm container:docs sutd/content/asd/containers/shading-daylight-heat-gain`
- Prediction-gate test: `simulation/simulation.test.ts`
- Package or shell tests: record exact commands in the preserved validation section below.

## How to run locally

```bash
pnpm container:docs sutd/content/asd/containers/shading-daylight-heat-gain
pnpm container:validate sutd/content/asd/containers/shading-daylight-heat-gain
pnpm graph:generate
```

## Kernel Boundaries

- Queue-declared kernels remain the simulation contract: `core/sim-runtime`, `core/linear-algebra`, `core/charting`, `core/prediction-gate`, and `core/ui-sim`.
- `core/linear-algebra` owns the reusable vector normalization and incidence dot-product operation used by the facade model.
- The daylight and heat-gain relationships are scoped as named teaching proxies for this container because no facade-performance core kernel exists in this branch. A future multi-container ASD facade wave should decide whether to promote these proxies into a shared kernel.
- `core/shared` branded units are used at the public model boundary for metre, degree, watt, ratio, and area values. `core/content-schema` is used only for the local `SimulationSpec` type pattern used across existing sim packages, not as an additional queue kernel.

## Validation Notes

- `pnpm -F @paideia/sim-harness test` initially failed in the sandbox because Vite could not bind `127.0.0.1:4173` (`listen EPERM`). It passed outside the sandbox after clearing stale Vite cache, including the four Shading, Daylight, and Heat Gain route tests and the revealed-state axe scan.
- `pnpm exec vitest run sutd/content/asd/containers/shading-daylight-heat-gain/embed/embed.test.ts` passed after adding the jsdom environment directive.
- Queue entry remains `in-build` for this PR by explicit maintainer instruction; the container metadata remains `reviewed` so the non-empty Filter section is enforced before PR review.

## Anieyrudh Filter pass

Date: 2026-05-21
Filter version: aniegpt v1.0

### P0 issues

- None open. Container is scoped to the assigned SUTD ASD queue item and does not modify adjacent containers.
- Kernel ownership is respected: staged runtime uses `core/sim-runtime` and `core/prediction-gate`, charting uses `core/charting`, controls use `core/ui-sim`, and vector/incidence calculations use `core/linear-algebra`.

### P1 issues

- Daylight and heat gain are simplified early-design proxies. The learner UI, concept card, and problem-solving artifact label them as teaching estimates rather than compliance outputs.
- Student-facing UI was reviewed to avoid package names, file paths, kernel names, YAML labels, and implementation details.

### High-bandwidth questions surfaced

- Future ASD containers could promote climate-based daylight autonomy or solar-position logic into a shared kernel once multiple facade containers need it.

## Iteration log

- Built one focused facade-tradeoff explorable with prediction gate, formula trail, legend, substitutions, units, interpretation, embed contract, media fallback, and transfer problem.
