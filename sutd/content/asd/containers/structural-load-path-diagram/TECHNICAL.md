# Structural Load Path Diagram · Technical Record

> **Auto-generated by `pnpm container:docs`. Updated by every product PR.**
> Source: `container.yaml`, sim source declarations, and preserved review sections.

## Architecture

- Container path: `sutd/content/asd/containers/structural-load-path-diagram`
- Status: `reviewed`
- Concept card: `concept-card.md`
- Concept map: `concept-map/concept-map.yaml`
- Simulation spec: `simulation/simulation.yaml`
- Embed API: `embed/api.ts`
- Problem-solving algorithm: `problem-solving/algorithm.md`

## Imports

| Sim | Module | Symbols / role |
|---|---|---|
| structural-load-path-diagram | `core/sim-runtime` | Declared in `simulation/simulation.yaml` |
| structural-load-path-diagram | `core/mechanics` | Declared in `simulation/simulation.yaml` |
| structural-load-path-diagram | `core/linear-algebra` | Declared in `simulation/simulation.yaml` |
| structural-load-path-diagram | `core/graph-layout` | Declared in `simulation/simulation.yaml` |
| structural-load-path-diagram | `core/prediction-gate` | Declared in `simulation/simulation.yaml` |
| structural-load-path-diagram | `core/shared` | Declared in `simulation/simulation.yaml` |
| structural-load-path-diagram | `core/ui-sim` | Declared in `simulation/simulation.yaml` |

## SimulationSpec (frozen)

Full validated YAML for the declared sim in this container at the time of last docs regeneration.

```yaml
id: structural-load-path-diagram
title: Structural Load Path Diagram
interaction_type: diagram-builder
kernel_deps:
  - core/sim-runtime
  - core/mechanics
  - core/linear-algebra
  - core/graph-layout
  - core/prediction-gate
  - core/shared
  - core/ui-sim
predict:
  prompt: |
    A braced bay takes a strong sideways load. Before calculating, which part is most likely to govern the load path?
  commit_format:
    kind: multiple-choice
    options:
      - The diagonal brace force, because it resolves the sideways load.
      - The roof load, because all members carry the same force.
      - The supports, because loads disappear when they reach the ground.
    correct_index: 0
  rationale_required: true
manipulate:
  controls:
    - id: load-direction
      label: Load direction
      kind: selector
      kernel_binding: state.loadDirection
    - id: brace-size
      label: Brace size
      kind: selector
      kernel_binding: state.braceSize
    - id: lateral-load
      label: Sideways load
      kind: slider
      kernel_binding: state.lateralLoadKn
      bounds:
        min: 10
        max: 54
        step: 2
    - id: roof-load
      label: Roof gravity load
      kind: slider
      kernel_binding: state.roofLoadKn
      bounds:
        min: 48
        max: 132
        step: 6
    - id: bay-width
      label: Bay width
      kind: slider
      kernel_binding: state.bayWidthM
      bounds:
        min: 4
        max: 9
        step: 0.5
    - id: storey-height
      label: Storey height
      kind: slider
      kernel_binding: state.storeyHeightM
      bounds:
        min: 3
        max: 6
        step: 0.5
observe:
  renderers:
    - id: structural-load-path-renderer
      module: local
      symbol: StructuralLoadPathDiagram
      props_binding: |
        Resolve lateral load through a diagonal brace, calculate support reactions, and label compression or tension paths.
explain:
  prompt: |
    Explain why the diagonal force is larger than the applied sideways load, then transfer the same reasoning to a canopy frame.
  socratic: true
  expected_misconceptions_surfaced:
    - Loads disappear at supports
    - All members carry equal force
```

## Kernel extensions

Declared runtime kernel dependencies are listed above. Any additive or breaking core API change must be linked in the preserved review section below.

## Accessibility

- Prediction checkpoint contract: required and tested when the sim is registered.
- Route-level axe coverage: record the latest shell or container-specific result in the preserved review section below.
- Media fallback: `media/fallback.svg` present.

## Tests

- Container validation: `pnpm container:validate sutd/content/asd/containers/structural-load-path-diagram`
- Docs regeneration: `pnpm container:docs sutd/content/asd/containers/structural-load-path-diagram`
- Prediction-checkpoint test: `simulation/simulation.test.ts`
- Package or shell tests: record exact commands in the preserved validation section below.

## How to run locally

```bash
pnpm container:docs sutd/content/asd/containers/structural-load-path-diagram
pnpm container:validate sutd/content/asd/containers/structural-load-path-diagram
pnpm graph:generate
```

## Kernel Boundaries

- Force equilibrium uses the shared mechanics kernel.
- Brace vector direction uses the shared 2D linear algebra kernel.
- Load path node placement uses the shared graph layout kernel.
- Prediction gating and PMOE-T state use the shared simulation runtime.

## Anieyrudh Filter pass

Date: 2026-05-21
Reviewers (subagents): container-auditor, sim-architect, pedagogy-reviewer

### P0 resolved

- Prediction checkpoint blocks reveal before observation - covered by simulation test.
- Calculations show formula, substitution, units, and interpretation - implemented in the revealed formula panel.
- Required queue kernels are declared and consumed - mechanics, linear algebra, graph layout, prediction checkpoint, shared, sim runtime, and UI sim are declared.

### P1 addressed or deferred

- Browser visual smoke evidence is required before PR merge - addressed with Playwright prediction-checkpoint, manipulation, and critical axe tests.
- Mobile horizontal overflow in the revealed formula/metric surface - addressed by responsive grid constraints, bounded SVGs, and wrapping formula blocks; verified at 390px viewport with no document overflow.

### P2 noted

- Future slices can add multi-bay frames and indeterminate load distribution once a structural-analysis kernel exists.

## Iteration log

- Regenerated README.md and TECHNICAL.md with `pnpm container:docs`.
- Browser smoke checked desktop and mobile reveal states; mobile overflow was found, fixed, and rechecked.
