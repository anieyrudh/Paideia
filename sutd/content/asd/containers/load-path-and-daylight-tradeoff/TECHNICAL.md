# Load Path and Daylight Tradeoff · Technical Record

> **Auto-generated by `pnpm container:docs`. Updated by every product PR.**
> Source: `container.yaml`, sim source declarations, and preserved review sections.

## Architecture

- Container path: `sutd/content/asd/containers/load-path-and-daylight-tradeoff`
- Status: `reviewed`
- Concept card: `concept-card.md`
- Concept map: `concept-map/concept-map.yaml`
- Simulation spec: `simulation/simulation.yaml`
- Embed API: `embed/api.ts`
- Problem-solving algorithm: `problem-solving/algorithm.md`

## Imports

| Sim | Module | Symbols / role |
|---|---|---|
| load-path-and-daylight-tradeoff | `core/sim-runtime` | Declared in `simulation/simulation.yaml` |
| load-path-and-daylight-tradeoff | `core/linear-algebra` | Declared in `simulation/simulation.yaml` |
| load-path-and-daylight-tradeoff | `core/mechanics` | Declared in `simulation/simulation.yaml` |
| load-path-and-daylight-tradeoff | `core/charting` | Declared in `simulation/simulation.yaml` |
| load-path-and-daylight-tradeoff | `core/prediction-gate` | Declared in `simulation/simulation.yaml` |
| load-path-and-daylight-tradeoff | `core/ui-sim` | Declared in `simulation/simulation.yaml` |

## SimulationSpec (frozen)

Full validated YAML for the declared sim in this container at the time of last docs regeneration.

```yaml
id: load-path-and-daylight-tradeoff
title: Load Path and Daylight Tradeoff Explorer
interaction_type: systems-flow-diagram
kernel_deps:
  - core/sim-runtime
  - core/linear-algebra
  - core/mechanics
  - core/charting
  - core/prediction-gate
  - core/ui-sim
predict:
  prompt: |
    For a single bay with a useful window opening, which option is most likely to keep a clear load path while preserving usable daylight?
  commit_format:
    kind: multiple-choice
    options:
      - Large opening with no brace
      - Medium opening with diagonal brace
      - Small opening with X-brace
  rationale_required: true
manipulate:
  controls:
    - id: aperture-ratio
      label: Opening ratio
      kind: slider
      kernel_binding: state.apertureRatio
      bounds:
        min: 0.25
        max: 0.8
        step: 0.05
    - id: lateral-load
      label: Lateral load
      kind: slider
      kernel_binding: state.lateralLoadKn
      bounds:
        min: 12
        max: 42
        step: 2
    - id: bay-height
      label: Bay height
      kind: slider
      kernel_binding: state.bayHeightM
      bounds:
        min: 3
        max: 6
        step: 0.5
observe:
  renderers:
    - id: load-path-daylight-renderer
      module: local
      symbol: LoadPathAndDaylightTradeoff
      props_binding: |
        Show lateral demand, resisting path, daylight proxy, and the formula-backed tradeoff for one bay.
explain:
  prompt: |
    Explain why a larger opening can improve daylight but weaken the lateral path, and identify the design move that restores equilibrium.
  socratic: true
  expected_misconceptions_surfaced:
    - More glass always improves performance
    - Loads travel only vertically
```

## Kernel extensions

No core-change proposal is declared in this container. Kernel use is limited to declared dependencies above.

## Accessibility

- Prediction checkpoint contract: required and tested when the sim is registered.
- Route-level axe coverage: record the latest shell or container-specific result in the preserved review section below.
- Media fallback: `media/fallback.svg` present.

## Tests

- Container validation: `pnpm container:validate sutd/content/asd/containers/load-path-and-daylight-tradeoff`
- Docs regeneration: `pnpm container:docs sutd/content/asd/containers/load-path-and-daylight-tradeoff`
- Prediction-checkpoint test: `simulation/simulation.test.ts`
- Package or shell tests: record exact commands in the preserved validation section below.

## How to run locally

```bash
pnpm container:docs sutd/content/asd/containers/load-path-and-daylight-tradeoff
pnpm container:validate sutd/content/asd/containers/load-path-and-daylight-tradeoff
pnpm graph:generate
```

## Anieyrudh Filter pass

Date: 2026-05-19
Filter version: aniegpt v1.0

### P0 issues

- Prior draft PR mixed ASD title with A-Level vector-resultant content and noncanonical package paths. Resolved by rebuilding this slice from a fresh ASD container under `sutd/content/asd/containers/load-path-and-daylight-tradeoff`, routing through `@paideia/sutd-sims/load-path-and-daylight-tradeoff`, and using `module: local`.
- Risk of local one-off structural math. Resolved by routing force residual through `core/mechanics`, vector magnitude through `core/linear-algebra`, charting through `core/charting`, controls through `core/ui-sim`, and reveal through `core/prediction-gate`.

### P1 issues

- Daylighting and structural capacity are simplified proxies. Addressed by naming them as teaching proxies in the learner UI, concept card, and problem-solving algorithm rather than implying design-code precision.
- Student-facing language could expose implementation details. Addressed by keeping package names and file paths out of the sim UI.

### High-bandwidth questions surfaced

- A future ASD slice should decide whether to introduce real daylight metrics such as daylight autonomy after the notebook/media pipeline is ready.
- Advisor review should decide whether the single-bay proxy should become a shared ASD kernel once more structural/environmental containers exist.

## Iteration log

- Rejected the previous scaffold direction because it reused A-Level vector wording and did not model the ASD load/daylight tradeoff.
- Kept the first version to a deterministic single-bay proxy so tests can assert formulas, residual force, and accessibility reliably.
