# Linear Programming Feasible Region · Technical Record

> **Auto-generated by `pnpm container:docs`. Updated by every product PR.**
> Source: `container.yaml`, sim source declarations, and preserved review sections.

## Architecture

- Container path: `sutd/content/esd/containers/linear-programming-feasible-region`
- Status: `draft`
- Concept card: `concept-card.md`
- Concept map: `concept-map/concept-map.yaml`
- Simulation spec: `simulation/simulation.yaml`
- Embed API: `embed/api.ts`
- Problem-solving algorithm: `problem-solving/algorithm.md`

## Imports

| Sim | Module | Symbols / role |
|---|---|---|
| linear-programming-feasible-region | `core/sim-runtime` | Declared in `simulation/simulation.yaml` |
| linear-programming-feasible-region | `core/prediction-gate` | Declared in `simulation/simulation.yaml` |

## SimulationSpec (frozen)

Full validated YAML for the declared sim in this container at the time of last docs regeneration.

```yaml
id: linear-programming-feasible-region
title: Linear Programming Feasible Region Explorer
interaction_type: decision-matrix
kernel_deps:
  - core/sim-runtime
  - core/prediction-gate
manipulate:
  controls:
    - id: x-control
      label: x units
      kind: slider
      kernel_binding: state.x
      bounds:
        min: 0
        max: 10
        step: 1
    - id: y-control
      label: y units
      kind: slider
      kernel_binding: state.y
      bounds:
        min: 0
        max: 10
        step: 1
predict:
  prompt: |
    Which point is most likely to maximize Z = 3x + 2y while satisfying all constraints?
  commit_format:
    kind: multiple-choice
    options:
      - (4, 4)
      - (6, 2)
      - (2, 5)
  rationale_required: true
observe:
  renderers:
    - id: constraint-check
      module: local
      symbol: text-evaluation
      props_binding: Display substituted constraints and objective value for current state.
explain:
  prompt: |
    Which constraints are binding at your chosen point, and how does that guide a corner-point search?
  socratic: true
  expected_misconceptions_surfaced:
    - Highest x or y value always gives the best objective value
    - Any point satisfying one constraint is feasible
```

## Kernel extensions

No core-change proposal is declared in this container. Kernel use is limited to declared dependencies above.

## Accessibility

- Prediction checkpoint contract: required and tested when the sim is registered.
- Route-level axe coverage: record the latest shell or container-specific result in the preserved review section below.
- Media fallback: `media/fallback.svg` present.

## Tests

- Container validation: `pnpm container:validate sutd/content/esd/containers/linear-programming-feasible-region`
- Docs regeneration: `pnpm container:docs sutd/content/esd/containers/linear-programming-feasible-region`
- Prediction-checkpoint test: `simulation/simulation.test.ts`
- Package or shell tests: record exact commands in the preserved validation section below.

## How to run locally

```bash
pnpm container:docs sutd/content/esd/containers/linear-programming-feasible-region
pnpm container:validate sutd/content/esd/containers/linear-programming-feasible-region
pnpm graph:generate
```

## Anieyrudh Filter pass

Date: 2026-05-19
Reviewers (subagents): container-auditor, sim-architect, pedagogy-reviewer

### P0 resolved
- transfer artifact + manifest consistency fixed.

### P1 addressed or deferred
- sim-architect and pedagogy suggestions deferred to follow-up for dedicated kernelization/refinement.

### P2 noted
- none

## Iteration log

- Regenerated README.md and TECHNICAL.md with `pnpm container:docs`.
