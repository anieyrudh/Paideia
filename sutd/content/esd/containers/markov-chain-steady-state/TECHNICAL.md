# Markov Chain Steady State · Technical Record

> **Auto-generated by `pnpm container:docs`. Updated by every product PR.**
> Source: `container.yaml`, sim source declarations, and preserved review sections.

## Architecture

- Container path: `sutd/content/esd/containers/markov-chain-steady-state`
- Status: `reviewed`
- Concept card: `concept-card.md`
- Concept map: `concept-map/concept-map.yaml`
- Simulation spec: `simulation/simulation.yaml`
- Embed API: `embed/api.ts`
- Problem-solving algorithm: `problem-solving/algorithm.md`

## Imports

| Sim | Module | Symbols / role |
|---|---|---|
| markov-chain-steady-state | `core/sim-runtime` | Declared in `simulation/simulation.yaml` |
| markov-chain-steady-state | `core/probability-stats` | Declared in `simulation/simulation.yaml` |
| markov-chain-steady-state | `core/linear-algebra` | Declared in `simulation/simulation.yaml` |
| markov-chain-steady-state | `core/charting` | Declared in `simulation/simulation.yaml` |
| markov-chain-steady-state | `core/prediction-gate` | Declared in `simulation/simulation.yaml` |
| markov-chain-steady-state | `core/shared` | Declared in `simulation/simulation.yaml` |
| markov-chain-steady-state | `core/ui-sim` | Declared in `simulation/simulation.yaml` |

## SimulationSpec (frozen)

Full validated YAML for the declared sim in this container at the time of last docs regeneration.

```yaml
id: markov-chain-steady-state
title: Markov Chain Steady-State Lab
interaction_type: systems-flow-diagram
kernel_deps:
  - core/sim-runtime
  - core/probability-stats
  - core/linear-algebra
  - core/charting
  - core/prediction-gate
  - core/shared
  - core/ui-sim
predict:
  prompt: |
    A system is more likely to stay smooth than to recover once it becomes congested. Before reveal, where should the long-run mix move?
  commit_format:
    kind: multiple-choice
    options:
      - Toward more smooth weeks
      - Toward more congested weeks
      - Exactly to the initial week mix
      - To whichever state has the larger one-step arrow
    correct_index: 1
  rationale_required: true
manipulate:
  controls:
    - id: smooth-stays-smooth
      label: Smooth week stays smooth
      kind: slider
      kernel_binding: state.smoothStaysSmooth
      bounds:
        min: 0.55
        max: 0.95
        step: 0.01
    - id: congested-recovers
      label: Congested week recovers
      kind: slider
      kernel_binding: state.congestedRecovers
      bounds:
        min: 0.05
        max: 0.65
        step: 0.01
    - id: initial-smooth
      label: Initial smooth probability
      kind: slider
      kernel_binding: state.initialSmooth
      bounds:
        min: 0.05
        max: 0.95
        step: 0.01
    - id: weeks
      label: Forecast horizon
      kind: slider
      kernel_binding: state.weeks
      bounds:
        min: 2
        max: 20
        step: 1
observe:
  renderers:
    - id: steady-state-readout
      module: "@paideia/sutd-sims/markov-chain-steady-state"
      symbol: MarkovChainSteadyState
      props_binding: |
        Render transition matrix, repeated-state update, steady-state formula with substitution, probability legend, convergence chart, and long-run interpretation.
explain:
  prompt: |
    Explain why a steady-state distribution can stay fixed while individual weeks still transition between states.
  socratic: true
  expected_misconceptions_surfaced:
    - Steady state means no individual transitions happen
    - The largest immediate transition always dominates long-run state
```

## Kernel extensions

Declared runtime kernel dependencies are listed above. Any additive or breaking core API change must be linked in the preserved review section below.

## Accessibility

- Prediction checkpoint contract: required and tested when the sim is registered.
- Route-level axe coverage: record the latest shell or container-specific result in the preserved review section below.
- Media fallback: `media/fallback.svg` present.

## Tests

- Container validation: `pnpm container:validate sutd/content/esd/containers/markov-chain-steady-state`
- Docs regeneration: `pnpm container:docs sutd/content/esd/containers/markov-chain-steady-state`
- Prediction-checkpoint test: `simulation/simulation.test.ts`
- Package or shell tests: record exact commands in the preserved validation section below.

## How to run locally

```bash
pnpm container:docs sutd/content/esd/containers/markov-chain-steady-state
pnpm container:validate sutd/content/esd/containers/markov-chain-steady-state
pnpm graph:generate
```

## Anieyrudh Filter pass

Date: 2026-05-21
Filter version: aniegpt v1.0
Reviewers (local): container-auditor, sim-architect, pedagogy-reviewer

### P0 resolved

- Prediction checkpoint must block observation-shaped steady-state evidence before commitment - resolved with package-level prediction, sim-level Playwright coverage, and package-level prediction-checkpoint unit coverage.
- Kernel boundary must not inline generic transition-matrix mechanics when a core kernel exists - resolved by using `core/linear-algebra` for matrix/vector updates and the eigenvector check, `core/probability-stats` for probability normalisation, and `core/charting` for the convergence chart.

### P1 addressed or deferred

- Learner-facing calculations need formula, substitution, units, interpretation, and legend - addressed in the observe-stage formula panel and tested in `simulation/simulation.test.ts`.
- Student UI must avoid package names, file paths, kernel names, YAML fields, and queue IDs - addressed by keeping implementation references in docs/tests only, not visible simulation copy.

### High-bandwidth questions surfaced

- The queue names a descriptive simulation type that is more specific than the current schema enum. This container maps it to `systems-flow-diagram` in `SimulationSpec` while preserving transition-matrix lab behavior in copy and renderer declarations.

## Iteration log

- Scaffolded the canonical v2 container with `pnpm container:new`, then replaced placeholder content with the SUTD ESD Markov chain slice.
- Built a two-state operations simulation with PMOE-T flow, formula-backed readouts, transition matrix table, repeated-update trajectory, convergence chart, and transfer problem.
- Corrected the initial schema mismatch by changing the sim `interaction_type` from the queue's descriptive `transition-matrix-steady-state-lab` to the allowed `systems-flow-diagram`.
