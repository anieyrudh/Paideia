# Paideia SUTD Shell

SUTD curriculum wrapper shell for mapping shared and SUTD-specific concept
containers into Freshmore and pillar pathways.

The generated graph is produced by:

```bash
pnpm graph:generate
```

The shell currently supports an empty generated container graph while SUTD
wrapper containers are being planned.
