# Eigenvector Transformations · Technical Record

> **Auto-generated by `pnpm container:docs`. Updated by every product PR.**
> Source: `container.yaml`, sim source declarations, and preserved review sections.

## Architecture

- Container path: `shared/content/math/containers/eigenvector-transformations`
- Status: `reviewed`
- Concept card: `concept-card.md`
- Concept map: `concept-map/concept-map.yaml`
- Simulation spec: `simulation/simulation.yaml`
- Embed API: `embed/api.ts`
- Problem-solving algorithm: `problem-solving/algorithm.md`

## Imports

| Sim | Module | Symbols / role |
|---|---|---|
| eigenvector-transformations | `core/content-schema` | Declared in `simulation/simulation.yaml` |
| eigenvector-transformations | `core/sim-runtime` | Declared in `simulation/simulation.yaml` |
| eigenvector-transformations | `core/linear-algebra` | Declared in `simulation/simulation.yaml` |
| eigenvector-transformations | `core/plotting` | Declared in `simulation/simulation.yaml` |
| eigenvector-transformations | `core/prediction-gate` | Declared in `simulation/simulation.yaml` |
| eigenvector-transformations | `core/ui-sim` | Declared in `simulation/simulation.yaml` |
| eigenvector-transformations | `core/shared` | Declared in `simulation/simulation.yaml` |

## SimulationSpec (frozen)

Full validated YAML for the declared sim in this container at the time of last docs regeneration.

```yaml
id: eigenvector-transformations
title: Eigenvector Direction Lab
interaction_type: diagram-builder
kernel_deps:
  - core/content-schema
  - core/sim-runtime
  - core/linear-algebra
  - core/plotting
  - core/prediction-gate
  - core/ui-sim
  - core/shared
predict:
  prompt: |
    Matrix A = [[3, 1], [0, 2]] acts on vector v = (1, 0). Before reveal, which statement best describes Av?
  commit_format:
    kind: multiple-choice
    options:
      - Av = (1, 0), so the vector is unchanged
      - Av = (3, 0), so the vector stays on its line and triples
      - Av = (0, 3), so the vector rotates onto the y-axis
      - Av = (3, 2), so every component is scaled differently
    correct_index: 1
  rationale_required: true
manipulate:
  controls:
    - id: vector-x
      label: Vector x component
      kind: slider
      kernel_binding: state.x
      bounds:
        min: -3
        max: 3
        step: 1
    - id: vector-y
      label: Vector y component
      kind: slider
      kernel_binding: state.y
      bounds:
        min: -3
        max: 3
        step: 1
    - id: a11
      label: Horizontal stretch
      kind: slider
      kernel_binding: state.a11
      bounds:
        min: -4
        max: 4
        step: 0.5
    - id: a12
      label: Upper-right shear
      kind: slider
      kernel_binding: state.a12
      bounds:
        min: -4
        max: 4
        step: 0.5
    - id: a21
      label: Lower-left shear
      kind: slider
      kernel_binding: state.a21
      bounds:
        min: -4
        max: 4
        step: 0.5
    - id: a22
      label: Vertical stretch
      kind: slider
      kernel_binding: state.a22
      bounds:
        min: -4
        max: 4
        step: 0.5
observe:
  renderers:
    - id: eigenvector-readout
      module: "@paideia/shared-sims/eigenvector-transformations"
      symbol: EigenvectorTransformations
      props_binding: |
        Show Av, lambda v comparison, invariant-direction verdict, and eigenpair reference directions.
explain:
  prompt: |
    What evidence shows that the direction survived the transformation rather than merely producing an output?
  socratic: true
  expected_misconceptions_surfaced:
    - Every vector is an eigenvector
    - Eigenvalues are rotation angles
```

## Kernel extensions

Declared runtime kernel dependencies are listed above. Any additive or breaking core API change must be linked in the preserved review section below.

## Accessibility

- Prediction gate contract: required and tested when the sim is registered.
- Route-level axe coverage: record the latest shell or container-specific result in the preserved review section below.
- Media fallback: `media/fallback.svg` present.

## Tests

- Container validation: `pnpm container:validate shared/content/math/containers/eigenvector-transformations`
- Docs regeneration: `pnpm container:docs shared/content/math/containers/eigenvector-transformations`
- Prediction-gate test: `simulation/simulation.test.ts`
- Package or shell tests: record exact commands in the preserved validation section below.

## How to run locally

```bash
pnpm container:docs shared/content/math/containers/eigenvector-transformations
pnpm container:validate shared/content/math/containers/eigenvector-transformations
pnpm graph:generate
```

## Anieyrudh Filter pass

Date: 2026-05-20
Reviewers (local): structural validator, sim package tests, product self-review

### P0 resolved

- Prediction gate blocks observation until a rationale-bearing prediction is committed — resolution: asserted in package and Playwright tests.
- Eigenvector verdict uses `core/linear-algebra` matrix/vector operations and tolerance constants — resolution: added and consumed `checkEigenvector2()` for `A*v`, projection scale, residual, and verdict.
- Shared-core path is `shared/content/math/containers/eigenvector-transformations` — resolution: no A-Level or SUTD wrapper placement.
- Explain prompt must be Socratic and non-telegraphing — resolution: changed the prompt to ask what evidence proves the direction survived.
- Misconception evidence artifact must exist — resolution: added `concept-map/misconceptions.md` with evidence and prediction-surface fields.
- Transfer must change surface form — resolution: replaced the second 2x2 calculation with a thermal-mode interpretation problem.

### P1 addressed or deferred

- Formula panel requires formula, legend, substitution, units, and interpretation — addressed in the Observe stage UI without exposing raw LaTeX color commands.
- Product docs must reflect the shared-core route and renderer — addressed by `pnpm container:docs`.
- Embed host inputs must be runtime-validated — addressed with Zod parsing and invalid-input tests.
- Queue status must match reviewed container status — addressed by setting `shared.linear-algebra.eigenvector-transformations` to `reviewed`.
- Declared kernel dependencies must match imports, including type-only schema imports — addressed by declaring `core/content-schema` and `core/shared`.
- Axe coverage exists for the revealed sim state — addressed by `simulation/simulation.test.ts`.
- Additive core API change must have traceability — addressed by GitHub issue #109 for `core/linear-algebra` `checkEigenvector2()`.

### P2 noted

- Future curriculum wrappers can point to this shared core without duplicating the simulation math.

## Iteration log

- Built the shared-core eigenvector transformation package from the reviewed SUTD pattern, moving reusable sim logic into `@paideia/shared-sims`.
