# ODE Phase Portrait Lab · Technical Record

> **Auto-generated by `pnpm container:docs`. Updated by every product PR.**
> Source: `container.yaml`, sim source declarations, and preserved review sections.

## Architecture

- Container path: `shared/content/math/containers/ode-phase-portrait`
- Status: `reviewed`
- Concept card: `concept-card.md`
- Concept map: `concept-map/concept-map.yaml`
- Simulation spec: `simulation/simulation.yaml`
- Embed API: `embed/api.ts`
- Problem-solving algorithm: `problem-solving/algorithm.md`

## Imports

| Sim | Module | Symbols / role |
|---|---|---|
| ode-phase-portrait | `core/sim-runtime` | Declared in `simulation/simulation.yaml` |
| ode-phase-portrait | `core/content-schema` | Declared in `simulation/simulation.yaml` |
| ode-phase-portrait | `core/dynamical-systems` | Declared in `simulation/simulation.yaml` |
| ode-phase-portrait | `core/plotting` | Declared in `simulation/simulation.yaml` |
| ode-phase-portrait | `core/prediction-gate` | Declared in `simulation/simulation.yaml` |
| ode-phase-portrait | `core/shared` | Declared in `simulation/simulation.yaml` |
| ode-phase-portrait | `core/ui-sim` | Declared in `simulation/simulation.yaml` |

## SimulationSpec (frozen)

Full validated YAML for the declared sim in this container at the time of last docs regeneration.

```yaml
id: ode-phase-portrait
title: ODE Phase Portrait Explorer
interaction_type: function-plot-with-draggable
kernel_deps:
  - core/sim-runtime
  - core/content-schema
  - core/dynamical-systems
  - core/plotting
  - core/prediction-gate
  - core/shared
  - core/ui-sim
predict:
  prompt: |
    For x' = y and y' = -1.2x - 0.6y, what should a nearby trajectory do after it is released?
  commit_format:
    kind: multiple-choice
    options:
      - Spiral inward toward the equilibrium
      - Move directly away from the equilibrium
      - Cross the equilibrium and stop there
  rationale_required: true
manipulate:
  controls:
    - id: preset
      label: Portrait preset
      kind: selector
      kernel_binding: state.preset
    - id: trace
      label: Trace
      kind: slider
      kernel_binding: state.trace
      bounds:
        min: -2
        max: 2
        step: 0.1
    - id: determinant
      label: Determinant
      kind: slider
      kernel_binding: state.determinant
      bounds:
        min: -1.5
        max: 2.5
        step: 0.1
    - id: initial-x
      label: Initial x
      kind: slider
      kernel_binding: state.initialX
      bounds:
        min: -2
        max: 2
        step: 0.1
    - id: initial-y
      label: Initial y
      kind: slider
      kernel_binding: state.initialY
      bounds:
        min: -2
        max: 2
        step: 0.1
observe:
  renderers:
    - id: phase-portrait-renderer
      module: local
      symbol: OdePhasePortrait
      props_binding: |
        Show trace-determinant classification, vector-field plot, trajectory, nullclines, and formula substitution.
explain:
  prompt: |
    How do trace, determinant, and discriminant predict the observed trajectory before you read the plotted path?
  socratic: true
  expected_misconceptions_surfaced:
    - Equilibrium means nothing changes anywhere
    - Arrows show only physical velocity
```

## Kernel extensions

No core-change proposal is declared in this container. Kernel use is limited to declared dependencies above.

## Accessibility

- Prediction gate contract: required and tested when the sim is registered.
- Route-level axe coverage: record the latest shell or container-specific result in the preserved review section below.
- Media fallback: `media/fallback.svg` present.

## Tests

- Container validation: `pnpm container:validate shared/content/math/containers/ode-phase-portrait`
- Docs regeneration: `pnpm container:docs shared/content/math/containers/ode-phase-portrait`
- Prediction-gate test: `simulation/simulation.test.ts`
- Package or shell tests: record exact commands in the preserved validation section below.

## How to run locally

```bash
pnpm container:docs shared/content/math/containers/ode-phase-portrait
pnpm container:validate shared/content/math/containers/ode-phase-portrait
pnpm graph:generate
```

## Anieyrudh Filter pass

Date: 2026-05-20
Reviewers: Codex local review using container-auditor, sim-architect, and pedagogy-reviewer criteria
Filter version: aniegpt v1.0

### P0 resolved

- Prediction reveal is blocked until commitment — resolution: Playwright test asserts the observation region is absent before commit and visible after commit.
- ODE vector-field construction, integration, and stability classification use `@paideia/dynamical-systems` — resolution: sim code calls `linearVectorField2D`, `integrateFlow`, and `classifyLinear2D`; no local reusable solver, derivative, or classifier is introduced.
- Student-facing UI avoids code-facing package names — resolution: visible copy uses mathematical terms only.

### P1 addressed or deferred

- Formula substitution required for shown calculations — addressed in the observation formula panel for trace, determinant, discriminant, eigenvalues, and RK4 trajectory setup, with nearby legend, units, and interpretation.
- Manipulation must visibly change the state — addressed by a test that switches to the saddle preset and checks the revealed classification and interpretation.
- Accessibility after reveal — addressed by an axe scan with no critical or serious violations.
- Concrete axe evidence: `CI=1 pnpm -F @paideia/sim-harness test` on 2026-05-20 ran `shared/content/math/containers/ode-phase-portrait/simulation/simulation.test.ts`; the revealed-state axe scan reported 0 critical or serious violations for `shared/math/ode-phase-portrait/ode-phase-portrait` and the sim harness passed 61 tests.

### P2 noted

- Future iteration can add nonlinear examples once a shared nonlinear nullcline plotting affordance exists.

## Iteration log

- Built the shared-core ODE phase portrait as one v2 container plus one shared sim export.
- Kept ODE vector-field construction, stepping, and stability classification in the existing dynamical-systems kernel.
- Used generated docs and graph files rather than hand-editing shell graph metadata.
