runtime:
  entry: simulation/index.tsx
  mount_mode: react-component
  state_model: local
  persistence: local
  deterministic: true
visual_quality:
  setup: []
  prediction:
    option_label: "Current lags the voltage because the inductor adds positive reactance."
    rationale: "The inductor adds positive reactance, so current phase is negative."
  reveal:
    observation_label: "Observation unlocked"
    visual: required
    formula: required
