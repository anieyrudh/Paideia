# Gradient Descent Landscape · Technical Record

> **Auto-generated by `pnpm container:docs`. Updated by every product PR.**
> Source: `container.yaml`, sim source declarations, and preserved review sections.

## Architecture

- Container path: `shared/content/cs/containers/gradient-descent-landscape`
- Status: `reviewed`
- Concept card: `concept-card.md`
- Concept map: `concept-map/concept-map.yaml`
- Simulation spec: `simulation/simulation.yaml`
- Embed API: `embed/api.ts`
- Problem-solving algorithm: `problem-solving/algorithm.md`

## Imports

| Sim | Module | Symbols / role |
|---|---|---|
| loss-surface-stepper | `core/optimization` | Declared in `simulation/simulation.yaml` |
| loss-surface-stepper | `core/plotting` | Declared in `simulation/simulation.yaml` |
| loss-surface-stepper | `core/content-schema` | Declared in `simulation/simulation.yaml` |
| loss-surface-stepper | `core/prediction-gate` | Declared in `simulation/simulation.yaml` |
| loss-surface-stepper | `core/shared` | Declared in `simulation/simulation.yaml` |
| loss-surface-stepper | `core/ui-sim` | Declared in `simulation/simulation.yaml` |

## SimulationSpec (frozen)

Full validated YAML for the declared sim in this container at the time of last docs regeneration.

```yaml
id: loss-surface-stepper
title: Loss Surface Stepper
interaction_type: function-plot-with-draggable
kernel_deps:
  - core/optimization
  - core/plotting
  - core/content-schema
  - core/prediction-gate
  - core/shared
  - core/ui-sim
predict:
  prompt: |
    On a narrow loss ravine, what is most likely to happen when the learning rate is made much larger?
  commit_format:
    kind: multiple-choice
    options:
      - The path can overshoot and zig-zag across the valley
      - The path must converge in fewer steps
      - The gradient becomes exactly zero everywhere
      - The starting point stops affecting the trace
    correct_index: 0
  rationale_required: true
manipulate:
  controls:
    - id: loss-landscape
      label: Loss landscape
      kind: selector
      kernel_binding: core/optimization.gradientDescent.objective
    - id: start-x
      label: Start x
      kind: slider
      kernel_binding: core/optimization.gradientDescent.start[0]
      bounds:
        min: -4
        max: 4
        step: 0.2
    - id: start-y
      label: Start y
      kind: slider
      kernel_binding: core/optimization.gradientDescent.start[1]
      bounds:
        min: -4
        max: 4
        step: 0.2
    - id: learning-rate
      label: Learning rate
      kind: slider
      kernel_binding: core/optimization.gradientDescent.learningRate
      bounds:
        min: 0.05
        max: 0.75
        step: 0.01
    - id: maximum-steps
      label: Maximum steps
      kind: slider
      kernel_binding: core/optimization.gradientDescent.maxSteps
      bounds:
        min: 4
        max: 40
        step: 1
observe:
  renderers:
    - id: loss-surface-trace
      module: "@paideia/shared-sims/gradient-descent-landscape"
      symbol: GradientDescentLandscapeSim
      props_binding: core/optimization.gradientDescent trace rendered in a core/plotting PlotFrame
explain:
  prompt: Which part of the update formula explains whether this trace converged, oscillated, or left the domain?
  socratic: true
  expected_misconceptions_surfaced:
    - Bigger learning rate is always faster
```

## Kernel extensions

Declared runtime kernel dependencies are listed above. Any additive or breaking core API change must be linked in the preserved review section below.

## Accessibility

- Prediction gate contract: required and tested when the sim is registered.
- Route-level axe coverage: record the latest shell or container-specific result in the preserved review section below.
- Media fallback: `media/fallback.svg` present.

## Tests

- Container validation: `pnpm container:validate shared/content/cs/containers/gradient-descent-landscape`
- Docs regeneration: `pnpm container:docs shared/content/cs/containers/gradient-descent-landscape`
- Prediction-gate test: `simulation/simulation.test.ts`
- Package or shell tests: record exact commands in the preserved validation section below.

## How to run locally

```bash
pnpm container:docs shared/content/cs/containers/gradient-descent-landscape
pnpm container:validate shared/content/cs/containers/gradient-descent-landscape
pnpm graph:generate
```

## Latest Validation

- Focused sim package: `pnpm -F @paideia/shared-sims test --run` passed on 2026-05-20.
- Focused typecheck: `pnpm -F @paideia/shared-sims typecheck` passed on 2026-05-20.
- Container validation: `pnpm container:validate shared/content/cs/containers/gradient-descent-landscape` passed on 2026-05-20.
- Axe summary: `simulation/simulation.test.ts` scans the revealed state; latest focused Playwright run reported 0 serious or critical violations.

## Anieyrudh Filter pass

Date: 2026-05-20
Reviewers: local Codex review aligned to container-auditor, sim-architect, pedagogy-reviewer contracts

### P0 resolved

- Prediction gate: observation readouts, trace, and formula panel are not rendered before prediction commit.
- Kernel boundary: descent traces come from `core/optimization`; the sim does not inline descent math.
- Canonical shared path: container is under `shared/content/cs/containers/gradient-descent-landscape`.
- Added standalone `concept-map/misconceptions.md` with named misconceptions, evidence, and predict-surface notes.
- Explain prompt is now Socratic-question shaped when `socratic: true`.
- Transfer now uses a one-parameter calibration setting instead of repeating the same ravine preset comparison.

### P1 addressed or deferred

- Formula standard: readable colored formula tokens, legend, substitution, units, and interpretation are included in the revealed panel.
- Accessibility: Playwright axe coverage checks revealed state for serious or critical violations; latest focused run reported 0 serious/critical violations.
- CodeRabbit issues addressed: embed state now round-trips all learner-controlled fields, executable embed tests replace the marker file, property tests cover generated descent cases, transfer problem eta values match presets, and the shared-sims root barrel remains empty.

### P2 noted

- Future iteration can replace the heat-map tiles with a richer contour renderer if `core/plotting` adds a contour primitive.

## Iteration log

- Regenerated README.md and TECHNICAL.md with `pnpm container:docs`.
