# Misconception Audit

## Visual distance equals edge weight

The simulation deliberately uses a layout where a visually short edge is not the cheapest edge. Learners must use labelled weights and the path-cost formula.

## Visited order is unique

The controls compare BFS and DFS on the same adjacency order. The trace makes deterministic tie handling visible instead of implying a universal order.
