# Graph Algorithm Explorer · Technical Record

> **Auto-generated by `pnpm container:docs`. Updated by every product PR.**
> Source: `container.yaml`, sim source declarations, and preserved review sections.

## Architecture

- Container path: `shared/content/cs/containers/graph-algorithm-explorer`
- Status: `reviewed`
- Concept card: `concept-card.md`
- Concept map: `concept-map/concept-map.yaml`
- Simulation spec: `simulation/simulation.yaml`
- Embed API: `embed/api.ts`
- Problem-solving algorithm: `problem-solving/algorithm.md`

## Imports

| Sim | Module | Symbols / role |
|---|---|---|
| graph-algorithm-explorer | `core/graph-algorithms` | Declared in `simulation/simulation.yaml` |
| graph-algorithm-explorer | `core/graph-layout` | Declared in `simulation/simulation.yaml` |
| graph-algorithm-explorer | `core/algorithm-trace` | Declared in `simulation/simulation.yaml` |
| graph-algorithm-explorer | `core/content-schema` | Declared in `simulation/simulation.yaml` |
| graph-algorithm-explorer | `core/prediction-gate` | Declared in `simulation/simulation.yaml` |
| graph-algorithm-explorer | `core/shared` | Declared in `simulation/simulation.yaml` |
| graph-algorithm-explorer | `core/ui-sim` | Declared in `simulation/simulation.yaml` |

## SimulationSpec (frozen)

Full validated YAML for the declared sim in this container at the time of last docs regeneration.

```yaml
id: graph-algorithm-explorer
title: Graph Algorithm Explorer
interaction_type: algorithm-state-visualisation
kernel_deps:
  - core/graph-algorithms
  - core/graph-layout
  - core/algorithm-trace
  - core/content-schema
  - core/prediction-gate
  - core/shared
  - core/ui-sim
predict:
  prompt: |
    In a weighted graph, which claim is safest before you run the algorithms?
  commit_format:
    kind: multiple-choice
    options:
      - BFS finds the fewest edges, while Dijkstra finds the lowest total weight
      - DFS always reaches the cheapest route first
      - The visually closest node must have the smallest edge weight
      - Every valid graph has only one possible visited order
    correct_index: 0
  rationale_required: true
manipulate:
  controls:
    - id: algorithm-mode
      label: Algorithm mode
      kind: selector
      kernel_binding: core/graph-algorithms.traversalOrShortestPath
    - id: graph-scenario
      label: Graph scenario
      kind: selector
      kernel_binding: core/graph-layout.forceDirected2D.graph
observe:
  renderers:
    - id: graph-trace-panel
      module: "@paideia/shared-sims/graph-algorithm-explorer"
      symbol: GraphAlgorithmExplorerSim
      props_binding: core/graph-layout coordinates, core/algorithm-trace traversal steps, and core/graph-algorithms answers
explain:
  prompt: Which invariant explains why the selected algorithm can be trusted for the current objective?
  socratic: true
  expected_misconceptions_surfaced:
    - Visual distance equals edge weight
    - Visited order is unique for every graph
```

## Kernel extensions

Declared runtime kernel dependencies are listed above. Any additive or breaking core API change must be linked in the preserved review section below.

## Accessibility

- Prediction gate contract: required and tested when the sim is registered.
- Route-level axe coverage: record the latest shell or container-specific result in the preserved review section below.
- Media fallback: `media/fallback.svg` present.

## Tests

- Container validation: `pnpm container:validate shared/content/cs/containers/graph-algorithm-explorer`
- Docs regeneration: `pnpm container:docs shared/content/cs/containers/graph-algorithm-explorer`
- Prediction-gate test: `simulation/simulation.test.ts`
- Package or shell tests: record exact commands in the preserved validation section below.

## How to run locally

```bash
pnpm container:docs shared/content/cs/containers/graph-algorithm-explorer
pnpm container:validate shared/content/cs/containers/graph-algorithm-explorer
pnpm graph:generate
```

## Anieyrudh Filter pass

Date: 2026-05-22
Reviewers (subagents): container-auditor, sim-architect, pedagogy-reviewer

### P0 resolved
- Prediction gate answer leak: graph evidence, formula panel, and path-cost readouts are inside `PredictionGate`; Playwright coverage asserts absence before commit.
- Kernel boundary: traversal answers use `core/graph-algorithms`, layouts use `core/graph-layout`, and operation traces use `core/algorithm-trace`.
- Representation truthfulness: the learner-facing text states that visual distance is not edge weight, and the formula panel uses labelled weights.

### P1 addressed or deferred
- Product formula standard: addressed with a formula panel that includes LaTeX-like source, legend, substitution, units, and interpretation.
- Transfer surface: addressed with route-planning shortest-path rubric and problem-solving steps.

### P2 noted
- Future iteration can add minimum-spanning-tree and component modes once the shared shell has a broader algorithms gallery.

## Iteration log

- Initial shared-core product slice built on 2026-05-22.
