runtime:
  package_id: graph-algorithm-explorer
  sim_id: graph-algorithm-explorer
  entry: simulation/index.tsx
  renderer_module: "@paideia/shared-sims/graph-algorithm-explorer"
  prediction_gate:
    required: true
    scope: per-sim
  persistence:
    local_state: true
    embed_state: true
visual_quality:
  setup: []
  prediction:
    option_label: "BFS finds the fewest edges, while Dijkstra finds the lowest total weight"
    rationale: "Weights and hop count are different objectives."
  reveal:
    observation_label: "Observation unlocked"
    visual: required
    formula: required
