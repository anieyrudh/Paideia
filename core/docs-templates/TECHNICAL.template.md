# <Concept Title> · Technical Record

> **Auto-generated by `/new-container` and `/review-container`. Updated by every PR.**
> Source: `container.yaml`, sim source files, CI artefacts.

## Imports

For each sim in this container, list the `core/` modules consumed and the
specific exported symbols used. The validator confirms imports here match
imports in source.

| Sim | Module | Symbols |
|---|---|---|
| <sim-id> | `core/numerical-math` | `derivative`, `secantSlope` |
| <sim-id> | `core/plotting` | `FunctionPlot` |
| <sim-id> | `core/sim-runtime` | `SimRuntime` |
| <sim-id> | `core/prediction-gate` | `PredictionGate` |

## SimulationSpec (frozen)

Full validated YAML for each sim in this container. Reproduced from
`simulation/simulation.yaml` at the time of last build.

```yaml
# (auto-injected at build)
```

## Kernel extensions

Any `core-change-proposal` issue filed during the building of this container.
If none, write "None". If yes, link issue + state of resolution.

## Accessibility

axe results at last green build:
- Critical issues: 0
- Serious issues: ...
- Documented suppressions: ...

## Tests

- Prediction-gate Playwright: `simulation/simulation.test.ts`
- Additional tests: ...

## How to run locally

```bash
pnpm dev --filter @paideia/<branch>-shell
pnpm container:validate
```

## Anieyrudh Filter pass

> Required once `container.yaml` reaches the configured Filter lifecycle
> threshold, usually `published`. Draft UI and layout iterations may leave this
> section as a short note explaining why the Filter was not run.

Date: <YYYY-MM-DD>
Filter version: aniegpt v1.0

### P0 issues

(Each P0 must be resolved before publishing. List them and resolution.)

- ...

### P1 issues

(P1 may be deferred with a tracked issue. List them and either resolution or
deferral issue link.)

- ...

### High-bandwidth questions surfaced

- ...

## Iteration log

Brief log of what the agent did and what was rejected during authoring. This
is the "What AI did / what I rejected" record from the PR template, persisted.

- ...
