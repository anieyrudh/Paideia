# Momentum and Impulse · Technical Record

> **Auto-generated by `pnpm container:docs`. Updated by every product PR.**
> Source: `container.yaml`, sim source declarations, and preserved review sections.

## Architecture

- Container path: `a-level/content/physics/containers/momentum`
- Status: `reviewed`
- Concept card: `concept-card.md`
- Concept map: `concept-map/concept-map.yaml`
- Simulation spec: `simulation/simulation.yaml`
- Embed API: `embed/api.ts`
- Problem-solving algorithm: `problem-solving/algorithm.md`

## Imports

| Sim | Module | Symbols / role |
|---|---|---|
| momentum-collision-lab | `core/sim-runtime` | Declared in `simulation/simulation.yaml` |
| momentum-collision-lab | `core/content-schema` | Declared in `simulation/simulation.yaml` |
| momentum-collision-lab | `core/mechanics` | Declared in `simulation/simulation.yaml` |
| momentum-collision-lab | `core/charting` | Declared in `simulation/simulation.yaml` |
| momentum-collision-lab | `core/prediction-gate` | Declared in `simulation/simulation.yaml` |
| momentum-collision-lab | `core/shared` | Declared in `simulation/simulation.yaml` |
| momentum-collision-lab | `core/ui-sim` | Declared in `simulation/simulation.yaml` |

## SimulationSpec (frozen)

Full validated YAML for the declared sim in this container at the time of last docs regeneration.

```yaml
id: momentum-collision-lab
title: Collision and Impulse Lab
interaction_type: diagram-builder
kernel_deps:
  - core/sim-runtime
  - core/content-schema
  - core/mechanics
  - core/charting
  - core/prediction-gate
  - core/shared
  - core/ui-sim
predict:
  prompt: |
    Cart A has mass 0.50 kg and moves at +2.0 m s^-1. Cart B has mass 1.0 kg and moves at -0.5 m s^-1. Before revealing the elastic collision, what happens to total momentum of the two-cart system?
  commit_format:
    kind: multiple-choice
    options:
      - Total momentum changes because both speeds change.
      - Total momentum stays constant if external horizontal force is negligible.
      - Momentum is conserved only when the cart masses are equal.
      - Momentum is not conserved because the carts exert forces on each other.
    correct_index: 1
  rationale_required: true
manipulate:
  controls:
    - id: mass-a
      label: Mass of cart A
      kind: slider
      kernel_binding: state.massAKilograms
      bounds:
        min: 0.2
        max: 3
        step: 0.1
    - id: mass-b
      label: Mass of cart B
      kind: slider
      kernel_binding: state.massBKilograms
      bounds:
        min: 0.2
        max: 3
        step: 0.1
    - id: velocity-a
      label: Initial velocity of cart A
      kind: slider
      kernel_binding: state.velocityAMetresPerSecond
      bounds:
        min: -5
        max: 5
        step: 0.1
    - id: velocity-b
      label: Initial velocity of cart B
      kind: slider
      kernel_binding: state.velocityBMetresPerSecond
      bounds:
        min: -5
        max: 5
        step: 0.1
observe:
  renderers:
    - id: momentum-collision-lab
      module: "@paideia/a-level-physics-sims/momentum"
      symbol: MomentumSim
      props_binding: |
        Show before-and-after momentum, final velocities, impulse on each cart, formula substitution, units, and interpretation.
explain:
  prompt: |
    Why can each cart's momentum change while the total momentum of the isolated two-cart system stays constant?
  socratic: true
  expected_misconceptions_surfaced:
    - Momentum and force are the same quantity
    - Momentum conservation needs equal masses
    - Conservation means each object keeps its own momentum
```

## Kernel extensions

No core-change proposal is declared in this container. Kernel use is limited to declared dependencies above.

## Accessibility

- Prediction gate contract: required and tested when the sim is registered.
- Route-level axe coverage: record the latest shell or container-specific result in the preserved review section below.
- Media fallback: `media/fallback.svg` present.

## Tests

- Container validation: `pnpm container:validate a-level/content/physics/containers/momentum`
- Docs regeneration: `pnpm container:docs a-level/content/physics/containers/momentum`
- Prediction-gate test: `simulation/simulation.test.ts`
- Package or shell tests: record exact commands in the preserved validation section below.

## How to run locally

```bash
pnpm container:docs a-level/content/physics/containers/momentum
pnpm container:validate a-level/content/physics/containers/momentum
pnpm graph:generate
```

## Latest Validation

Local validation on 2026-05-19:

- `pnpm install --no-frozen-lockfile` - passed after sandbox DNS failed without network access.
- `pnpm -F @paideia/a-level-physics-sims build` - passed.
- `pnpm -F @paideia/a-level-physics-sims test` - passed, 25 tests.
- `pnpm container:validate a-level/content/physics/containers/momentum` - passed.
- `pnpm graph:generate` - passed.
- `pnpm typecheck` - passed.
- `pnpm lint` - passed.
- `pnpm -F @paideia/sim-harness test` - passed, 26 browser tests.
- `pnpm -F @paideia/a-level-shell test` - passed, 12 browser tests.
- Container-level axe coverage: `a-level/content/physics/containers/momentum/simulation/simulation.test.ts` runs Axe after reveal; latest focused sim-harness run passed with 27 browser tests and no critical accessibility violations for this sim.
- `pnpm boundary` - passed.
- `pnpm license:check` - passed.
- `pnpm roadmap:validate` - passed.
- `pnpm agent:validate` - passed.
- `pnpm test` - passed across core, A-Level, SUTD shell, and sim harness suites.
- `pnpm graph:check` - expected to pass after this branch commits generated graph files; before commit it correctly reports the generated catalogue diff as uncommitted.

## Anieyrudh Filter pass

Date: 2026-05-19

### P0 resolved
- Prediction-before-reveal could be bypassed if the observation panel mounted directly. Resolution: mounted the reveal stage through `@paideia/sim-runtime`, which wraps observe/explain in `@paideia/prediction-gate`, and added package plus browser contract tests.
- Reusable physics could drift into the container. Resolution: work and kinetic energy calculations call `core/mechanics`; chart rendering uses `core/charting`; controls use `core/ui-sim`.

### P1 addressed or deferred
- Student UI could become formula-only. Addressed with a force/displacement diagram, energy-store bar, presets, and formula substitution with units.
- Braking work can mathematically exceed the starting kinetic store. Addressed by clamping the displayed kinetic store at zero and naming the transfer out of the store.

### High-bandwidth questions surfaced
- Should a future core/mechanics function model stopping distance explicitly when negative work exceeds the kinetic store?
- Should A-Level product sims standardize on `core/sim-runtime`, or keep direct `PredictionGate` wrappers for simpler flows?

## Iteration log

- Regenerated README.md and TECHNICAL.md with `pnpm container:docs`.
