# Forces and Equilibrium · Technical Record

> **Auto-generated by `pnpm container:docs`. Updated by every product PR.**
> Source: `container.yaml`, sim source declarations, and preserved review sections.

## Architecture

- Container path: `a-level/content/physics/containers/forces-and-equilibrium`
- Status: `reviewed`
- Concept card: `concept-card.md`
- Concept map: `concept-map/concept-map.yaml`
- Simulation spec: `simulation/simulation.yaml`
- Embed API: `embed/api.ts`
- Problem-solving algorithm: `problem-solving/algorithm.md`

## Imports

| Sim | Module | Symbols / role |
|---|---|---|
| force-balance | `core/content-schema` | Declared in `simulation/simulation.yaml` |
| force-balance | `core/mechanics` | Declared in `simulation/simulation.yaml` |
| force-balance | `core/prediction-gate` | Declared in `simulation/simulation.yaml` |
| force-balance | `core/shared` | Declared in `simulation/simulation.yaml` |
| force-balance | `core/ui-sim` | Declared in `simulation/simulation.yaml` |

## SimulationSpec (frozen)

Full validated YAML for the declared sim in this container at the time of last docs regeneration.

```yaml
id: force-balance
title: Force Balance Explorer
interaction_type: diagram-builder
kernel_deps:
  - core/content-schema
  - core/mechanics
  - core/prediction-gate
  - core/shared
  - core/ui-sim
predict:
  prompt: |
    A 6 N force pulls left and a 5 N weight pulls down. What pair of support forces is needed for equilibrium?
  commit_format:
    kind: multiple-choice
    options:
      - 6 N right and 5 N up
      - 5 N right and 6 N up
      - 11 N right only
      - 1 N up only
  rationale_required: true
manipulate:
  controls:
    - id: right-support
      label: Right support force
      kind: slider
      kernel_binding: state.supportRightNewtons
      bounds:
        min: 0
        max: 10
        step: 0.5
    - id: up-support
      label: Up support force
      kind: slider
      kernel_binding: state.supportUpNewtons
      bounds:
        min: 0
        max: 10
        step: 0.5
observe:
  renderers:
    - id: force-balance
      module: "@paideia/a-level-physics-sims/forces-and-equilibrium"
      symbol: ForcesAndEquilibriumSim
      props_binding: state -> force diagram, net-force readout, and equilibrium formula
explain:
  prompt: Which component is still unbalanced, and what acceleration direction would that imply?
  socratic: true
  expected_misconceptions_surfaced:
    - Balanced means equal-looking arrows
    - Zero net force means no forces act
```

## Kernel extensions

No core-change proposal is declared in this container. Kernel use is limited to declared dependencies above.

## Accessibility

- Prediction checkpoint contract: required and tested when the sim is registered.
- Route-level axe coverage: record the latest shell or container-specific result in the preserved review section below.
- Media fallback: `media/fallback.svg` present.

## Tests

- Container validation: `pnpm container:validate a-level/content/physics/containers/forces-and-equilibrium`
- Docs regeneration: `pnpm container:docs a-level/content/physics/containers/forces-and-equilibrium`
- Prediction-checkpoint test: `simulation/simulation.test.ts`
- Package or shell tests: record exact commands in the preserved validation section below.

## How to run locally

```bash
pnpm container:docs a-level/content/physics/containers/forces-and-equilibrium
pnpm container:validate a-level/content/physics/containers/forces-and-equilibrium
pnpm graph:generate
```

## Anieyrudh Filter pass

Date: 2026-05-19
Filter version: aniegpt v1.0

### P0 issues

- Package-boundary violation: draft renderer pointed at `core/plotting`. Resolved by adding `@paideia/a-level-physics-sims/forces-and-equilibrium` and making the container re-export it.
- Placeholder sim-runtime code bypassed the established A-Level package pattern. Resolved by implementing a real package sim with `PredictionGate`, `core/mechanics`, and UI controls.

### P1 issues

- Scaffold content and docs were placeholders. Resolved with reviewed concept text, sources, tests, and technical record.

### High-bandwidth questions surfaced

- A later container should extend this to angled tensions after the learner has mastered vector resolution.

## Iteration log

- Rejected the generic plotting scaffold because equilibrium is a vector-balance concept.
- Kept the first slice intentionally perpendicular: horizontal balance and vertical balance are visible separately.
