# Oscillations · Technical Record

> **Auto-generated by `pnpm container:docs`. Updated by every product PR.**
> Source: `container.yaml`, sim source declarations, and preserved review sections.

## Architecture

- Container path: `a-level/content/physics/containers/oscillations`
- Status: `reviewed`
- Concept card: `concept-card.md`
- Concept map: `concept-map/concept-map.yaml`
- Simulation spec: `simulation/simulation.yaml`
- Embed API: `embed/api.ts`
- Problem-solving algorithm: `problem-solving/algorithm.md`

## Imports

| Sim | Module | Symbols / role |
|---|---|---|
| simple-harmonic-motion-lab | `core/sim-runtime` | Declared in `simulation/simulation.yaml` |
| simple-harmonic-motion-lab | `core/content-schema` | Declared in `simulation/simulation.yaml` |
| simple-harmonic-motion-lab | `core/mechanics` | Declared in `simulation/simulation.yaml` |
| simple-harmonic-motion-lab | `core/charting` | Declared in `simulation/simulation.yaml` |
| simple-harmonic-motion-lab | `core/prediction-gate` | Declared in `simulation/simulation.yaml` |
| simple-harmonic-motion-lab | `core/shared` | Declared in `simulation/simulation.yaml` |
| simple-harmonic-motion-lab | `core/ui-sim` | Declared in `simulation/simulation.yaml` |

## SimulationSpec (frozen)

Full validated YAML for the declared sim in this container at the time of last docs regeneration.

```yaml
id: simple-harmonic-motion-lab
title: Simple Harmonic Motion Lab
interaction_type: animation-playback
kernel_deps:
  - core/sim-runtime
  - core/content-schema
  - core/mechanics
  - core/charting
  - core/prediction-gate
  - core/shared
  - core/ui-sim
predict:
  prompt: |
    A spring oscillator has the same mass and spring constant, but the amplitude is doubled. Before comparing with the lab, what happens to the period?
  commit_format:
    kind: multiple-choice
    options:
      - The period doubles
      - The period stays the same
      - The period halves
      - The period becomes zero at equilibrium
    correct_index: 1
  rationale_required: true
manipulate:
  controls:
    - id: mass
      label: Mass
      kind: slider
      kernel_binding: state.massKilograms
      bounds:
        min: 0.5
        max: 5
        step: 0.1
    - id: spring-constant
      label: Spring stiffness
      kind: slider
      kernel_binding: state.springConstantNewtonsPerMetre
      bounds:
        min: 4
        max: 80
        step: 1
    - id: amplitude
      label: Amplitude
      kind: slider
      kernel_binding: state.amplitudeMetres
      bounds:
        min: 0.1
        max: 2
        step: 0.05
    - id: phase
      label: Starting phase
      kind: slider
      kernel_binding: state.phaseRadians
      bounds:
        min: 0
        max: 6.28
        step: 0.1
    - id: time
      label: Time marker
      kind: slider
      kernel_binding: state.timeSeconds
      bounds:
        min: 0
        max: 8
        step: 0.05
observe:
  renderers:
    - id: simple-harmonic-motion-lab
      module: "@paideia/a-level-physics-sims/oscillations"
      symbol: OscillationsSim
      props_binding: |
        Show mass-spring displacement, velocity and acceleration signs, energy exchange, formula substitution, and period-frequency readouts.
explain:
  prompt: |
    Which quantity controls the period, which evidence shows the restoring acceleration, and what changes when amplitude is larger?
  socratic: true
  expected_misconceptions_surfaced:
    - Amplitude changes the period for every oscillator
    - Acceleration is greatest at equilibrium
    - Zero displacement means zero speed and zero energy
```

## Kernel extensions

Declared runtime kernel dependencies are listed above. Any additive or breaking core API change must be linked in the preserved review section below.

## Accessibility

- Prediction checkpoint contract: required and tested when the sim is registered.
- Route-level axe coverage: record the latest shell or container-specific result in the preserved review section below.
- Media fallback: `media/fallback.svg` present.

## Tests

- Container validation: `pnpm container:validate a-level/content/physics/containers/oscillations`
- Docs regeneration: `pnpm container:docs a-level/content/physics/containers/oscillations`
- Prediction-checkpoint test: `simulation/simulation.test.ts`
- Package or shell tests: record exact commands in the preserved validation section below.

## How to run locally

```bash
pnpm container:docs a-level/content/physics/containers/oscillations
pnpm container:validate a-level/content/physics/containers/oscillations
pnpm graph:generate
```

## Latest Validation

Local validation on 2026-05-21:

- `pnpm install` - passed after sandbox DNS failed with `ENOTFOUND registry.npmjs.org`; rerun with network approval succeeded.
- `pnpm -F @paideia/shared build` - passed.
- `pnpm -F @paideia/mechanics build` - passed.
- `pnpm -F @paideia/mechanics typecheck` - passed.
- `pnpm -F @paideia/mechanics test` - passed, 26 tests.
- `pnpm -F @paideia/a-level-physics-sims typecheck` - passed.
- `pnpm -F @paideia/a-level-physics-sims test` - passed, 53 tests.
- `pnpm container:validate a-level/content/physics/containers/oscillations` - passed.
- `pnpm container:docs a-level/content/physics/containers/oscillations` - passed.
- `pnpm container:docs --check a-level/content/physics/containers/oscillations` - passed.
- `pnpm graph:generate` - passed.
- `pnpm graph:check` - passed.
- `pnpm roadmap:validate` - passed.
- `pnpm agent:validate` - passed.
- `pnpm license:check` - passed.
- `pnpm typecheck` - passed.
- `pnpm lint` - passed.
- `pnpm boundary` - passed.
- `CI=1 pnpm -F @paideia/sim-harness test a-level/content/physics/containers/oscillations/simulation/simulation.test.ts` - passed, 3 browser tests including revealed-state axe scan.
- `CI=1 pnpm -F @paideia/sim-harness test shared/content/math/containers/eigenvector-transformations/simulation/simulation.test.ts` - passed, 3 browser tests, confirming the harness stylesheet override fixed the unrelated contrast regression.
- `CI=1 pnpm test` - passed, including 83 sim-harness browser tests.

## Anieyrudh Filter pass

Date: 2026-05-21
Reviewers (subagents): container-auditor, sim-architect, pedagogy-reviewer, test-writer, frontend/game UI reviewer

### P0 resolved

- Prediction-checkpoint leak in manipulation stage — resolved by removing the numeric period preview before commit and keeping period, energy, displacement, velocity, and acceleration evidence behind the runtime prediction checkpoint.
- Reusable oscillator math in the sim surface — resolved by moving ideal spring oscillator timing, SHM sampling, and energy trace generation into `core/mechanics.springOscillator`.
- Dead `core/function-eval` dependency — resolved by removing it from the queue entry, simulation spec, runtime TS spec, and generated docs because the mechanics kernel owns the oscillator math.
- Filter output placeholder in this technical record — resolved by replacing the generated placeholder with this explicit review disposition.
- Misconception artifact missing — resolved by adding `concept-map/misconceptions.md` with evidence and predict-surface fields.

### P1 addressed or deferred

- Public oscillator state/model units used bare numbers — addressed by adding branded `MetresPerSecondSquared` and `NewtonsPerMetre` unit types in `core/shared` and using branded angular frequency, velocity, and acceleration in the public SHM/mechanics APIs consumed by the sim.
- Explain prompt telegraphed the conclusion — addressed by changing it to ask learners to use evidence to decide what controls timing, acceleration, and energy.
- Learner-facing Explain stage skipped straight to transfer — addressed by adding an explicit explain prompt before the car-suspension transfer.
- Chart and diagram colors conflicted for velocity and acceleration — addressed by ordering chart series as displacement, acceleration, velocity to match the shared chart palette and diagram colors.
- Formula/legend acceleration color conflicted with the chart and diagram — addressed by adding red formula and swatch classes and using them for acceleration.
- Harness route lacked product sim styling — addressed by importing the A-Level shell stylesheet through a harness stylesheet, adding the missing purple/red legend swatches, and overriding SUTD formula-card legend colors to preserve accessibility.
- Queue status remains `in-build` while this branch is under review — intentionally kept per maintainer instruction to leave the queue entry `in-build` before PR merge.
- Accessibility record incomplete — addressed by focused sim-harness Playwright coverage: prediction-checkpoint blocking, manipulation changing the visible period, and revealed-state axe scan all passed locally.

### P2 noted

- Add a current-time marker or highlighted dot on the trace in a future polish pass.

### High-bandwidth questions surfaced

- Should the sim harness own a dedicated shared sim stylesheet rather than importing the A-Level shell stylesheet?

## Iteration log

- Regenerated README.md and TECHNICAL.md with `pnpm container:docs`.
