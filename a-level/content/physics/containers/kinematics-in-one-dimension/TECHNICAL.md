# Kinematics in One Dimension · Technical Record

> **Auto-generated by `pnpm container:docs`. Updated by every product PR.**
> Source: `container.yaml`, sim source declarations, and preserved review sections.

## Architecture

- Container path: `a-level/content/physics/containers/kinematics-in-one-dimension`
- Status: `reviewed`
- Concept card: `concept-card.md`
- Concept map: `concept-map/concept-map.yaml`
- Simulation spec: `simulation/simulation.yaml`
- Embed API: `embed/api.ts`
- Problem-solving algorithm: `problem-solving/algorithm.md`

## Imports

| Sim | Module | Symbols / role |
|---|---|---|
| motion-equations-lab | `core/content-schema` | Declared in `simulation/simulation.yaml` |
| motion-equations-lab | `core/mechanics` | Declared in `simulation/simulation.yaml` |
| motion-equations-lab | `core/prediction-gate` | Declared in `simulation/simulation.yaml` |
| motion-equations-lab | `core/shared` | Declared in `simulation/simulation.yaml` |
| motion-equations-lab | `core/ui-sim` | Declared in `simulation/simulation.yaml` |

## SimulationSpec (frozen)

Full validated YAML for the declared sim in this container at the time of last docs regeneration.

```yaml
id: motion-equations-lab
title: Motion Equations Lab
interaction_type: animation-playback
kernel_deps:
  - core/content-schema
  - core/mechanics
  - core/prediction-gate
  - core/shared
  - core/ui-sim
predict:
  prompt: |
    A trolley starts from rest and accelerates at 2.0 m s^-2 for 3.0 s. Before revealing the trace, which displacement is closest?
  commit_format:
    kind: multiple-choice
    options:
      - 3.0 m
      - 6.0 m
      - 9.0 m
      - 18.0 m
    correct_index: 2
  rationale_required: true
manipulate:
  controls:
    - id: initial-velocity
      label: Initial velocity
      kind: slider
      kernel_binding: state.initialVelocityMetresPerSecond
      bounds:
        min: -5
        max: 12
        step: 0.5
    - id: acceleration
      label: Acceleration
      kind: slider
      kernel_binding: state.accelerationMetresPerSecondSquared
      bounds:
        min: -4
        max: 6
        step: 0.5
    - id: elapsed-time
      label: Elapsed time
      kind: slider
      kernel_binding: state.elapsedSeconds
      bounds:
        min: 0
        max: 8
        step: 0.5
observe:
  renderers:
    - id: motion-equations-lab
      module: "@paideia/a-level-physics-sims/kinematics-one-dimension"
      symbol: KinematicsOneDimensionSim
      props_binding: |
        Show a displacement-time trace, current trolley position, final velocity, formula substitution, units, and velocity-time area reasoning from the current one-dimensional state.
explain:
  prompt: |
    How do the signs of velocity and acceleration tell you whether the object is speeding up, slowing down, or changing direction?
  socratic: true
  expected_misconceptions_surfaced:
    - Velocity and acceleration must have the same sign
    - Average velocity equals final velocity
    - Distance and displacement are interchangeable
```

## Kernel extensions

No core-change proposal is declared in this container. Kernel use is limited to declared dependencies above.

## Accessibility

- Prediction gate contract: required and tested when the sim is registered.
- Route-level axe coverage: record the latest shell or container-specific result in the preserved review section below.
- Media fallback: `media/fallback.svg` present.

## Tests

- Container validation: `pnpm container:validate a-level/content/physics/containers/kinematics-in-one-dimension`
- Docs regeneration: `pnpm container:docs a-level/content/physics/containers/kinematics-in-one-dimension`
- Prediction-gate test: `simulation/simulation.test.ts`
- Package or shell tests: record exact commands in the preserved validation section below.

## How to run locally

```bash
pnpm container:docs a-level/content/physics/containers/kinematics-in-one-dimension
pnpm container:validate a-level/content/physics/containers/kinematics-in-one-dimension
pnpm graph:generate
```

## Failure log

| Attempt | Failed where | Symptom | Resolution |
| --- | --- | --- | --- |
| Manual README/TECHNICAL draft | container-auditor | `pnpm container:docs --check` reported stale docs | Regenerated docs with `pnpm container:docs` and preserved review sections |
| First sim model | sim-architect | The sim recomputed displacement for the velocity-time area note | Reused `core/mechanics` displacement output as the area value |
| First public sim types | sim-architect | Velocity and acceleration fields were exported as bare numbers | Added local branded composite-unit types at the sim package boundary |
| First misconception record | pedagogy-reviewer | Misconceptions were present in YAML but not as a learner-flow audit artifact | Added `concept-map/misconceptions.md` using the established physics-container convention |
| First formula note | pedagogy-reviewer | The observation panel answered the sign-confusion explain prompt directly | Reworded it as a Socratic question tied to the slowing-down preset |

## Deferred fixes

- Advisor sign-off remains deferred until a human Physics reviewer checks the final copy against classroom sequence.
- A generated post-reveal axe test per sim would reduce manual shell test maintenance.
- A future motion-graphs sibling container should deepen graph gradient and area reasoning.

## Latest Validation

Local validation on 2026-05-18:

- `pnpm graph:generate` - passed
- `pnpm -F @paideia/a-level-physics-sims test` - passed, 16 tests
- `pnpm -F @paideia/a-level-shell test` - passed, 10 Playwright/axe tests
- `pnpm -F @paideia/sim-harness test` - passed, 4 prediction-gate browser tests
- `pnpm typecheck` - passed
- `pnpm lint` - passed
- `pnpm test` - passed
- `pnpm boundary` - passed
- `pnpm license:check` - passed
- `pnpm container:validate` - passed
- `pnpm container:docs --check a-level/content/physics/containers/kinematics-in-one-dimension` - passed after regeneration

## Anieyrudh Filter pass

Date: 2026-05-18
Reviewers: container-auditor, sim-architect, pedagogy-reviewer
Filter version: aniegpt v1.0

### P0 resolved

- Prediction reveal risk: the observation readout, trace, and formula panel are inside `PredictionGate`; jsdom and sim-harness tests assert absence before commit.
- Kernel ownership risk: constant-acceleration calculations route through `core/mechanics`; the sim package prepares state, samples, and renderable labels without recomputing displacement.
- Container integrity risk: canonical files exist and `container.yaml` records the Filter pass.
- Sim architecture review P0s: declared `core/content-schema`, moved the React contract to `.tsx`, and branded composite-unit public fields.

### P1 addressed or deferred

- Formula opacity addressed: the simulation shows equations, substitution, signed units, and velocity-time area reasoning.
- Misconception coverage addressed: prediction and explanation target final-velocity-as-average and sign-confusion errors.
- Accessibility summary addressed: shell axe tests pass on the first shell screen and after physical-quantities, scalars-and-vectors, resolving-vectors, and kinematics reveals.
- Documentation freshness addressed: README.md and TECHNICAL.md were regenerated with `pnpm container:docs`.
- Human advisor review deferred: no human sign-off yet, tracked in `advisor_signoffs`.

### P2 noted

- Motion graphs deserve a separate deeper container, linked as a planned sibling.

## Iteration log

- Regenerated README.md and TECHNICAL.md with `pnpm container:docs`.
