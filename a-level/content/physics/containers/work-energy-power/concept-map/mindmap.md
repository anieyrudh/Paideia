# Work, Energy, Power Mindmap

```mermaid
mindmap
  root((Work, energy, power))
    Work
      "force component along displacement"
      "W = F s cos(theta)"
      "positive, zero, negative"
    Energy stores
      "kinetic store"
      "transfer into store"
      "transfer out of store"
    Power
      "rate of transfer"
      "P = W / t"
      "same work, different time"
    Misconceptions
      "ignore direction"
      "energy disappears"
      "power equals energy"
```
