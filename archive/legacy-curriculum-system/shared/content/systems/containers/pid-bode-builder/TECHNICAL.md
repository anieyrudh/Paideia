# PID Tuner and Bode Builder · Technical Record

> **Auto-generated by `pnpm container:docs`. Updated by every product PR.**
> Source: `container.yaml`, sim source declarations, and preserved review sections.

## Architecture

- Container path: `shared/content/systems/containers/pid-bode-builder`
- Status: `reviewed`
- Concept card: `concept-card.md`
- Concept map: `concept-map/concept-map.yaml`
- Simulation spec: `simulation/simulation.yaml`
- Embed API: `embed/api.ts`
- Problem-solving algorithm: `problem-solving/algorithm.md`

## Imports

| Sim | Module | Symbols / role |
|---|---|---|
| pid-bode-builder | `core/sim-runtime` | Declared in `simulation/simulation.yaml` |
| pid-bode-builder | `core/control-systems` | Declared in `simulation/simulation.yaml` |
| pid-bode-builder | `core/charting` | Declared in `simulation/simulation.yaml` |
| pid-bode-builder | `core/prediction-gate` | Declared in `simulation/simulation.yaml` |
| pid-bode-builder | `core/ui-sim` | Declared in `simulation/simulation.yaml` |
| pid-bode-builder | `core/shared` | Declared in `simulation/simulation.yaml` |

## SimulationSpec (frozen)

Full validated YAML for the declared sim in this container at the time of last docs regeneration.

```yaml
id: pid-bode-builder
title: PID Tuner and Bode Builder
interaction_type: systems-flow-diagram
kernel_deps:
  - core/sim-runtime
  - core/control-systems
  - core/charting
  - core/prediction-gate
  - core/ui-sim
  - core/shared
predict:
  prompt: |
    A PID loop starts with Kp = 1.4, Ki = 0.7, and Kd = 0.18. If Kp is increased while the plant is unchanged, what is the most likely robustness tradeoff?
  commit_format:
    kind: multiple-choice
    options:
      - The response can get faster, but the phase margin may shrink.
      - The phase margin must increase because the controller is stronger.
      - Only the Bode magnitude changes; the step response cannot change.
      - Derivative action will remove the final steady-state error.
    correct_index: 0
  rationale_required: true
manipulate:
  controls:
    - id: proportional-gain
      label: Proportional gain Kp
      kind: slider
      kernel_binding: state.kp
      bounds:
        min: 0.2
        max: 5
        step: 0.1
    - id: integral-gain
      label: Integral gain Ki
      kind: slider
      kernel_binding: state.ki
      bounds:
        min: 0
        max: 2.5
        step: 0.05
    - id: derivative-gain
      label: Derivative gain Kd
      kind: slider
      kernel_binding: state.kd
      bounds:
        min: 0
        max: 1
        step: 0.02
    - id: natural-frequency
      label: Plant natural frequency
      kind: slider
      kernel_binding: state.naturalFrequencyRadPerSec
      bounds:
        min: 1
        max: 6
        step: 0.1
    - id: damping-ratio
      label: Plant damping ratio
      kind: slider
      kernel_binding: state.dampingRatio
      bounds:
        min: 0.15
        max: 1.2
        step: 0.05
observe:
  renderers:
    - id: pid-bode-readout
      module: "@paideia/shared-sims/pid-bode-builder"
      symbol: PidBodeBuilderSim
      props_binding: |
        Show PID gains, closed-loop step response, open-loop Bode traces, margin readouts, formula legend, substitutions, units, and interpretation.
explain:
  prompt: |
    Which gain improved the time response, and what did it cost in phase margin or overshoot?
  socratic: true
  expected_misconceptions_surfaced:
    - High gain always improves control
    - Bode magnitude alone determines stability
    - Derivative gain removes steady-state error
```

## Kernel extensions

Declared runtime kernel dependencies are listed above. Any additive or breaking core API change must be linked in the preserved review section below.

## Accessibility

- Prediction checkpoint contract: required and tested when the sim is registered.
- Route-level axe coverage: record the latest shell or container-specific result in the preserved review section below.
- Media fallback: `media/fallback.svg` present.

## Tests

- Container validation: `pnpm container:validate shared/content/systems/containers/pid-bode-builder`
- Docs regeneration: `pnpm container:docs shared/content/systems/containers/pid-bode-builder`
- Prediction-checkpoint test: `simulation/simulation.test.ts`
- Package or shell tests: record exact commands in the preserved validation section below.

## How to run locally

```bash
pnpm container:docs shared/content/systems/containers/pid-bode-builder
pnpm container:validate shared/content/systems/containers/pid-bode-builder
pnpm graph:generate
```

## Anieyrudh Filter pass

Date: 2026-05-22
Reviewers (subagents): container-auditor, sim-architect, pedagogy-reviewer

### P0 resolved

- Prediction checkpoint blocks response and Bode evidence before commit - resolved by `core/sim-runtime` and `core/prediction-gate`.
- Reusable PID, transfer-function, Bode, and step-response math comes from `core/control-systems`.

### P1 addressed or deferred

- Formula panel includes LaTeX source, legend, substitutions, units, and interpretation - addressed in the shared sim and container sources.

### P2 noted

- Future wrapper containers can map this shared core into SUTD-specific assessment sequences.

## Iteration log

- Regenerated README.md and TECHNICAL.md with `pnpm container:docs`.
