# Circuit & Phasor Lab · Technical Record

> **Auto-generated by `pnpm container:docs`. Updated by every product PR.**
> Source: `container.yaml`, sim source declarations, and preserved review sections.

## Architecture

- Container path: `shared/content/physics/containers/circuit-phasor-reasoning`
- Status: `reviewed`
- Concept card: `concept-card.md`
- Concept map: `concept-map/concept-map.yaml`
- Simulation spec: `simulation/simulation.yaml`
- Embed API: `embed/api.ts`
- Problem-solving algorithm: `problem-solving/algorithm.md`

## Imports

| Sim | Module | Symbols / role |
|---|---|---|
| circuit-phasor-lab | `core/circuits` | Declared in `simulation/simulation.yaml` |
| circuit-phasor-lab | `core/charting` | Declared in `simulation/simulation.yaml` |
| circuit-phasor-lab | `core/content-schema` | Declared in `simulation/simulation.yaml` |
| circuit-phasor-lab | `core/prediction-gate` | Declared in `simulation/simulation.yaml` |
| circuit-phasor-lab | `core/shared` | Declared in `simulation/simulation.yaml` |
| circuit-phasor-lab | `core/ui-sim` | Declared in `simulation/simulation.yaml` |

## SimulationSpec (frozen)

Full validated YAML for the declared sim in this container at the time of last docs regeneration.

```yaml
id: circuit-phasor-lab
title: Circuit Phasor Lab
interaction_type: other
kernel_deps:
  - core/circuits
  - core/charting
  - core/content-schema
  - core/prediction-gate
  - core/shared
  - core/ui-sim
predict:
  prompt: |
    A resistor and inductor are connected in series to a sinusoidal supply. Before comparing with the lab, what happens to the current phase compared with the voltage?
  commit_format:
    kind: multiple-choice
    options:
      - Current leads the voltage because every AC circuit is capacitive.
      - Current lags the voltage because the inductor adds positive reactance.
      - Current is exactly in phase because resistance is present.
      - Current becomes zero because the inductor blocks all AC.
  rationale_required: true
manipulate:
  controls:
    - id: supply-voltage
      label: Supply voltage
      kind: slider
      kernel_binding: state.sourceVoltageRmsVolts
      bounds:
        min: 2
        max: 24
        step: 1
    - id: frequency
      label: Frequency
      kind: slider
      kernel_binding: state.frequencyHertz
      bounds:
        min: 10
        max: 200
        step: 5
    - id: resistance
      label: Resistance
      kind: slider
      kernel_binding: state.resistanceOhms
      bounds:
        min: 5
        max: 120
        step: 5
    - id: inductance
      label: Inductance
      kind: slider
      kernel_binding: state.inductanceMilliHenrys
      bounds:
        min: 10
        max: 500
        step: 10
    - id: capacitance
      label: Capacitance
      kind: slider
      kernel_binding: state.capacitanceMicroFarads
      bounds:
        min: 10
        max: 500
        step: 10
observe:
  renderers:
    - id: circuit-phasor-lab
      module: "@paideia/shared-sims/circuit-phasor-reasoning"
      symbol: CircuitPhasorReasoningSim
      props_binding: state -> impedance phasor, waveform chart, formula panel, and phase interpretation
explain:
  prompt: Which term sets the sign of the net reactance, and how does that sign decide current lead or lag?
  socratic: true
  expected_misconceptions_surfaced:
    - Phasors are physical rotating wires
    - Resistance makes every AC circuit in phase
    - An inductor blocks all AC current
```

## Kernel extensions

Declared runtime kernel dependencies are listed above. Any additive or breaking core API change must be linked in the preserved review section below.

## Accessibility

- Prediction checkpoint contract: required and tested when the sim is registered.
- Route-level axe coverage: record the latest shell or container-specific result in the preserved review section below.
- Media fallback: `media/fallback.svg` present.

## Tests

- Container validation: `pnpm container:validate shared/content/physics/containers/circuit-phasor-reasoning`
- Docs regeneration: `pnpm container:docs shared/content/physics/containers/circuit-phasor-reasoning`
- Prediction-checkpoint test: `simulation/simulation.test.ts`
- Package or shell tests: record exact commands in the preserved validation section below.

## How to run locally

```bash
pnpm container:docs shared/content/physics/containers/circuit-phasor-reasoning
pnpm container:validate shared/content/physics/containers/circuit-phasor-reasoning
pnpm graph:generate
```

## Anieyrudh Filter pass

Date: 2026-05-20
Reviewers: local container-auditor, sim-architect, pedagogy-reviewer criteria

### P0 resolved
- Prediction checkpoint saves reflection while observation and formula content remain visible — verified by simulation test.
- AC impedance and phase math live in `core/circuits`, not inside the container.
- Container shape follows `docs/container-spec.md`.
- Misconception evidence exists as a dedicated artifact — addressed by `concept-map/misconceptions.md`.

### P1 addressed or deferred
- Formula panel includes LaTeX source, nearby legend, substituted values, units, and interpretation — addressed in simulation UI.
- Main manipulation changes visible phasor, waveform, and readout state — addressed in simulation UI and Playwright coverage.
- Embed host inputs are runtime-validated — addressed with Zod parsing and contract tests.
- Additive core API traceability exists — addressed by GitHub issue #110 for `core/circuits` `solveSeriesAcCircuit()`.
- Revealed-state axe coverage passes — `simulation/simulation.test.ts` filters for serious or critical violations and expects an empty list.
- Electrical quantities still use SI-named `number` fields because `core/shared` has no electrical unit brands yet — deferred to GitHub issue #111 before any broad `core!` type migration.

### P2 noted
- Future work may add arbitrary AC network support to `core/circuits`; this container intentionally uses series RLC only.

## Iteration log

- Regenerated README.md and TECHNICAL.md with `pnpm container:docs`.
