# Alternating Current · Technical Record

> **Auto-generated by `pnpm container:docs`. Updated by every product PR.**
> Source: `container.yaml`, sim source declarations, and preserved review sections.

## Architecture

- Container path: `a-level/content/physics/containers/alternating-current`
- Status: `reviewed`
- Concept card: `concept-card.md`
- Concept map: `concept-map/concept-map.yaml`
- Simulation spec: `simulation/simulation.yaml`
- Embed API: `embed/api.ts`
- Problem-solving algorithm: `problem-solving/algorithm.md`

## Imports

| Sim | Module | Symbols / role |
|---|---|---|
| ac-rms-phase-lab | `core/sim-runtime` | Declared in `simulation/simulation.yaml` |
| ac-rms-phase-lab | `core/content-schema` | Declared in `simulation/simulation.yaml` |
| ac-rms-phase-lab | `core/circuits` | Declared in `simulation/simulation.yaml` |
| ac-rms-phase-lab | `core/charting` | Declared in `simulation/simulation.yaml` |
| ac-rms-phase-lab | `core/prediction-gate` | Declared in `simulation/simulation.yaml` |
| ac-rms-phase-lab | `core/shared` | Declared in `simulation/simulation.yaml` |
| ac-rms-phase-lab | `core/ui-sim` | Declared in `simulation/simulation.yaml` |

## SimulationSpec (frozen)

Full validated YAML for the declared sim in this container at the time of last docs regeneration.

```yaml
id: ac-rms-phase-lab
title: AC RMS and Phase Lab
interaction_type: diagram-builder
kernel_deps:
  - core/sim-runtime
  - core/content-schema
  - core/circuits
  - core/charting
  - core/prediction-gate
  - core/shared
  - core/ui-sim
predict:
  prompt: |
    A sinusoidal supply has the same peak voltage but its frequency is doubled. Before comparing with the lab, what happens to the rms voltage of the supply?
  commit_format:
    kind: multiple-choice
    options:
      - The rms voltage doubles because the wave oscillates twice as often.
      - The rms voltage stays the same because rms depends on amplitude, not frequency.
      - The rms voltage halves because each cycle takes less time.
      - The rms voltage becomes zero because positive and negative halves cancel.
    correct_index: 1
  rationale_required: true
manipulate:
  controls:
    - id: source-voltage
      label: Supply rms voltage
      kind: slider
      kernel_binding: state.sourceVoltageRmsVolts
      bounds:
        min: 4
        max: 24
        step: 1
    - id: frequency
      label: Frequency
      kind: slider
      kernel_binding: state.frequencyHertz
      bounds:
        min: 20
        max: 200
        step: 5
    - id: resistance
      label: Resistance
      kind: slider
      kernel_binding: state.resistanceOhms
      bounds:
        min: 10
        max: 120
        step: 5
    - id: inductance
      label: Inductance
      kind: slider
      kernel_binding: state.inductanceMilliHenrys
      bounds:
        min: 10
        max: 500
        step: 10
    - id: capacitance
      label: Capacitance
      kind: slider
      kernel_binding: state.capacitanceMicroFarads
      bounds:
        min: 20
        max: 500
        step: 10
    - id: sample-time
      label: Time marker
      kind: slider
      kernel_binding: state.sampleTimeMilliseconds
      bounds:
        min: 0
        max: 40
        step: 1
observe:
  renderers:
    - id: ac-rms-phase-lab
      module: "@paideia/a-level-physics-sims/alternating-current"
      symbol: AlternatingCurrentSim
      props_binding: |
        Show rms-to-peak conversion, series RLC impedance, current phase, power factor, formula substitution, units, interpretation, and symbol legend.
explain:
  prompt: |
    Which quantities are set by waveform amplitude, which are set by impedance, and how does phase decide whether current leads or lags voltage?
  socratic: true
  expected_misconceptions_surfaced:
    - RMS is an arithmetic average voltage
    - Higher frequency always means higher rms value
    - Resistance alone sets current in every AC circuit
    - Leading and lagging phase are interchangeable
```

## Kernel extensions

Declared runtime kernel dependencies are listed above. Any additive or breaking core API change must be linked in the preserved review section below.

## Accessibility

- Prediction checkpoint contract: required and tested when the sim is registered.
- Route-level axe coverage: record the latest shell or container-specific result in the preserved review section below.
- Media fallback: `media/fallback.svg` present.

## Tests

- Container validation: `pnpm container:validate a-level/content/physics/containers/alternating-current`
- Docs regeneration: `pnpm container:docs a-level/content/physics/containers/alternating-current`
- Prediction-checkpoint test: `simulation/simulation.test.ts`
- Package or shell tests: record exact commands in the preserved validation section below.

## How to run locally

```bash
pnpm container:docs a-level/content/physics/containers/alternating-current
pnpm container:validate a-level/content/physics/containers/alternating-current
pnpm graph:generate
```

## Anieyrudh Filter pass

Date: 2026-05-21
Reviewers (subagents): container-auditor, sim-architect, pedagogy-reviewer

### P0 issues

- Prediction checkpoint could leak impedance, phase, and power readouts before commit - resolved by keeping the observe surface inside `SimRuntime` / `PredictionGate` and asserting absence in jsdom and sim-harness tests.
- AC calculations could drift from the reviewed circuit kernel - resolved by computing impedance, current phase, current rms, and power terms through `core/circuits.solveSeriesAcCircuit`.
- Student-facing calculations could omit formula, substitution, units, or interpretation - resolved by showing a formula panel with substitutions and a nearby symbol legend for rms, reactance, impedance, phase, waveform sample, and power.

### P1 issues

- Rendered waveform QA needs browser evidence after generated registry update - addressed with `simulation/simulation.test.ts`, including a revealed-state axe scan.
- A future shared AC phasor renderer could reduce repeated SVG logic across shared and A-Level sims - deferred because this PR owns one A-Level container and does not change core renderer contracts.

### High-bandwidth questions surfaced

- Should the course shell expose a compact phasor legend outside individual sims, or keep AC symbol legends local until more electricity containers land?

## Iteration log

Brief log of what the agent did and what was rejected during authoring. This
is the "What AI did / what I rejected" record from the PR template, persisted.

- 2026-05-21: Built the A-Level alternating-current container, implemented the package sim, and kept reusable impedance math in `core/circuits`.
- 2026-05-21: Rejected a custom AC solver in the container; the sim composes kernel output into student-facing waveform, phasor, formula, and transfer surfaces.
