# Waves · Technical Record

> **Auto-generated by `pnpm container:docs`. Updated by every product PR.**
> Source: `container.yaml`, sim source declarations, and preserved review sections.

## Architecture

- Container path: `a-level/content/physics/containers/waves`
- Status: `reviewed`
- Concept card: `concept-card.md`
- Concept map: `concept-map/concept-map.yaml`
- Simulation spec: `simulation/simulation.yaml`
- Embed API: `embed/api.ts`
- Problem-solving algorithm: `problem-solving/algorithm.md`

## Imports

| Sim | Module | Symbols / role |
|---|---|---|
| wave-superposition-lab | `core/sim-runtime` | Declared in `simulation/simulation.yaml` |
| wave-superposition-lab | `core/content-schema` | Declared in `simulation/simulation.yaml` |
| wave-superposition-lab | `core/function-eval` | Declared in `simulation/simulation.yaml` |
| wave-superposition-lab | `core/charting` | Declared in `simulation/simulation.yaml` |
| wave-superposition-lab | `core/prediction-gate` | Declared in `simulation/simulation.yaml` |
| wave-superposition-lab | `core/shared` | Declared in `simulation/simulation.yaml` |
| wave-superposition-lab | `core/ui-sim` | Declared in `simulation/simulation.yaml` |

## SimulationSpec (frozen)

Full validated YAML for the declared sim in this container at the time of last docs regeneration.

```yaml
id: wave-superposition-lab
title: Wave Superposition Lab
interaction_type: animation-playback
kernel_deps:
  - core/sim-runtime
  - core/content-schema
  - core/function-eval
  - core/charting
  - core/prediction-gate
  - core/shared
  - core/ui-sim
predict:
  prompt: |
    Two identical waves meet in phase. Before comparing with the lab, what happens to the displacement where their crests overlap?
  commit_format:
    kind: multiple-choice
    options:
      - They cancel to zero
      - They add to double the displacement
      - The wavelength doubles
      - The frequency halves
    correct_index: 1
  rationale_required: true
manipulate:
  controls:
    - id: amplitude
      label: Amplitude
      kind: slider
      kernel_binding: state.amplitudeMetres
      bounds:
        min: 0.2
        max: 3
        step: 0.1
    - id: wavelength
      label: Wavelength
      kind: slider
      kernel_binding: state.wavelengthMetres
      bounds:
        min: 1
        max: 8
        step: 0.25
    - id: period
      label: Period
      kind: slider
      kernel_binding: state.periodSeconds
      bounds:
        min: 0.5
        max: 6
        step: 0.25
    - id: phase
      label: Phase difference
      kind: slider
      kernel_binding: state.phaseDegrees
      bounds:
        min: 0
        max: 180
        step: 15
observe:
  renderers:
    - id: wave-superposition-lab
      module: "@paideia/a-level-physics-sims/waves"
      symbol: WavesSim
      props_binding: |
        Show two same-frequency waves, their resultant displacement, phase comparison, formula substitution, and interference condition.
explain:
  prompt: |
    Why do identical waves reinforce when their displacements point the same way, and cancel when they point opposite ways?
  socratic: true
  expected_misconceptions_surfaced:
    - Amplitude and wavelength are interchangeable
    - Constructive interference changes the frequency
    - Destructive interference destroys energy
```

## Kernel extensions

No core-change proposal is declared in this container. Kernel use is limited to declared dependencies above.

## Accessibility

- Prediction checkpoint contract: required and tested when the sim is registered.
- Route-level axe coverage: record the latest shell or container-specific result in the preserved review section below.
- Media fallback: `media/fallback.svg` present.

## Tests

- Container validation: `pnpm container:validate a-level/content/physics/containers/waves`
- Docs regeneration: `pnpm container:docs a-level/content/physics/containers/waves`
- Prediction-checkpoint test: `simulation/simulation.test.ts`
- Package or shell tests: record exact commands in the preserved validation section below.

## How to run locally

```bash
pnpm container:docs a-level/content/physics/containers/waves
pnpm container:validate a-level/content/physics/containers/waves
pnpm graph:generate
```

## Latest Validation

Local validation on 2026-05-19:

- `pnpm install --no-frozen-lockfile` - passed after sandbox DNS failed without network access.
- `pnpm -F @paideia/a-level-physics-sims build` - passed.
- `pnpm -F @paideia/a-level-physics-sims test` - passed, 25 tests.
- `pnpm container:validate a-level/content/physics/containers/waves` - passed.
- `pnpm graph:generate` - passed.
- `pnpm typecheck` - passed.
- `pnpm lint` - passed.
- `pnpm -F @paideia/sim-harness test` - passed, 26 browser tests.
- `pnpm -F @paideia/a-level-shell test` - passed, 12 browser tests.
- Container-level axe coverage: `a-level/content/physics/containers/waves/simulation/simulation.test.ts` runs Axe after reveal; latest focused sim-harness run passed with 27 browser tests and no critical accessibility violations for this sim.
- `pnpm boundary` - passed.
- `pnpm license:check` - passed.
- `pnpm roadmap:validate` - passed.
- `pnpm agent:validate` - passed.
- `pnpm test` - passed across core, A-Level, SUTD shell, and sim harness suites.
- `pnpm graph:check` - expected to pass after this branch commits generated graph files; before commit it correctly reports the generated catalogue diff as uncommitted.

## Anieyrudh Filter pass

Date: 2026-05-19

### P0 resolved
- Prediction-before-reveal could be bypassed if the observation panel mounted directly. Resolution: mounted the reveal stage through `@paideia/sim-runtime`, which wraps observe/explain in `@paideia/prediction-gate`, and added package plus browser contract tests.
- Reusable physics could drift into the container. Resolution: work and kinetic energy calculations call `core/mechanics`; chart rendering uses `core/charting`; controls use `core/ui-sim`.

### P1 addressed or deferred
- Student UI could become formula-only. Addressed with a force/displacement diagram, energy-store bar, presets, and formula substitution with units.
- Braking work can mathematically exceed the starting kinetic store. Addressed by clamping the displayed kinetic store at zero and naming the transfer out of the store.

### High-bandwidth questions surfaced
- Should a future core/mechanics function model stopping distance explicitly when negative work exceeds the kinetic store?
- Should A-Level product sims standardize on `core/sim-runtime`, or keep direct `PredictionGate` wrappers for simpler flows?

## Iteration log

- Regenerated README.md and TECHNICAL.md with `pnpm container:docs`.
