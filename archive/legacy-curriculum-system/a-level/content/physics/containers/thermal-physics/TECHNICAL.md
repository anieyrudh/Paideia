# Thermal Physics · Technical Record

> **Auto-generated by `pnpm container:docs`. Updated by every product PR.**
> Source: `container.yaml`, sim source declarations, and preserved review sections.

## Architecture

- Container path: `a-level/content/physics/containers/thermal-physics`
- Status: `reviewed`
- Concept card: `concept-card.md`
- Concept map: `concept-map/concept-map.yaml`
- Simulation spec: `simulation/simulation.yaml`
- Embed API: `embed/api.ts`
- Problem-solving algorithm: `problem-solving/algorithm.md`

## Imports

| Sim | Module | Symbols / role |
|---|---|---|
| gas-law-energy-transfer-lab | `core/sim-runtime` | Declared in `simulation/simulation.yaml` |
| gas-law-energy-transfer-lab | `core/content-schema` | Declared in `simulation/simulation.yaml` |
| gas-law-energy-transfer-lab | `core/numerical-math` | Declared in `simulation/simulation.yaml` |
| gas-law-energy-transfer-lab | `core/charting` | Declared in `simulation/simulation.yaml` |
| gas-law-energy-transfer-lab | `core/prediction-gate` | Declared in `simulation/simulation.yaml` |
| gas-law-energy-transfer-lab | `core/shared` | Declared in `simulation/simulation.yaml` |
| gas-law-energy-transfer-lab | `core/ui-sim` | Declared in `simulation/simulation.yaml` |

## SimulationSpec (frozen)

Full validated YAML for the declared sim in this container at the time of last docs regeneration.

```yaml
id: gas-law-energy-transfer-lab
title: Gas Law and Energy Transfer Lab
interaction_type: animation-playback
kernel_deps:
  - core/sim-runtime
  - core/content-schema
  - core/numerical-math
  - core/charting
  - core/prediction-gate
  - core/shared
  - core/ui-sim
predict:
  prompt: |
    A 0.040 mol gas sample occupies 1.0 L at 27 deg C. Before comparing with the lab, which pressure is closest when the ideal-gas law is used correctly?
  commit_format:
    kind: multiple-choice
    options:
      - 9.0 kPa
      - 100 kPa
      - 270 kPa
      - 830 kPa
    correct_index: 1
  rationale_required: true
manipulate:
  controls:
    - id: volume
      label: Gas volume
      kind: slider
      kernel_binding: state.volumeLitres
      bounds:
        min: 0.5
        max: 5
        step: 0.1
    - id: gas-temperature
      label: Gas temperature
      kind: slider
      kernel_binding: state.gasTemperatureCelsius
      bounds:
        min: -20
        max: 120
        step: 1
    - id: amount
      label: Amount of gas
      kind: slider
      kernel_binding: state.amountMoles
      bounds:
        min: 0.01
        max: 0.12
        step: 0.005
    - id: heated-mass
      label: Mass heated
      kind: slider
      kernel_binding: state.heatingMassKilograms
      bounds:
        min: 0.05
        max: 1.2
        step: 0.05
    - id: initial-temperature
      label: Initial temperature
      kind: slider
      kernel_binding: state.initialTemperatureCelsius
      bounds:
        min: 0
        max: 80
        step: 1
    - id: final-temperature
      label: Final temperature
      kind: slider
      kernel_binding: state.finalTemperatureCelsius
      bounds:
        min: 0
        max: 100
        step: 1
observe:
  renderers:
    - id: gas-law-energy-transfer-lab
      module: "@paideia/a-level-physics-sims/thermal-physics"
      symbol: ThermalPhysicsSim
      props_binding: |
        Show Kelvin conversion, ideal-gas pressure, Celsius-substitution trap, heat-transfer calculation, formula substitution, and pressure against inverse-volume trend.
explain:
  prompt: |
    Why must gas-law temperature be in kelvin, and why can two samples at the same temperature require different energy transfers?
  socratic: true
  expected_misconceptions_surfaced:
    - Temperature is the same as thermal energy
    - Celsius can be substituted directly into gas laws
    - Increasing volume always increases gas pressure
```

## Kernel extensions

Declared runtime kernel dependencies are listed above. Any additive or breaking core API change must be linked in the preserved review section below.

## Accessibility

- Prediction checkpoint contract: required and tested when the sim is registered.
- Route-level axe coverage: record the latest shell or container-specific result in the preserved review section below.
- Media fallback: `media/fallback.svg` present.

## Tests

- Container validation: `pnpm container:validate a-level/content/physics/containers/thermal-physics`
- Docs regeneration: `pnpm container:docs a-level/content/physics/containers/thermal-physics`
- Prediction-checkpoint test: `simulation/simulation.test.ts`
- Package or shell tests: record exact commands in the preserved validation section below.

## How to run locally

```bash
pnpm container:docs a-level/content/physics/containers/thermal-physics
pnpm container:validate a-level/content/physics/containers/thermal-physics
pnpm graph:generate
```

## Latest Validation

Local validation on 2026-05-21:

- `pnpm install` - failed in sandbox with `getaddrinfo ENOTFOUND registry.npmjs.org`; escalated rerun passed.
- `pnpm container:validate a-level/content/physics/containers/thermal-physics` - passed.
- `pnpm container:docs a-level/content/physics/containers/thermal-physics` - passed.
- `pnpm container:docs a-level/content/physics/containers/thermal-physics --check` - passed.
- `pnpm graph:generate` - passed.
- `pnpm graph:check` - passed.
- `pnpm -F @paideia/a-level-physics-sims test` - passed, 51 tests.
- `pnpm -F @paideia/a-level-physics-sims typecheck` - passed.
- `pnpm -F @paideia/a-level-shell typecheck` - passed.
- `CI=1 pnpm -F @paideia/a-level-shell test -- tests/shell.spec.ts -g "keeps the active thermal"` - blocked in sandbox with `listen EPERM: operation not permitted 127.0.0.1:4174`.
- `pnpm typecheck` - passed.
- `pnpm lint` - passed.
- `CI=1 pnpm -F @paideia/sim-harness test -- a-level/content/physics/containers/thermal-physics/simulation/simulation.test.ts` - passed, 3 browser tests.
- `pnpm boundary` - passed.
- `pnpm license:check` - passed.
- `pnpm roadmap:validate` - passed.
- `pnpm agent:validate` - passed.
- `pnpm test` - partial pass; all Vitest package suites completed, then sim-harness webServer failed in sandbox with `listen EPERM: operation not permitted 127.0.0.1:4173`. The focused thermal sim-harness run above passed when run with `CI=1` and local-server approval.
- `pnpm container:docs --check` - failed on pre-existing stale docs across other containers; the focused thermal docs check passed.
- Subagent review attempt - blocked by environment usage limit before agents could run; manual review recorded below.

## Anieyrudh Filter pass

Date: 2026-05-21
Reviewers: manual Codex review. Requested subagents were unavailable because the environment reported a usage-limit error before they could run.

### P0 resolved
- Prediction-before-reveal could be bypassed if pressure and heat readouts mounted directly. Resolution: mounted the reveal stage through `@paideia/sim-runtime`, which wraps observe/explain in `@paideia/prediction-gate`, and added package plus browser contract tests.
- Reusable charting or control logic could drift into the container. Resolution: chart rendering uses `core/charting`, inverse-volume trend fitting uses `core/numerical-math`, controls use `core/ui-sim`, and stage gating uses `core/sim-runtime`.

### P1 addressed or deferred
- Student UI could confuse temperature with energy. Addressed with separate gas-law and heat-transfer substitutions, a color-coded legend, units, and a plain-language interpretation.
- Celsius substitution could be hidden as a minor arithmetic issue. Addressed by surfacing the incorrect Celsius pressure beside the kelvin result after prediction commit.

### P2 noted
- A future `core/thermal-physics` kernel could centralize ideal-gas and heat-transfer formulas once more containers need the same reusable physics.

## Iteration log

- Regenerated README.md and TECHNICAL.md with `pnpm container:docs`.
