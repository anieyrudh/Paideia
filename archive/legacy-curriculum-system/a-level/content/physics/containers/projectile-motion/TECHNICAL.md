# Projectile Motion · Technical Record

> **Auto-generated by `pnpm container:docs`. Updated by every product PR.**
> Source: `container.yaml`, sim source declarations, and preserved review sections.

## Architecture

- Container path: `a-level/content/physics/containers/projectile-motion`
- Status: `reviewed`
- Concept card: `concept-card.md`
- Concept map: `concept-map/concept-map.yaml`
- Simulation spec: `simulation/simulation.yaml`
- Embed API: `embed/api.ts`
- Problem-solving algorithm: `problem-solving/algorithm.md`

## Imports

| Sim | Module | Symbols / role |
|---|---|---|
| trajectory-parameter-lab | `core/content-schema` | Declared in `simulation/simulation.yaml` |
| trajectory-parameter-lab | `core/mechanics` | Declared in `simulation/simulation.yaml` |
| trajectory-parameter-lab | `core/plotting` | Declared in `simulation/simulation.yaml` |
| trajectory-parameter-lab | `core/prediction-gate` | Declared in `simulation/simulation.yaml` |
| trajectory-parameter-lab | `core/shared` | Declared in `simulation/simulation.yaml` |
| trajectory-parameter-lab | `core/ui-sim` | Declared in `simulation/simulation.yaml` |

## SimulationSpec (frozen)

Full validated YAML for the declared sim in this container at the time of last docs regeneration.

```yaml
id: trajectory-parameter-lab
title: Trajectory Parameter Lab
interaction_type: function-plot-with-draggable
kernel_deps:
  - core/content-schema
  - core/mechanics
  - core/plotting
  - core/prediction-gate
  - core/shared
  - core/ui-sim
predict:
  prompt: |
    A ball is launched horizontally from a bench. Before comparing with the path, what happens to its horizontal velocity while it falls?
  commit_format:
    kind: multiple-choice
    options:
      - It stays constant if air resistance is ignored.
      - It decreases because gravity pulls downward.
      - It increases because the ball is speeding up overall.
      - It becomes zero at the highest point.
    correct_index: 0
  rationale_required: true
manipulate:
  controls:
    - id: launch-speed
      label: Launch speed
      kind: slider
      kernel_binding: state.launchSpeedMetresPerSecond
      bounds:
        min: 4
        max: 30
        step: 0.5
    - id: launch-angle
      label: Launch angle
      kind: slider
      kernel_binding: state.launchAngleDegrees
      bounds:
        min: 0
        max: 70
        step: 1
    - id: launch-height
      label: Launch height
      kind: slider
      kernel_binding: state.launchHeightMetres
      bounds:
        min: 0
        max: 20
        step: 0.5
observe:
  renderers:
    - id: trajectory-parameter-lab
      module: "@paideia/a-level-physics-sims/projectile-motion"
      symbol: ProjectileMotionSim
      props_binding: |
        Show the sampled projectile arc, independent horizontal and vertical motion, range, flight time, peak height, and formula substitution from the current launch state.
explain:
  prompt: |
    Which part of the projectile motion comes from the initial velocity, and which part comes from gravity?
  socratic: true
  expected_misconceptions_surfaced:
    - Horizontal and vertical motion affect each other directly
    - A projectile needs a continuing forward force
    - Launch angle changes gravitational acceleration
```

## Kernel extensions

Declared runtime kernel dependencies are listed above. Any additive or breaking core API change must be linked in the preserved review section below.

## Accessibility

- Prediction checkpoint contract: required and tested when the sim is registered.
- Route-level axe coverage: record the latest shell or container-specific result in the preserved review section below.
- Media fallback: `media/fallback.svg` present.

## Tests

- Container validation: `pnpm container:validate a-level/content/physics/containers/projectile-motion`
- Docs regeneration: `pnpm container:docs a-level/content/physics/containers/projectile-motion`
- Prediction-checkpoint test: `simulation/simulation.test.ts`
- Package or shell tests: record exact commands in the preserved validation section below.

## How to run locally

```bash
pnpm container:docs a-level/content/physics/containers/projectile-motion
pnpm container:validate a-level/content/physics/containers/projectile-motion
pnpm graph:generate
```

## Anieyrudh Filter pass

Date: 2026-05-28

### P0 issues

- None open. The container has the required v2 surfaces, prediction checkpoint,
  visual revealed state, cited sources, and no copied or incompatible runtime
  assets.

### P1 issues

- None open. Reusable projectile sampling is not inlined in the container; the
  sim composes `core/mechanics`. Formula readouts include legend,
  substitution, units, result, and interpretation. Controls are labelled and
  use the shared simulation control kit.

### P2 follow-ups

- Add a second transfer problem for a package dropped from a horizontally
  moving vehicle.
- Add a slower-motion animation scrubber after the static trajectory model has
  classroom feedback.

## Iteration log

- Regenerated README.md and TECHNICAL.md with `pnpm container:docs`.
