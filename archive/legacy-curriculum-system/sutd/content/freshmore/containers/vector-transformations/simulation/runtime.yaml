runtime:
  entry: simulation/index.tsx
  mount_mode: react-component
  state_model: local
  persistence: local
  deterministic: true
visual_quality:
  setup:
    - role: button
      name: "Set up transformation"
    - role: button
      name: "Reveal transformed vector"
  prediction:
    option_label: "x = 3"
    rationale: "The first coordinate is 2(1) + 1(1) = 3."
  reveal:
    observation_label: "Observation unlocked"
    visual: required
    formula: required
