# Determinant and Trace · Technical Record

> **Auto-generated by `pnpm container:docs`. Updated by every product PR.**
> Source: `container.yaml`, sim source declarations, and preserved review sections.

## Architecture

- Container path: `sutd/content/10-018-modelling-space-and-systems-multivariable-calc-and-linear-algebra/containers/determinant-and-trace`
- Status: `draft`
- Concept card: `concept-card.md`
- Concept map: `concept-map/concept-map.yaml`
- Simulation spec: `simulation/simulation.yaml`
- Embed API: `embed/api.ts`
- Problem-solving algorithm: `problem-solving/algorithm.md`

## Imports

| Sim | Module | Symbols / role |
|---|---|---|
| determinant-and-trace | `core/sim-runtime` | Declared in `simulation/simulation.yaml` |
| determinant-and-trace | `core/linear-algebra` | Declared in `simulation/simulation.yaml` |
| determinant-and-trace | `core/prediction-gate` | Declared in `simulation/simulation.yaml` |
| determinant-and-trace | `core/ui-sim` | Declared in `simulation/simulation.yaml` |

## SimulationSpec (frozen)

Full validated YAML for the declared sim in this container at the time of last docs regeneration.

```yaml
id: determinant-and-trace
title: Determinant and Trace Lab
interaction_type: diagram-builder
kernel_deps:
  - core/sim-runtime
  - core/linear-algebra
  - core/prediction-gate
  - core/ui-sim
predict:
  prompt: |
    Matrix A = [[3, 1], [0, 2]]. Before reveal, which statement best describes its determinant and trace?
  commit_format:
    kind: multiple-choice
    options:
      - det A = 6 and tr A = 5, so area is scaled by 6 and the eigenvalues sum to 5
      - det A = 5 and tr A = 6, so area is scaled by 5 and the eigenvalues sum to 6
      - det A = 1 and tr A = 0, because the off-diagonal entries cancel
      - det A = 0 and tr A = 5, because the lower-left zero collapses the matrix
    correct_index: 0
  rationale_required: true
manipulate:
  controls:
    - id: a11
      label: Top-left entry a
      kind: slider
      kernel_binding: state.a11
      bounds:
        min: -4
        max: 4
        step: 0.5
    - id: a12
      label: Top-right entry b
      kind: slider
      kernel_binding: state.a12
      bounds:
        min: -4
        max: 4
        step: 0.5
    - id: a21
      label: Bottom-left entry c
      kind: slider
      kernel_binding: state.a21
      bounds:
        min: -4
        max: 4
        step: 0.5
    - id: a22
      label: Bottom-right entry d
      kind: slider
      kernel_binding: state.a22
      bounds:
        min: -4
        max: 4
        step: 0.5
observe:
  renderers:
    - id: determinant-and-trace-readout
      module: "@paideia/sutd-sims/determinant-and-trace"
      symbol: DeterminantAndTrace
      props_binding: |
        Show the transformed unit-square parallelogram, the signed area, trace and determinant values, eigenvalue sum/product, and the discriminant verdict.
explain:
  prompt: |
    Explain how trace and determinant determine the eigenvalues without solving the characteristic polynomial in full.
  socratic: true
  expected_misconceptions_surfaced:
    - Determinant is unsigned area
    - Trace equals determinant
```

## Kernel extensions

Declared runtime kernel dependencies are listed above. Any additive or breaking core API change must be linked in the preserved review section below.

## Accessibility

- Prediction checkpoint contract: required and tested when the sim is registered.
- Route-level axe coverage: record the latest shell or container-specific result in the preserved review section below.
- Media fallback: `media/fallback.svg` present.

## Tests

- Container validation: `pnpm container:validate sutd/content/10-018-modelling-space-and-systems-multivariable-calc-and-linear-algebra/containers/determinant-and-trace`
- Docs regeneration: `pnpm container:docs sutd/content/10-018-modelling-space-and-systems-multivariable-calc-and-linear-algebra/containers/determinant-and-trace`
- Prediction-checkpoint test: `simulation/simulation.test.ts`
- Package or shell tests: record exact commands in the preserved validation section below.

## How to run locally

```bash
pnpm container:docs sutd/content/10-018-modelling-space-and-systems-multivariable-calc-and-linear-algebra/containers/determinant-and-trace
pnpm container:validate sutd/content/10-018-modelling-space-and-systems-multivariable-calc-and-linear-algebra/containers/determinant-and-trace
pnpm graph:generate
```

## Anieyrudh Filter pass

Date: 2026-05-26
Filter version: aniegpt v1.0 (builder self-audit, awaiting reviewer pass)

### P0 issues

- None observed. Container shape validates against `ContainerSpec`. Prediction checkpoint is declared in `simulation.yaml`, asserted in `simulation/simulation.test.ts`, and gated in the React reveal path via `SimRuntime` + the `predict` spec. The reveal shows an interactive SVG of the transformed unit square plus a formula card with substitution, units, and an interpretation line. The sim consumes `core/linear-algebra` (`matrix2`, `determinant2`, `trace2`) instead of inlining matrix math. No GPL or proprietary deps were introduced.

### P1 issues

- Formula colours pair `det A` (blue), `tr A` (green), `eigenvalue sum` (orange), and `eigenvalue product` (red); the legend uses the same `.legend-swatch` classes already styled in the SUTD shell. Manipulation visibly changes the parallelogram fill colour when `det A` flips sign, providing direct-manipulation feedback. No P1 outstanding.

### P2 issues (deferred)

- Add an in-sim discriminant strip on the trace-determinant plane to show where the current matrix sits relative to the parabola `T^2 = 4D` (carries over from the analogous treatment in `sutd.freshmore.eigenvector-transformations`).
- Add a notebook-lab follow-up that lets learners enumerate a few canonical matrices (rotation, reflection, shear, scaling) and compare their `(T, D)` coordinates side by side.
- Add a transfer problem on the singular case `det A = 0` showing how the parallelogram collapses to a line segment.

### High-bandwidth questions surfaced

- Should the new `sutd/content/10-018-…/` taxonomy ratify a roadmap entry for every Batch C ID so the existing `container-build-queue.yaml` becomes the single source of truth alongside the codex-goals batch runbook?

## Iteration log

- 2026-05-26: Scaffolded the container by deriving the path from the codex-goals batch runbook’s rule. Imported the `core/linear-algebra` kernel (`matrix2`, `determinant2`, `trace2`) instead of inlining matrix math. Added SVG-based unit-square parallelogram visualisation that recolours on orientation flip. Added discriminant + trace-determinant stability classifier. Generated README.md and TECHNICAL.md via `pnpm container:docs`.

## Validation log (2026-05-26)

| Check | Result |
| --- | --- |
| `pnpm container:validate sutd/content/10-018-modelling-space-and-systems-multivariable-calc-and-linear-algebra/containers/determinant-and-trace` | passed (54 containers OK) |
| `pnpm container:docs sutd/content/10-018-modelling-space-and-systems-multivariable-calc-and-linear-algebra/containers/determinant-and-trace` | regenerated README.md and TECHNICAL.md |
| `pnpm graph:generate` | wrote SUTD shell knowledge-graph with new container entry |
| `pnpm graph:check` | clean (no drift) |
| `pnpm typecheck` | clean across all workspaces |
| `pnpm lint` | clean across all workspaces |
| `pnpm boundary` | 1208 modules, 2020 dependencies, no violations |
| `pnpm license:check` | 84 production deps, all compatible |
| `pnpm roadmap:validate` | 54 queue entries OK |
| `pnpm agent:validate` | OK |
| `pnpm test` | vitest suites green across `core/*`, `sutd/packages/*`, and embed contract; Playwright suites in `sutd/apps/shell` and `testing/sim-harness` fail at browser launch (`chrome-headless-shell-linux64/chrome-headless-shell` not present for the installed Playwright version) — this is an environment failure unrelated to this PR. |

