# Partial Derivatives and Gradient · Technical Record

> **Auto-generated by `pnpm container:docs`. Updated by every product PR.**
> Source: `container.yaml`, sim source declarations, and preserved review sections.

## Architecture

- Container path: `sutd/content/10-018-modelling-space-and-systems-multivariable-calc-and-linear-algebra/containers/partial-derivatives-and-gradient`
- Status: `reviewed`
- Concept card: `concept-card.md`
- Concept map: `concept-map/concept-map.yaml`
- Simulation spec: `simulation/simulation.yaml`
- Embed API: `embed/api.ts`
- Problem-solving algorithm: `problem-solving/algorithm.md`

## Imports

| Sim | Module | Symbols / role |
|---|---|---|
| partial-derivatives-and-gradient | `core/sim-runtime` | Declared in `simulation/simulation.yaml` |
| partial-derivatives-and-gradient | `core/vector-calculus` | Declared in `simulation/simulation.yaml` |
| partial-derivatives-and-gradient | `core/plotting` | Declared in `simulation/simulation.yaml` |
| partial-derivatives-and-gradient | `core/prediction-gate` | Declared in `simulation/simulation.yaml` |
| partial-derivatives-and-gradient | `core/ui-sim` | Declared in `simulation/simulation.yaml` |

## SimulationSpec (frozen)

Full validated YAML for the declared sim in this container at the time of last docs regeneration.

```yaml
id: partial-derivatives-and-gradient
title: Partial Derivatives and Gradient Lab
interaction_type: diagram-builder
kernel_deps:
  - core/sim-runtime
  - core/vector-calculus
  - core/plotting
  - core/prediction-gate
  - core/ui-sim
predict:
  prompt: |
    On a contour plot, which direction does the gradient vector point at the selected point?
  commit_format:
    kind: multiple-choice
    options:
      - Perpendicular to the contour, toward fastest increase
      - Along the contour, because the contour is the visible line
      - Always toward the positive x-axis
      - Always toward the origin
    correct_index: 0
  rationale_required: true
manipulate:
  controls:
    - id: surface-kind
      label: Surface
      kind: selector
      kernel_binding: state.surfaceKind
    - id: point-x
      label: Point x
      kind: slider
      kernel_binding: state.x
      bounds:
        min: -2
        max: 2
        step: 0.1
    - id: point-y
      label: Point y
      kind: slider
      kernel_binding: state.y
      bounds:
        min: -2
        max: 2
        step: 0.1
    - id: direction-degrees
      label: Test direction angle
      kind: slider
      kernel_binding: state.directionDegrees
      bounds:
        min: 0
        max: 360
        step: 5
observe:
  renderers:
    - id: partial-derivatives-gradient-renderer
      module: "@paideia/sutd-sims/partial-derivatives-and-gradient"
      symbol: PartialDerivativesAndGradient
      props_binding: |
        Show a contour map with selected point, partial derivative readouts, gradient vector, directional derivative projection, tangent-plane formula, symbol legend, substitutions, units, and interpretation.
explain:
  prompt: |
    Explain why the gradient is perpendicular to the level curve and how its components are the two partial derivatives.
  socratic: true
  expected_misconceptions_surfaced:
    - The gradient points along a contour
    - Partial derivatives describe total change in every direction
transfer:
  prompt: |
    A temperature surface is measured over a plate. Use the gradient at a sensor point to estimate the fastest local temperature increase and the change in a chosen walking direction.
  rubric_path: ../problem-solving/temperature-surface-gradient.md
```

## Kernel extensions

Declared runtime kernel dependencies are listed above. Any additive or breaking core API change must be linked in the preserved review section below.

## Accessibility

- Prediction checkpoint contract: required and tested when the sim is registered.
- Route-level axe coverage: record the latest shell or container-specific result in the preserved review section below.
- Media fallback: `media/fallback.svg` present.

## Tests

- Container validation: `pnpm container:validate sutd/content/10-018-modelling-space-and-systems-multivariable-calc-and-linear-algebra/containers/partial-derivatives-and-gradient`
- Docs regeneration: `pnpm container:docs sutd/content/10-018-modelling-space-and-systems-multivariable-calc-and-linear-algebra/containers/partial-derivatives-and-gradient`
- Prediction-checkpoint test: `simulation/simulation.test.ts`
- Package or shell tests: record exact commands in the preserved validation section below.

## How to run locally

```bash
pnpm container:docs sutd/content/10-018-modelling-space-and-systems-multivariable-calc-and-linear-algebra/containers/partial-derivatives-and-gradient
pnpm container:validate sutd/content/10-018-modelling-space-and-systems-multivariable-calc-and-linear-algebra/containers/partial-derivatives-and-gradient
pnpm graph:generate
```

## Anieyrudh Filter pass

- P0: none.
- P1: none.
- P2: no open issues.

## Performance notes

- The sim uses one finite-difference gradient kernel call per render and a static SVG contour diagram; it does not fetch network assets, run an animation loop, or use an iterative solver.

## Iteration log

- Regenerated README.md and TECHNICAL.md with `pnpm container:docs`.
