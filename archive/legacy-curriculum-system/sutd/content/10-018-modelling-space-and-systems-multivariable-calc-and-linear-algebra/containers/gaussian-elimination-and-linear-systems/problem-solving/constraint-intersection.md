# Constraint Intersection

Two design constraints form a 2 by 2 system. Row-reduce the augmented matrix. Two pivots mean the constraints intersect once. A zero row with a nonzero right-hand side means no feasible setting. A zero row with zero right-hand side means the constraints describe the same line.
