# Scheduling and Project Management · Technical Record

> **Auto-generated by `pnpm container:docs`. Updated by every product PR.**
> Source: `container.yaml`, sim source declarations, and preserved review sections.

## Architecture

- Container path: `sutd/content/40-012-manufacturing-and-service-operations-mso/containers/scheduling-and-project-management`
- Status: `reviewed`
- Concept card: `concept-card.md`
- Concept map: `concept-map/concept-map.yaml`
- Simulation spec: `simulation/simulation.yaml`
- Embed API: `embed/api.ts`
- Problem-solving algorithm: `problem-solving/algorithm.md`

## Imports

| Sim | Module | Symbols / role |
|---|---|---|
| schedule-critical-path-lab | `core/content-schema` | Declared in `simulation/simulation.yaml` |
| schedule-critical-path-lab | `core/shared` | Declared in `simulation/simulation.yaml` |
| schedule-critical-path-lab | `core/scheduling` | Declared in `simulation/simulation.yaml` |
| schedule-critical-path-lab | `core/algorithm-trace` | Declared in `simulation/simulation.yaml` |
| schedule-critical-path-lab | `core/prediction-gate` | Declared in `simulation/simulation.yaml` |
| schedule-critical-path-lab | `core/ui-sim` | Declared in `simulation/simulation.yaml` |

## SimulationSpec (frozen)

Full validated YAML for the declared sim in this container at the time of last docs regeneration.

```yaml
schema_version: 1.0.0
id: schedule-critical-path-lab
title: Schedule Critical Path Lab
module: "@paideia/sutd-sims/scheduling-and-project-management"
symbol: SchedulingAndProjectManagement
interaction_type: systems-flow-diagram
prediction_gate: true
kernel_deps:
  - core/content-schema
  - core/shared
  - core/scheduling
  - core/algorithm-trace
  - core/prediction-gate
  - core/ui-sim
manipulate:
  controls:
    - id: prototype-duration
      label: Prototype duration
      kind: slider
      kernel_binding: state.prototype
      bounds:
        min: 1
        max: 12
        step: 1
    - id: tooling-duration
      label: Tooling duration
      kind: slider
      kernel_binding: state.tooling
      bounds:
        min: 1
        max: 12
        step: 1
    - id: training-duration
      label: Training duration
      kind: slider
      kernel_binding: state.training
      bounds:
        min: 1
        max: 12
        step: 1
observe:
  renderers:
    - id: critical-path-readout
      module: "@paideia/sutd-sims/scheduling-and-project-management"
      symbol: SchedulingAndProjectManagement
      props_binding: Render CPM network, critical path, slack table, formula legend, substitution, units, and interpretation.
explain:
  prompt: Explain why an activity with positive slack can slip without moving launch.
  socratic: true
  expected_misconceptions_surfaced:
    - The longest activity always controls project duration.
    - Every activity delay changes the completion date.
```

## Kernel extensions

Declared runtime kernel dependencies are listed above. Any additive or breaking core API change must be linked in the preserved review section below.

## Accessibility

- Prediction checkpoint contract: required and tested when the sim is registered.
- Route-level axe coverage: record the latest shell or container-specific result in the preserved review section below.
- Media fallback: `media/fallback.svg` present.

## Tests

- Container validation: `pnpm container:validate sutd/content/40-012-manufacturing-and-service-operations-mso/containers/scheduling-and-project-management`
- Docs regeneration: `pnpm container:docs sutd/content/40-012-manufacturing-and-service-operations-mso/containers/scheduling-and-project-management`
- Prediction-checkpoint test: `simulation/simulation.test.ts`
- Package or shell tests: record exact commands in the preserved validation section below.

## How to run locally

```bash
pnpm container:docs sutd/content/40-012-manufacturing-and-service-operations-mso/containers/scheduling-and-project-management
pnpm container:validate sutd/content/40-012-manufacturing-and-service-operations-mso/containers/scheduling-and-project-management
pnpm graph:generate
```

## Anieyrudh Filter pass

Date: 2026-05-28
Filter version: aniegpt v1.0
Reviewers (local): container-auditor, sim-architect, pedagogy-reviewer

### P0 resolved

- Prediction checkpoint must block CPM evidence before commitment - resolved with package-level prediction and route-level Playwright coverage.
- Critical-path math must come from a reusable kernel - resolved by using `core/scheduling` for activity brands and CPM timing.
- The visual model must not be text-only - resolved with an activity-on-node SVG network that marks critical activities after reveal.

### P1 addressed or deferred

- Learner-facing calculations need formula, substitution, units, interpretation, and legend - addressed in the CPM formula panel and route tests.
- Student UI must avoid package names, file paths, kernel names, YAML fields, and queue IDs - addressed by keeping implementation references in docs/tests only, not visible simulation copy.

### P2 follow-ups

- Add a second transfer problem for a resource-constrained project where CPM is necessary but not sufficient for final staffing decisions.
- Add drag-to-edit precedence edges after a reusable graph editing control exists in `core/ui-sim`.

## Iteration log

- Scaffolded from a reviewed SUTD operations container, then replaced copied content with the MSO scheduling slice.
- Built a CPM simulation with prediction checkpoint, duration sliders, activity-on-node network, critical-path readout, slack interpretation, and transfer problem.
- Corrected the initial schema mismatch by mapping the descriptive queue simulation type to the allowed `systems-flow-diagram` enum while preserving CPM behavior in copy and renderer declarations.
