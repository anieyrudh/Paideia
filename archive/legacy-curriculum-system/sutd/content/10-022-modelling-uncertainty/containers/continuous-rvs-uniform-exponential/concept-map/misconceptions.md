# Misconceptions

- `f(x)` is not `P(X=x)` for a continuous random variable.
- A constant density only applies on the declared bounded support.
- Exponential waiting time is unbounded and memoryless; it is not uniform over a chosen viewing window.
