# Signal Filter Frequency Response · Technical Record

> **Auto-generated by `pnpm container:docs`. Updated by every product PR.**
> Source: `container.yaml`, sim source declarations, and preserved review sections.

## Architecture

- Container path: `sutd/content/epd/containers/signal-filter-frequency-response`
- Status: `reviewed`
- Concept card: `concept-card.md`
- Concept map: `concept-map/concept-map.yaml`
- Simulation spec: `simulation/simulation.yaml`
- Embed API: `embed/api.ts`
- Problem-solving algorithm: `problem-solving/algorithm.md`

## Imports

| Sim | Module | Symbols / role |
|---|---|---|
| signal-filter-frequency-response | `core/shared` | Declared in `simulation/simulation.yaml` |
| signal-filter-frequency-response | `core/content-schema` | Declared in `simulation/simulation.yaml` |
| signal-filter-frequency-response | `core/sim-runtime` | Declared in `simulation/simulation.yaml` |
| signal-filter-frequency-response | `core/control-systems` | Declared in `simulation/simulation.yaml` |
| signal-filter-frequency-response | `core/circuits` | Declared in `simulation/simulation.yaml` |
| signal-filter-frequency-response | `core/charting` | Declared in `simulation/simulation.yaml` |
| signal-filter-frequency-response | `core/prediction-gate` | Declared in `simulation/simulation.yaml` |
| signal-filter-frequency-response | `core/ui-sim` | Declared in `simulation/simulation.yaml` |

## SimulationSpec (frozen)

Full validated YAML for the declared sim in this container at the time of last docs regeneration.

```yaml
id: signal-filter-frequency-response
title: RC Filter Frequency Response Lab
interaction_type: comparative-matrix
kernel_deps:
  - core/shared
  - core/content-schema
  - core/sim-runtime
  - core/control-systems
  - core/circuits
  - core/charting
  - core/prediction-gate
  - core/ui-sim
manipulate:
  controls:
    - id: filter-mode
      label: Filter mode
      kind: selector
      kernel_binding: state.mode
    - id: resistance
      label: Resistance
      kind: slider
      kernel_binding: state.resistanceKiloOhms
      bounds:
        min: 1
        max: 47
        step: 1
    - id: capacitance
      label: Capacitance
      kind: slider
      kernel_binding: state.capacitanceMicroFarads
      bounds:
        min: 0.01
        max: 0.47
        step: 0.01
    - id: probe-frequency
      label: Probe frequency
      kind: slider
      kernel_binding: state.probeFrequencyHertz
      bounds:
        min: 20
        max: 20000
        step: 20
predict:
  prompt: |
    For a one-pole RC low-pass filter, what happens to the output at exactly the cutoff frequency?
  commit_format:
    kind: multiple-choice
    options:
      - The output becomes zero because frequencies above cutoff are removed instantly
      - The output is about 0.707 of the input and the phase has already shifted
      - The output is unchanged because cutoff only affects phase
      - The output doubles because the capacitor stores extra charge
    correct_index: 1
  rationale_required: true
observe:
  renderers:
    - id: filter-bode-readout
      module: "@paideia/sutd-sims/signal-filter-frequency-response"
      symbol: SignalFilterFrequencyResponse
      props_binding: |
        Show magnitude and phase traces for the selected RC filter and explain the cutoff-frequency substitution.
explain:
  prompt: |
    Why is cutoff a gradual response marker rather than an on/off boundary, and why must the phase trace be read with the magnitude trace?
  socratic: true
  expected_misconceptions_surfaced:
    - Filters remove all frequencies above cutoff instantly
    - Phase shift is optional decoration
```

## Kernel extensions

Declared runtime kernel dependencies are listed above. Any additive or breaking core API change must be linked in the preserved review section below.

## Accessibility

- Prediction checkpoint contract: required and tested when the sim is registered.
- Route-level axe coverage: record the latest shell or container-specific result in the preserved review section below.
- Media fallback: `media/fallback.svg` present.

## Tests

- Container validation: `pnpm container:validate sutd/content/epd/containers/signal-filter-frequency-response`
- Docs regeneration: `pnpm container:docs sutd/content/epd/containers/signal-filter-frequency-response`
- Prediction-checkpoint test: `simulation/simulation.test.ts`
- Package or shell tests: record exact commands in the preserved validation section below.

## How to run locally

```bash
pnpm container:docs sutd/content/epd/containers/signal-filter-frequency-response
pnpm container:validate sutd/content/epd/containers/signal-filter-frequency-response
pnpm graph:generate
```

## Anieyrudh Filter pass

Date: 2026-05-21
Filter version: aniegpt v1.0
Reviewers: container-auditor, sim-architect, pedagogy-reviewer

### P0 issues

- None open. The container has canonical v2 layout, prediction-checkpointd observation, declared kernel dependencies, and generated docs.

### P1 issues

- None open. Formula, substitution, units, interpretation, and legend are visible in the student-facing sim.

### High-bandwidth questions surfaced

- Should a future shared filter-design container cover multi-pole filters and active op-amp filters once the EPD sequence needs steeper roll-off than one-pole RC behavior?

## Iteration log

Brief log of what the agent did and what was rejected during authoring. This
is the "What AI did / what I rejected" record from the PR template, persisted.

- Claimed only `sutd.epd.signal-filter-frequency-response` by moving its queue status to `in-build`.
- Built a SUTD EPD v2 container under `sutd/content/epd/containers/signal-filter-frequency-response`.
- Added one SUTD sim package surface using `core/control-systems`, `core/circuits`, `core/charting`, `core/prediction-gate`, `core/shared`, `core/sim-runtime`, and `core/ui-sim`.
- Rejected an inline-only RC implementation; transfer-function evaluation and circuit phasor checks are delegated to core kernels.
- Regenerated README.md and TECHNICAL.md with `pnpm container:docs`.
