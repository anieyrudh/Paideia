runtime:
  pmoe_t:
    predict: "Learner commits which mode should dominate before seeing coefficients or error."
    manipulate: "Learner selects the target family and sets the first four coefficients."
    observe: "Lab reveals reconstruction, projection coefficients, RMS error, and formula substitution."
    explain: "Learner connects coefficient sign and residual shape to a transfer PDE context."
  prediction_gate:
    required: true
    blocks:
      - projection coefficients
      - reconstruction error
      - target/reconstruction chart
      - formula panel
visual_quality:
  setup: []
  prediction:
    option_label: "Mode 1, the longest single arch"
    rationale: "The default centre pluck is mostly a single positive arch, so its projection onto sin(pi x / L) dominates."
  observation:
    observation_label: "Observation unlocked"
    visual: required
    formula: required
